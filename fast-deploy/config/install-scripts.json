{
  "scripts": {
    "node": {
      "windows": "winget install OpenJS.NodeJS.LTS --accept-package-agreements",
      "linux": "sudo apt update && sudo apt install -y nodejs npm || sudo dnf install -y nodejs npm",
      "ubuntu22": "sudo rm -f /etc/apt/sources.list.d/caddy-stable.list 2>/dev/null; sudo apt update && sudo apt install -y ca-certificates curl gnupg && sudo mkdir -p /etc/apt/keyrings && curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key | sudo gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg && echo 'deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_22.x nodistro main' | sudo tee /etc/apt/sources.list.d/nodesource.list && sudo apt update && sudo apt install -y nodejs",
      "debian": "fast-deploy 在 Debian 上由 install_node_nodesource_deb 安装（USE_MIRROR=1 时 NodeSource 走国内备用源）",
      "rhel": "fast-deploy 在 RHEL/CentOS/Rocky/Alma 上由 install_node_nodesource_rpm 安装（USE_MIRROR=1 时 NodeSource 走国内备用源）",
      "fedora": "fast-deploy 在 Fedora 上由 install_node_nodesource_rpm 安装（USE_MIRROR=1 时 NodeSource 走国内备用源）"
    },
    "python": {
      "windows": "winget install Python.Python.3.12 --accept-package-agreements",
      "linux": "sudo apt install -y python3.12 python3.12-venv python3-pip || sudo dnf install -y python3.12",
      "ubuntu22": "sudo rm -f /etc/apt/sources.list.d/caddy-stable.list 2>/dev/null; sudo apt update && sudo apt install -y software-properties-common && sudo add-apt-repository -y ppa:deadsnakes/ppa && sudo apt update && sudo apt install -y python3.12 python3.12-venv && curl -sS https://bootstrap.pypa.io/get-pip.py | python3.12",
      "debian": "sudo apt update && sudo apt install -y python3.12 python3.12-venv || sudo apt install -y python3 python3-venv python3-pip",
      "rhel": "fast-deploy 在 RHEL/CentOS/Rocky/Alma 上由 install_python_rhel 安装（EL8 自动尝试启用 CRB/PowerTools）",
      "fedora": "fast-deploy 在 Fedora 上由 install_python_rhel 安装"
    },
    "uv": {
      "windows": "powershell -c \"irm https://astral.sh/uv/install.ps1 | iex\"",
      "linux": "curl -LsSf https://astral.sh/uv/install.sh | sh",
      "ubuntu22": "curl -LsSf https://astral.sh/uv/install.sh | sh",
      "debian": "fast-deploy 由 install_uv_shell 安装（官方脚本 + ghproxy 备用）",
      "rhel": "fast-deploy 由 install_uv_shell 安装（官方脚本 + ghproxy 备用）",
      "fedora": "fast-deploy 由 install_uv_shell 安装（官方脚本 + ghproxy 备用）"
    },
    "postgresql": {
      "windows": "winget install PostgreSQL.PostgreSQL --accept-package-agreements",
      "linux": "sudo apt install -y postgresql postgresql-contrib || sudo dnf install -y postgresql-server postgresql-contrib",
      "ubuntu22": "fast-deploy 在 Debian/Ubuntu 上由 install_postgresql_pgdg 安装（USE_MIRROR=1 时走阿里云 PGDG 镜像）",
      "debian": "fast-deploy 在 Debian 上由 install_postgresql_pgdg 安装（USE_MIRROR=1 时走阿里云 PGDG 镜像）",
      "rhel": "fast-deploy 在 RHEL/CentOS/Rocky/Alma 上由 install_postgresql_pgdg_rhel 安装（USE_MIRROR=1 时走阿里云 PGDG yum 镜像）",
      "fedora": "fast-deploy 在 Fedora 上由 install_postgresql_pgdg_rhel 安装（USE_MIRROR=1 时走阿里云 PGDG yum 镜像）"
    },
    "caddy": {
      "windows": "winget install -e --id CaddyServer.Caddy --accept-package-agreements --accept-source-agreements",
      "linux": "fast-deploy 在 Debian/Ubuntu 上由 install_caddy_apt 安装（USE_MIRROR=1 时走国内 apt 镜像）",
      "ubuntu22": "fast-deploy 在 Debian/Ubuntu 上由 install_caddy_apt 安装（USE_MIRROR=1 时走国内 apt 镜像）",
      "debian": "fast-deploy 在 Debian 上由 install_caddy_apt 安装（USE_MIRROR=1 时走国内 apt 镜像）",
      "rhel": "fast-deploy 在 RHEL/CentOS/Rocky/Alma 上由 install_caddy_dnf 安装（USE_MIRROR=1 时走国内 rpm 镜像）",
      "fedora": "fast-deploy 在 Fedora 上由 install_caddy_dnf 安装（USE_MIRROR=1 时走国内 rpm 镜像）"
    },
    "zbar": {
      "windows": "fast-deploy 由 ensure_pyzbar_windows_native 重装 pyzbar（自带 libzbar DLL）",
      "linux": "sudo apt-get install -y libzbar0 libgomp1 libgl1 || sudo dnf install -y zbar libgomp mesa-libGL",
      "ubuntu22": "sudo apt-get update && sudo apt-get install -y libzbar0 libgomp1 libgl1",
      "debian": "sudo apt-get update && sudo apt-get install -y libzbar0 libgomp1 libgl1",
      "rhel": "sudo dnf install -y zbar libgomp mesa-libGL || sudo yum install -y zbar libgomp mesa-libGL",
      "fedora": "sudo dnf install -y zbar libgomp mesa-libGL"
    },
    "invoice-runtime": {
      "windows": "fast-deploy：pyzbar DLL + uv sync --extra ocr",
      "linux": "sudo apt-get install -y libzbar0 libgomp1 libgl1 && uv sync --extra ocr",
      "ubuntu22": "sudo apt-get update && sudo apt-get install -y libzbar0 libgomp1 libgl1",
      "debian": "sudo apt-get update && sudo apt-get install -y libzbar0 libgomp1 libgl1",
      "rhel": "sudo dnf install -y zbar libgomp mesa-libGL || sudo yum install -y zbar libgomp mesa-libGL",
      "fedora": "sudo dnf install -y zbar libgomp mesa-libGL"
    }
  },
  "scripts_cn": {
    "node": "sudo apt update && sudo apt install -y nodejs npm",
    "python": "sudo apt update && sudo apt install -y python3 python3-venv python3-pip",
    "uv": "pip3 install -i https://pypi.tuna.tsinghua.edu.cn/simple uv 2>/dev/null || python3 -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple uv",
    "zbar": "sudo apt-get update && sudo apt-get install -y libzbar0 libgomp1 libgl1",
    "invoice-runtime": "sudo apt-get update && sudo apt-get install -y libzbar0 libgomp1 libgl1"
  }
}
