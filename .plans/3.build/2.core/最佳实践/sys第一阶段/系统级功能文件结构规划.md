# 系统级功能文件结构规划

## 📋 概述

本文档规划系统级功能模块的文件组织结构，明确各功能模块在项目中的位置。

**框架命名哲学**：
- `soil/` - 平台级功能（土壤，平台运营和管理）
- `tree-root/` - 系统级功能（根，组织内系统功能）

---

## 🗂️ 后端文件结构

### 系统级功能位置：`src/tree-root/`

```
riveredge-backend/src/tree-root/
├── __init__.py
├── api/                      # 系统级 API 路由
│   ├── __init__.py
│   ├── deps/                 # API 依赖（认证、权限检查等）
│   │   ├── __init__.py
│   │   └── deps.py
│   ├── roles/                # 角色管理 API
│   │   ├── __init__.py
│   │   └── roles.py
│   ├── permissions/          # 权限管理 API
│   │   ├── __init__.py
│   │   └── permissions.py
│   ├── departments/          # 部门管理 API
│   │   ├── __init__.py
│   │   └── departments.py
│   ├── positions/            # 职位管理 API
│   │   ├── __init__.py
│   │   └── positions.py
│   ├── users/                # 账户管理 API
│   │   ├── __init__.py
│   │   └── users.py
│   └── menus/                # 菜单管理 API（暂缓）
│       ├── __init__.py
│       └── menus.py
│
├── models/                   # 系统级数据模型
│   ├── __init__.py
│   ├── base.py               # 基础模型（包含 tenant_id）
│   ├── role.py               # 角色模型
│   ├── permission.py         # 权限模型
│   ├── role_permission.py   # 角色-权限关联模型
│   ├── user_role.py          # 用户-角色关联模型
│   ├── department.py         # 部门模型
│   ├── position.py            # 职位模型
│   └── menu.py                # 菜单模型（暂缓）
│
├── schemas/                  # 系统级数据验证（Pydantic Schema）
│   ├── __init__.py
│   ├── role.py               # 角色 Schema（Create、Update、Response）
│   ├── permission.py         # 权限 Schema
│   ├── department.py         # 部门 Schema
│   ├── position.py            # 职位 Schema
│   ├── user.py               # 用户 Schema（扩展）
│   └── menu.py               # 菜单 Schema（暂缓）
│
├── services/                 # 系统级业务服务
│   ├── __init__.py
│   ├── role_service.py       # 角色服务
│   ├── permission_service.py # 权限服务
│   ├── department_service.py  # 部门服务
│   ├── position_service.py   # 职位服务
│   ├── user_service.py       # 用户服务（扩展）
│   └── menu_service.py       # 菜单服务（暂缓）
│
├── core/                     # 系统级核心功能
│   ├── __init__.py
│   ├── permissions/          # 权限验证核心
│   │   ├── __init__.py
│   │   ├── middleware.py     # 权限验证中间件
│   │   ├── decorator.py      # 权限装饰器
│   │   └── rbac.py           # RBAC 核心逻辑
│   └── utils/                # 工具函数
│       ├── __init__.py
│       └── import_export.py  # 导入导出工具
│
└── README.md                 # 系统级功能说明文档
```

---

## 📁 各功能模块详细文件结构

### 1. 角色权限管理

**模块路径**：`src/tree-root/`

**文件列表**：
```
api/
├── roles/
│   └── roles.py              # 角色管理 API（7个接口）
└── permissions/
    └── permissions.py        # 权限管理 API（2个接口）

models/
├── role.py                   # Role 模型
├── permission.py            # Permission 模型
├── role_permission.py       # RolePermission 关联模型
└── user_role.py             # UserRole 关联模型

schemas/
├── role.py                   # RoleCreate, RoleUpdate, RoleResponse
└── permission.py             # PermissionResponse

services/
├── role_service.py           # RoleService
└── permission_service.py     # PermissionService

core/
└── permissions/
    ├── middleware.py         # 权限验证中间件
    ├── decorator.py          # @require_permission 装饰器
    └── rbac.py               # RBAC 核心逻辑
```

---

### 2. 部门管理

**模块路径**：`src/tree-root/`

**文件列表**：
```
api/
└── departments/
    └── departments.py        # 部门管理 API（7个接口）

models/
└── department.py             # Department 模型（自关联）

schemas/
└── department.py             # DepartmentCreate, DepartmentUpdate, DepartmentResponse

services/
└── department_service.py     # DepartmentService
```

---

### 3. 职位管理

**模块路径**：`src/tree-root/`

**文件列表**：
```
api/
└── positions/
    └── positions.py          # 职位管理 API（5个接口）

models/
└── position.py               # Position 模型

schemas/
└── position.py               # PositionCreate, PositionUpdate, PositionResponse

services/
└── position_service.py       # PositionService
```

---

### 4. 账户管理

**模块路径**：`src/tree-root/`

**文件列表**：
```
api/
└── users/
    └── users.py              # 账户管理 API（9个接口）

models/
└── user.py                  # User 模型（扩展，在 soil/models/user.py 基础上扩展）

schemas/
└── user.py                  # UserCreate, UserUpdate, UserResponse, UserImport, UserExport

services/
└── user_service.py          # UserService（扩展）

core/
└── utils/
    └── import_export.py     # Excel 导入导出工具（使用 openpyxl）
```

**⚠️ 注意**：
- `User` 模型基础定义在 `soil/models/user.py`
- 系统级功能在 `tree-root/models/user.py` 中扩展（添加部门、职位、角色关联）
- 或直接在 `soil/models/user.py` 中扩展（推荐，避免重复）

---

### 5. 菜单管理（暂缓）

**模块路径**：`src/tree-root/`

**文件列表**：
```
api/
└── menus/
    └── menus.py              # 菜单管理 API（暂缓）

models/
└── menu.py                   # Menu 模型（暂缓）

schemas/
└── menu.py                   # MenuCreate, MenuUpdate, MenuResponse（暂缓）

services/
└── menu_service.py           # MenuService（暂缓）
```

---

## 🎨 前端文件结构

### 系统级功能位置：`src/tree-stem/pages/system/`

```
riveredge-frontend/src/tree-stem/pages/system/
├── roles/                    # 角色管理
│   ├── list/
│   │   └── index.tsx        # 角色列表页面
│   └── permissions/
│       └── index.tsx        # 角色权限分配页面
│
├── permissions/              # 权限管理
│   └── list/
│       └── index.tsx        # 权限列表页面
│
├── departments/              # 部门管理
│   ├── list/
│   │   └── index.tsx        # 部门列表页面（树形结构）
│   └── detail/
│       └── index.tsx        # 部门详情页面（可选）
│
├── positions/                # 职位管理
│   └── list/
│       └── index.tsx        # 职位列表页面
│
├── users/                    # 账户管理
│   ├── list/
│   │   └── index.tsx        # 账户列表页面
│   └── detail/
│       └── index.tsx        # 账户详情页面（可选）
│
└── menus/                    # 菜单管理（暂缓）
    └── list/
        └── index.tsx        # 菜单列表页面（树形结构，暂缓）
```

---

## 📝 服务文件结构

### 前端服务位置：`src/tree-stem/services/`

```
riveredge-frontend/src/tree-stem/services/
├── role.ts                   # 角色管理 API 服务
├── permission.ts             # 权限管理 API 服务
├── department.ts             # 部门管理 API 服务
├── position.ts               # 职位管理 API 服务
└── user.ts                   # 账户管理 API 服务（扩展）
```

---

## 🔗 模块依赖关系

### 后端依赖

```
tree-root/
├── models/
│   ├── role.py              # 独立模块
│   ├── permission.py        # 独立模块
│   ├── department.py        # 独立模块
│   ├── position.py          # 依赖：department.py
│   └── user.py              # 依赖：role.py, department.py, position.py
│
├── services/
│   ├── role_service.py      # 依赖：models/role.py
│   ├── permission_service.py # 依赖：models/permission.py
│   ├── department_service.py # 依赖：models/department.py
│   ├── position_service.py   # 依赖：models/position.py, models/department.py
│   └── user_service.py      # 依赖：models/user.py, 所有其他 models
│
└── api/
    ├── roles/               # 依赖：services/role_service.py
    ├── permissions/         # 依赖：services/permission_service.py
    ├── departments/         # 依赖：services/department_service.py
    ├── positions/           # 依赖：services/position_service.py
    └── users/               # 依赖：services/user_service.py
```

### 前端依赖

```
pages/system/
├── roles/                   # 依赖：services/role.ts, services/permission.ts
├── permissions/             # 依赖：services/permission.ts
├── departments/             # 依赖：services/department.ts
├── positions/               # 依赖：services/position.ts, services/department.ts
└── users/                   # 依赖：services/user.ts, services/role.ts, services/department.ts, services/position.ts
```

---

## 📊 文件命名规范

### 后端文件命名

**模型文件**：使用单数形式
- ✅ `role.py` - 角色模型
- ✅ `permission.py` - 权限模型
- ✅ `department.py` - 部门模型
- ❌ `roles.py` - 错误

**API 文件**：使用复数形式
- ✅ `roles.py` - 角色管理 API
- ✅ `permissions.py` - 权限管理 API
- ✅ `departments.py` - 部门管理 API
- ❌ `role.py` - 错误

**Service 文件**：使用单数形式 + `_service`
- ✅ `role_service.py` - 角色服务
- ✅ `permission_service.py` - 权限服务
- ✅ `department_service.py` - 部门服务
- ❌ `roles_service.py` - 错误

**Schema 文件**：使用单数形式
- ✅ `role.py` - 角色 Schema
- ✅ `permission.py` - 权限 Schema
- ✅ `department.py` - 部门 Schema
- ❌ `roles.py` - 错误

### 前端文件命名

**页面文件**：使用复数形式
- ✅ `pages/system/roles/list/index.tsx` - 角色列表
- ✅ `pages/system/departments/list/index.tsx` - 部门列表
- ❌ `pages/system/role/list/index.tsx` - 错误

**服务文件**：使用单数形式
- ✅ `services/role.ts` - 角色服务
- ✅ `services/department.ts` - 部门服务
- ❌ `services/roles.ts` - 错误

---

## 🚀 实施顺序

### 第一阶段：基础权限体系

**实施顺序**：
1. **角色权限管理** → `tree-root/api/roles/`, `tree-root/api/permissions/`
2. **部门管理** → `tree-root/api/departments/`
3. **职位管理** → `tree-root/api/positions/`
4. **账户管理** → `tree-root/api/users/`

**文件创建顺序**：
1. 创建 `models/` 文件（数据库模型）
2. 创建 `schemas/` 文件（数据验证）
3. 创建 `services/` 文件（业务逻辑）
4. 创建 `api/` 文件（API 路由）
5. 创建前端 `pages/` 文件（页面组件）
6. 创建前端 `services/` 文件（API 服务）

---

## 📚 相关文档

- [1.角色权限管理最佳实践.md](./1.角色权限管理最佳实践.md)
- [2.部门管理最佳实践.md](./2.部门管理最佳实践.md)
- [3.职位管理最佳实践.md](./3.职位管理最佳实践.md)
- [4.账户管理最佳实践.md](./4.账户管理最佳实践.md)
- [前端页面布局规范.md](./前端页面布局规范.md)
- [项目结构规范.md](../../2.rules/5.项目结构规范.md)

---

## ✅ 文件结构检查清单

### 后端检查

- [ ] 创建 `tree-root/` 目录结构
- [ ] 创建 `tree-root/models/` 目录和模型文件
- [ ] 创建 `tree-root/schemas/` 目录和 Schema 文件
- [ ] 创建 `tree-root/services/` 目录和服务文件
- [ ] 创建 `tree-root/api/` 目录和 API 文件
- [ ] 创建 `tree-root/core/permissions/` 权限验证核心

### 前端检查

- [ ] 创建 `pages/system/` 目录结构
- [ ] 创建各模块的列表页面
- [ ] 创建各模块的服务文件
- [ ] 集成 `UniTable` 组件
- [ ] 参考组织管理页面布局

---

**最后更新**：2025-01-XX

