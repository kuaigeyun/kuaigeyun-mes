# 数据源管理最佳实践

## 📋 模块概述

**模块名称**：数据源管理（DataSource Management）  
**优先级**：⭐⭐⭐⭐（高优先级）  
**预计时间**：2-3 周  
**依赖**：无（独立功能）

**功能范围**：
- 数据源定义（数据源名称、类型、连接配置）
- 数据源管理（CRUD）
- 数据源连接测试（测试连接、连接状态）
- 多数据源支持（PostgreSQL、MySQL、MongoDB、API等）

---

## 🔍 库选择评估

### 数据源管理实现方案

**结论**：✅ **使用自定义实现 + SQLAlchemy（可选）**

#### 方案一：自定义实现 + SQLAlchemy ⭐⭐⭐⭐（推荐）

**优势**：
- ✅ 功能完整，支持多种数据库类型
- ✅ SQLAlchemy 支持多种数据库（PostgreSQL、MySQL、SQLite等）
- ✅ 多租户隔离天然支持（tenant_id 字段）
- ✅ 完全符合框架规范（命名、注释、代码风格）
- ✅ 支持连接池管理

**劣势**：
- ⚠️ SQLAlchemy 是可选依赖，增加项目复杂度
- ⚠️ 需要自己实现连接测试逻辑

**适用场景**：
- ✅ 需要支持多种数据库类型
- ✅ 需要连接池管理
- ✅ 需要多租户数据源隔离

**实现复杂度**：⭐⭐⭐（中等，2-3 周可完成）

---

#### 方案二：仅支持 PostgreSQL ⭐⭐⭐（简化方案）

**优势**：
- ✅ 实现简单，使用 Tortoise ORM（已使用）
- ✅ 无需额外依赖
- ✅ 多租户隔离天然支持

**劣势**：
- ⚠️ 仅支持 PostgreSQL，不支持其他数据库

**适用场景**：
- ✅ 仅需支持 PostgreSQL
- ✅ 需要快速实现

**实现复杂度**：⭐⭐（简单，1-2 周可完成）

---

### 最终推荐方案

**推荐**：✅ **优先支持 PostgreSQL，可选支持其他数据库**

**理由**：
1. ✅ **PostgreSQL 是主数据库**，优先支持
2. ✅ **其他数据库可选**，根据实际需求决定是否支持
3. ✅ **SQLAlchemy 可选**，仅在需要支持多种数据库时安装
4. ✅ **多租户是核心需求**，自定义实现天然支持
5. ✅ **完全符合框架规范**，代码风格统一

---

## 🗄️ 数据库设计

### 数据源表（root_data_sources）

```sql
CREATE TABLE root_data_sources (
    id SERIAL PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,        -- 业务ID（UUID，对外暴露，安全且唯一）
    tenant_id INTEGER NOT NULL,
    
    -- 数据源基本信息
    name VARCHAR(100) NOT NULL,               -- 数据源名称
    code VARCHAR(50) NOT NULL,                -- 数据源代码（唯一，用于程序识别）
    description TEXT,                         -- 数据源描述
    type VARCHAR(20) NOT NULL,                -- 数据源类型（postgresql、mysql、mongodb、api等）
    
    -- 连接配置（JSON格式，根据类型不同而不同）
    config JSONB NOT NULL,                    -- 连接配置（JSON格式）
    
    -- 连接状态
    is_active BOOLEAN DEFAULT TRUE,           -- 是否启用
    is_connected BOOLEAN DEFAULT FALSE,      -- 是否已连接
    last_connected_at TIMESTAMP NULL,         -- 最后连接时间
    last_error TEXT,                          -- 最后连接错误信息
    
    -- 标准字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,
    
    -- 索引
    CONSTRAINT uk_root_data_sources_tenant_code UNIQUE (tenant_id, code),
    INDEX idx_root_data_sources_tenant_id (tenant_id),
    INDEX idx_root_data_sources_uuid (uuid),
    INDEX idx_root_data_sources_code (code),
    INDEX idx_root_data_sources_type (type),
    INDEX idx_root_data_sources_created_at (created_at)
);
```

**设计要点**：
- ✅ **混合ID方案**：`id`（自增ID，内部使用）+ `uuid`（UUID，对外暴露）
- ✅ `code` 字段用于程序识别，与 `name` 分离（支持多语言）
- ✅ `type` 字段标识数据源类型（postgresql、mysql、mongodb、api等）
- ✅ `config` 字段使用 JSONB 存储连接配置（根据类型不同而不同）
- ✅ `is_connected` 和 `last_connected_at` 用于跟踪连接状态
- ✅ `tenant_id + code` 唯一约束，确保组织内数据源代码唯一
- ✅ 软删除支持（`deleted_at`）

**连接配置示例**：

**PostgreSQL**：
```json
{
  "host": "localhost",
  "port": 5432,
  "database": "mydb",
  "username": "user",
  "password": "password",
  "pool_size": 10
}
```

**MySQL**：
```json
{
  "host": "localhost",
  "port": 3306,
  "database": "mydb",
  "username": "user",
  "password": "password",
  "charset": "utf8mb4"
}
```

**MongoDB**：
```json
{
  "host": "localhost",
  "port": 27017,
  "database": "mydb",
  "username": "user",
  "password": "password"
}
```

**API**：
```json
{
  "base_url": "https://api.example.com",
  "auth_type": "bearer",
  "token": "token_value",
  "headers": {
    "Content-Type": "application/json"
  }
}
```

---

## 🐍 后端模型设计

### 数据源模型（DataSource）

```python
# riveredge-backend/src/tree_root/models/data_source.py
from tortoise.models import Model
from tortoise import fields
from typing import Optional, Dict, Any
from .base import BaseModel


class DataSource(BaseModel):
    """
    数据源模型
    
    用于定义和管理组织内的数据源，支持多种数据库类型和API数据源。
    支持多组织隔离，每个组织的数据源相互独立。
    
    注意：继承自 BaseModel，自动包含 uuid、tenant_id、created_at、updated_at 字段。
    """
    id = fields.IntField(pk=True, description="数据源ID（主键，自增ID，内部使用）")
    # uuid 字段由 BaseModel 提供
    # tenant_id 字段由 BaseModel 提供
    
    name = fields.CharField(max_length=100, description="数据源名称")
    code = fields.CharField(max_length=50, description="数据源代码（唯一，用于程序识别）")
    description = fields.TextField(null=True, description="数据源描述")
    type = fields.CharField(max_length=20, description="数据源类型（postgresql、mysql、mongodb、api等）")
    
    config = fields.JSONField(description="连接配置（JSON格式）")
    
    is_active = fields.BooleanField(default=True, description="是否启用")
    is_connected = fields.BooleanField(default=False, description="是否已连接")
    last_connected_at = fields.DatetimeField(null=True, description="最后连接时间")
    last_error = fields.TextField(null=True, description="最后连接错误信息")
    
    class Meta:
        table = "root_data_sources"
        indexes = [
            ("tenant_id", "code"),  # 唯一索引
            ("type",),
        ]
        unique_together = [("tenant_id", "code")]
    
    def __str__(self):
        return f"{self.name} ({self.type})"
```

---

## 📝 Schema 设计

### 数据源 Schema

```python
# riveredge-backend/src/tree_root/schemas/data_source.py
from pydantic import BaseModel, Field, ConfigDict, field_validator
from typing import Optional, Dict, Any
from datetime import datetime
from uuid import UUID


class DataSourceBase(BaseModel):
    """数据源基础 Schema"""
    name: str = Field(..., max_length=100, description="数据源名称")
    code: str = Field(..., max_length=50, description="数据源代码")
    description: Optional[str] = Field(None, description="数据源描述")
    type: str = Field(..., max_length=20, description="数据源类型")
    config: Dict[str, Any] = Field(..., description="连接配置")
    is_active: bool = Field(True, description="是否启用")
    
    @field_validator('type')
    def validate_type(cls, v):
        """验证数据源类型"""
        allowed_types = ['postgresql', 'mysql', 'mongodb', 'api']
        if v not in allowed_types:
            raise ValueError(f'数据源类型必须是 {allowed_types} 之一')
        return v


class DataSourceCreate(DataSourceBase):
    """创建数据源 Schema"""
    pass


class DataSourceUpdate(BaseModel):
    """更新数据源 Schema"""
    name: Optional[str] = Field(None, max_length=100, description="数据源名称")
    code: Optional[str] = Field(None, max_length=50, description="数据源代码")
    description: Optional[str] = Field(None, description="数据源描述")
    type: Optional[str] = Field(None, max_length=20, description="数据源类型")
    config: Optional[Dict[str, Any]] = Field(None, description="连接配置")
    is_active: Optional[bool] = Field(None, description="是否启用")


class DataSourceResponse(DataSourceBase):
    """数据源响应 Schema"""
    uuid: UUID = Field(..., description="数据源UUID")
    tenant_id: int = Field(..., description="组织ID")
    is_connected: bool = Field(..., description="是否已连接")
    last_connected_at: Optional[datetime] = Field(None, description="最后连接时间")
    last_error: Optional[str] = Field(None, description="最后连接错误信息")
    created_at: datetime = Field(..., description="创建时间")
    updated_at: datetime = Field(..., description="更新时间")
    
    model_config = ConfigDict(from_attributes=True)


class TestConnectionResponse(BaseModel):
    """测试连接响应 Schema"""
    success: bool = Field(..., description="连接是否成功")
    message: str = Field(..., description="连接消息")
    elapsed_time: float = Field(..., description="连接耗时（秒）")
```

---

## 🔧 Service 层设计

### 数据源服务（DataSourceService）

**核心功能**：
1. 数据源 CRUD（创建、读取、更新、删除）
2. 连接测试（根据类型测试连接）
3. 连接状态跟踪（更新连接状态、错误信息）

**关键实现要点**：
- ✅ PostgreSQL：使用 Tortoise ORM 或 SQLAlchemy 测试连接
- ✅ MySQL：使用 SQLAlchemy 测试连接
- ✅ MongoDB：使用 pymongo 测试连接
- ✅ API：使用 httpx 测试连接
- ✅ 连接测试结果包含成功/失败、消息、耗时等信息

---

## 🌐 API 设计

### 数据源管理 API

**路由前缀**：`/api/tree-root/data-sources`

**接口列表**：
1. `POST /` - 创建数据源
2. `GET /list` - 获取数据源列表（分页、搜索、筛选）
3. `GET /{uuid}` - 获取数据源详情
4. `PUT /{uuid}` - 更新数据源
5. `DELETE /{uuid}` - 删除数据源
6. `POST /{uuid}/test` - 测试数据源连接

---

## 🎨 前端页面设计

### 数据源管理页面

**页面路径**：`pages/system/data-sources/list/index.tsx`

**核心功能**：
1. 数据源列表展示（UniTable，支持表格视图）
2. 数据源创建/编辑（Modal + ProForm，根据类型动态渲染配置表单）
3. 数据源连接测试（测试连接、显示测试结果）
4. 数据源删除（单个删除、批量删除）
5. 数据源搜索和筛选（按名称、类型）

**布局规范**：
- ✅ 使用 `UniTable` 组件展示列表
- ✅ 使用高级搜索（`showAdvancedSearch={true}`）
- ✅ 使用 `searchFormValues` 获取搜索条件
- ✅ 创建/编辑使用 Modal + ProForm
- ✅ 连接测试使用 Drawer 展示测试结果

---

## ⚠️ 重要注意事项

### 1. 吸取第一、第二阶段经验教训

**必须遵守的规范**：
1. ✅ **禁止假设和猜测**：必须直接查看实际代码，使用完全相同的值
2. ✅ **禁止擅作主张**：只做用户明确要求的事，不做额外"优化"
3. ✅ **数据库迁移规范**：必须使用 Aerich 迁移系统，禁止直接使用 SQL
4. ✅ **前端搜索规范**：必须使用高级搜索（`showAdvancedSearch={true}`），使用 `searchFormValues` 获取搜索条件
5. ✅ **UniTable 规范**：`request` 函数必须接收四个参数：`(params, sort, _filter, searchFormValues)`

### 2. 数据源连接测试注意事项

**连接安全**：
- 连接配置包含敏感信息（密码、token等），需要加密存储（可选）
- 连接测试仅限组织内数据源
- 禁止测试外部数据源（防止安全风险）

**连接池管理**：
- 使用连接池管理数据库连接
- 连接池大小根据实际需求配置
- 连接超时时间合理设置

---

## 📚 相关文档

- [前端页面布局规范.md](./前端页面布局规范.md)
- [系统级功能文件结构规划.md](./系统级功能文件结构规划.md)
- [建设进度.md](./建设进度.md)

---

**最后更新**：2025-01-XX

