# 页面迁移进度

**创建时间**: 2025-12-26  
**目标**: 将所有页面迁移到统一的布局模板

---

## ✅ 已迁移页面（34个）

**注意**: 所有带有卡片视图的系统级页面已统一迁移到 UniTable 的卡片视图功能，提升设计一致性。

### 系统级页面（31个）

1. ✅ `pages/system/positions/list/index.tsx` - 职位管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 移除硬编码的 Modal/Drawer 宽度，使用布局常量

2. ✅ `pages/system/roles/list/index.tsx` - 角色管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留权限分配Modal（特殊功能）

3. ✅ `pages/system/permissions/list/index.tsx` - 权限管理
   - 使用 `ListPageTemplate` + `DetailDrawerTemplate`
   - 只读页面，无编辑功能

4. ✅ `pages/system/languages/list/index.tsx` - 语言管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留翻译管理Drawer（特殊功能）

5. ✅ `pages/system/datasets/list/index.tsx` - 数据集管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留查询执行Modal（特殊功能）

6. ✅ `pages/system/integration-configs/list/index.tsx` - 集成配置管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留视图切换功能（卡片视图/列表视图）

7. ✅ `pages/system/scripts/list/index.tsx` - 脚本管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留执行脚本Modal（特殊功能）

8. ✅ `pages/system/scheduled-tasks/list/index.tsx` - 定时任务管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留启动/停止功能

9. ✅ `pages/system/print-templates/list/index.tsx` - 打印模板管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留视图切换功能（卡片视图/列表视图）
   - 保留渲染模板Modal（特殊功能）

10. ✅ `pages/system/print-devices/list/index.tsx` - 打印设备管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留视图切换功能（卡片视图/列表视图）
   - 保留测试连接和执行打印Modal（特殊功能）

11. ✅ `pages/system/data-sources/list/index.tsx` - 数据源管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留视图切换功能（卡片视图/列表视图）
   - 保留连接测试功能

12. ✅ `pages/system/invitation-codes/list/index.tsx` - 邀请码管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 移除硬编码的 Modal/Drawer 宽度，使用布局常量

13. ✅ `pages/system/apis/list/index.tsx` - API管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留接口测试Drawer（特殊功能）
   - 处理多个JSON字段（请求头、参数、体、响应格式、响应示例）

14. ✅ `pages/system/data-dictionaries/list/index.tsx` - 数据字典管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留字典项管理Drawer（特殊功能）

15. ✅ `pages/system/approval-processes/list/index.tsx` - 审批流程管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 处理JSON配置字段（节点配置、流程配置）

16. ✅ `pages/system/system-parameters/list/index.tsx` - 系统参数管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 移除分组表单视图（GroupedFormView）和视图切换（Tabs）
   - 集成UniTable的卡片视图功能（viewTypes: ['table', 'card']）
   - 实现cardViewConfig.renderCard渲染参数卡片
   - 默认视图设置为表格视图（table）
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留视图切换功能（分组表单视图/列表视图）
   - 处理不同参数类型的值输入（字符串、数字、布尔、JSON）

17. ✅ `pages/system/login-logs/index.tsx` - 登录日志
   - 使用 `ListPageTemplate`（带统计卡片）+ `DetailDrawerTemplate`
   - 将统计卡片转换为ListPageTemplate的statCards格式
   - 将详情Modal替换为DetailDrawerTemplate

18. ✅ `pages/system/operation-logs/index.tsx` - 操作日志
   - 使用 `ListPageTemplate`（带统计卡片）+ `DetailDrawerTemplate`
   - 将统计卡片转换为ListPageTemplate的statCards格式
   - 将详情Modal替换为DetailDrawerTemplate

19. ✅ `pages/system/online-users/index.tsx` - 在线用户
   - 使用 `ListPageTemplate`（带统计卡片）+ `DetailDrawerTemplate`
   - 保留视图切换功能（卡片视图/列表视图）
   - 将统计卡片转换为ListPageTemplate的statCards格式
   - 将详情Modal替换为DetailDrawerTemplate

20. ✅ `pages/system/users/list/index.tsx` - 用户管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 移除FormModal组件，使用FormModalTemplate
   - 保留所有原有功能（导入导出、重置密码等）

21. ✅ `pages/system/messages/config/index.tsx` - 消息配置管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 处理JSON配置字段

22. ✅ `pages/system/messages/template/index.tsx` - 消息模板管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 处理JSON变量字段

23. ✅ `pages/system/plugin-manager/index.tsx` - 插件管理
   - 使用 `ListPageTemplate` + `UniTable`
   - 移除ProTable，使用UniTable

24. ✅ `pages/system/applications/list/index.tsx` - 应用管理
   - 使用 `ListPageTemplate` + `DetailDrawerTemplate` + `UniTable`
   - 移除自定义的Row/Col卡片布局和搜索表单
   - 集成UniTable的卡片视图功能（viewTypes: ['table', 'card']）
   - 实现cardViewConfig.renderCard渲染应用卡片
   - 默认视图设置为卡片视图（card）
   - 将搜索和筛选功能集成到UniTable的request函数中

25. ✅ `pages/system/approval-processes/instances/index.tsx` - 审批流程实例
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留看板视图和列表视图切换

26. ✅ `pages/system/site-settings/index.tsx` - 站点设置
   - 使用 `ListPageTemplate`
   - 保留表单配置功能

27. ✅ `pages/system/departments/list/index.tsx` - 部门管理
   - 使用 `TwoColumnLayout` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留树形结构和拖拽排序功能

28. ✅ `pages/system/menus/index.tsx` - 菜单管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 保留树形结构

29. ✅ `pages/system/custom-fields/list/index.tsx` - 自定义字段管理
   - 使用 `FormModalTemplate` + `DetailDrawerTemplate` + `UniTable`
   - 使用PAGE_SPACING常量统一padding
   - 保留左右布局结构

30. ✅ `pages/system/code-rules/list/index.tsx` - 编码规则管理
   - 使用PAGE_SPACING常量统一padding
   - 保留左右布局和表达式构建器

31. ✅ `pages/system/data-backups/index.tsx` - 数据备份（已迁移到UniTable视图切换）

### 个人级页面（2个）

1. ✅ `pages/personal/messages/index.tsx` - 我的消息
   - 使用 `ListPageTemplate` + `UniTable` + `DetailDrawerTemplate`
   - 将统计卡片移到ListPageTemplate的statCards
   - 集成UniTable的卡片视图功能（viewTypes: ['table', 'card']）
   - 使用DetailDrawerTemplate替换Modal.info
   - 移除CardView组件的使用
   - 默认视图设置为表格视图（table）

2. ✅ `pages/personal/tasks/index.tsx` - 我的任务
   - 使用 `ListPageTemplate` + `UniTable` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 将统计卡片移到ListPageTemplate的statCards
   - 集成UniTable的看板视图功能（viewTypes: ['table', 'kanban']）
   - 使用FormModalTemplate替换自定义Modal
   - 使用DetailDrawerTemplate替换Modal.info
   - 根据taskType动态调整看板视图的状态分组
   - 默认视图设置为表格视图（table）

### 应用级页面（kuaizhizao）（24个）

1. ✅ `apps/kuaizhizao/pages/sales-management/sales-orders/index.tsx` - 销售订单管理
   - 使用 `ListPageTemplate`（带统计卡片）+ `DetailDrawerTemplate`
   - 移除硬编码的统计卡片布局，使用模板统一管理

2. ✅ `apps/kuaizhizao/pages/production-execution/work-orders/index.tsx` - 工单管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 移除硬编码的 Modal/Drawer 宽度，使用布局常量
   - 使用 `ProDescriptionsItemType` 定义详情列

3. ✅ `apps/kuaizhizao/pages/purchase-management/purchase-orders/index.tsx` - 采购订单管理
   - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate` + `DetailDrawerTemplate`
   - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
   - 合并创建和编辑 Modal 为一个 `FormModalTemplate`
   - 使用 `customContent` 自定义详情内容（包含订单明细表格）

4. ✅ `apps/kuaizhizao/pages/plan-management/production-plans/index.tsx` - 生产计划管理
   - 使用 `ListPageTemplate`（带统计卡片）+ `DetailDrawerTemplate`
   - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
   - 使用 `customContent` 自定义详情内容（包含计划明细表格）

5. ✅ `apps/kuaizhizao/pages/warehouse-management/inbound/index.tsx` - 入库管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 将Form.Item+原生组件替换为ProForm系列组件
   - 使用布局常量统一Modal/Drawer宽度

6. ✅ `apps/kuaizhizao/pages/warehouse-management/outbound/index.tsx` - 出库管理
   - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
   - 将原生select/input替换为ProForm系列组件
   - 使用布局常量统一Modal/Drawer宽度

7. ✅ `apps/kuaizhizao/pages/quality-management/incoming-inspection/index.tsx` - 来料检验
   - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate` + `DetailDrawerTemplate`
   - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
   - 使用ProFormDigit替换Input number，使用ProFormTextArea替换TextArea

8. ✅ `apps/kuaizhizao/pages/finance-management/accounts-receivable/index.tsx` - 应收管理
   - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate` + `DetailDrawerTemplate`
   - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
   - 使用ProFormSelect、ProFormDatePicker、ProFormTextArea替换原生组件

9. ✅ `apps/kuaizhizao/pages/warehouse-management/inventory/index.tsx` - 库存查询
   - 使用 `ListPageTemplate`（带统计卡片）
   - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式

10. ✅ `apps/kuaizhizao/pages/warehouse-management/finished-goods-inventory/index.tsx` - 成品库存入库
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 将ProForm嵌套Modal转换为FormModalTemplate，使用colProps布局

11. ✅ `apps/kuaizhizao/pages/quality-management/finished-goods-inspection/index.tsx` - 成品检验
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 使用ProFormDigit、ProFormTextArea替换Input，保留扫码Modal特殊功能

12. ✅ `apps/kuaizhizao/pages/finance-management/accounts-payable/index.tsx` - 应付管理
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate` + `DetailDrawerTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 使用ProFormSelect、ProFormDatePicker、ProFormTextArea替换原生组件
    - 修复字段名不一致问题（dueDate -> due_date）

13. ✅ `apps/kuaizhizao/pages/sales-management/sales-forecasts/index.tsx` - 销售预测
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate` + `DetailDrawerTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 合并创建和编辑Modal为一个FormModalTemplate
    - 使用customContent自定义详情内容（包含预测明细表格）

14. ✅ `apps/kuaizhizao/pages/warehouse-management/sales-outbound/index.tsx` - 销售出库
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 使用FormModalTemplate替换Modal+ProForm组合，使用colProps布局
    - 支持dependencies和hidden属性（订单类型联动显示销售订单字段）

15. ✅ `apps/kuaizhizao/pages/quality-management/process-inspection/index.tsx` - 过程检验
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 使用ProFormRadio.Group、ProFormSelect、ProFormTextArea、ProFormText替换原生组件
    - 保留扫码Modal特殊功能

16. ✅ `apps/kuaizhizao/pages/production-execution/reporting/index.tsx` - 报工管理
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 使用ProFormSelect、ProFormDigit、ProFormTextArea替换原生组件
    - 保留扫码Modal特殊功能

17. ✅ `apps/kuaizhizao/pages/plan-management/demand-management/index.tsx` - 需求管理
    - 使用 `ListPageTemplate` + `FormModalTemplate` + `DetailDrawerTemplate`
    - 合并创建和编辑Modal为一个FormModalTemplate
    - 使用customContent自定义详情内容（包含预测明细表格）

18. ✅ `apps/kuaizhizao/pages/plan-management/bill-of-materials/index.tsx` - BOM管理
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate` + `DetailDrawerTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 合并创建和编辑Modal为一个FormModalTemplate
    - 使用customContent自定义详情内容（包含BOM明细表格）
    - 保留展开Drawer特殊功能

19. ✅ `apps/kuaizhizao/pages/plan-management/lrp-computation/index.tsx` - LRP运算
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate` + `DetailDrawerTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 将Modal转换为FormModalTemplate，使用ProFormSelect替换原生Select
    - 将Drawer转换为DetailDrawerTemplate，使用customContent自定义详情内容
    - 保留Alert提示在ListPageTemplate中

20. ✅ `apps/kuaizhizao/pages/plan-management/mrp-computation/index.tsx` - MRP运算
    - 使用 `ListPageTemplate`（带统计卡片）+ `FormModalTemplate` + `DetailDrawerTemplate`
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 将Modal转换为FormModalTemplate，使用ProFormSelect替换原生Select
    - 将Drawer转换为DetailDrawerTemplate，使用customContent自定义详情内容
    - 保留Alert提示在ListPageTemplate中

21. ✅ `apps/kuaizhizao/pages/reports/inventory-report/index.tsx` - 库存报表
    - 使用 `ListPageTemplate`（带统计卡片）
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 保留报表控制面板和图表功能在ListPageTemplate中

22. ✅ `apps/kuaizhizao/pages/reports/production-report/index.tsx` - 生产报表
    - 使用 `ListPageTemplate`（带统计卡片，6个卡片）
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 保留报表控制面板和图表功能在ListPageTemplate中

23. ✅ `apps/kuaizhizao/pages/reports/quality-report/index.tsx` - 质量报表
    - 使用 `ListPageTemplate`（带统计卡片，7个卡片）
    - 将统计卡片转换为 `ListPageTemplate` 的 `statCards` 格式
    - 保留报表控制面板和图表功能在ListPageTemplate中

24. ✅ `apps/kuaizhizao/pages/plan-management/scheduling/index.tsx` - 排程管理
    - 使用 `ListPageTemplate`（不带统计卡片）
    - 保留Card的Tab切换功能（MRP、LRP、排程、甘特图）
    - 保留所有表格和甘特图功能

---

## 📊 迁移统计

- **已迁移**: 61 个页面
  - 系统级页面: 31 个
  - 个人级页面: 2 个
  - 应用级页面: 24 个（kuaizhizao）
  - 应用级页面: 4 个（master-data）
- **待迁移**: 155+ 个页面
- **完成度**: 约 28%

---

## 🚀 下一步计划

### 阶段1: 应用级页面（kuaizhizao APP）

继续迁移列表页面：
- [x] `apps/kuaizhizao/pages/plan-management/*` - 计划管理模块页面（已完成：生产计划、需求管理、BOM管理、LRP运算、MRP运算、排程管理）
- [x] `apps/kuaizhizao/pages/warehouse-management/*` - 仓储管理模块页面（部分完成：入库、出库、库存查询、成品库存、销售出库）
- [x] `apps/kuaizhizao/pages/quality-management/*` - 质量管理模块页面（已完成：来料检验、成品检验、过程检验）
- [x] `apps/kuaizhizao/pages/finance-management/*` - 财务管理模块页面（已完成：应收管理、应付管理）
- [x] `apps/kuaizhizao/pages/reports/*` - 报表分析模块页面（已完成：库存报表、生产报表、质量报表）

### 阶段2: 应用级页面（master-data APP）

迁移主数据管理页面：
- [x] `apps/master-data/pages/factory/workshops/index.tsx` - 车间管理（已完成）
- [x] `apps/master-data/pages/warehouse/warehouses/index.tsx` - 仓库管理（已完成）
- [ ] `apps/master-data/pages/factory/workstations/index.tsx` - 工位管理（待迁移）
- [ ] `apps/master-data/pages/factory/production-lines/index.tsx` - 产线管理（待迁移）
- [ ] `apps/master-data/pages/warehouse/storage-areas/index.tsx` - 存储区域管理（待迁移）
- [ ] `apps/master-data/pages/warehouse/storage-locations/index.tsx` - 存储位置管理（待迁移）
- [x] `apps/master-data/pages/supply-chain/customers/index.tsx` - 客户管理（已完成）
- [x] `apps/master-data/pages/supply-chain/suppliers/index.tsx` - 供应商管理（已完成）
- [x] `apps/master-data/pages/performance/skills/index.tsx` - 技能管理（已完成）
- [x] `apps/master-data/pages/performance/holidays/index.tsx` - 假期管理（已完成）
- [ ] `apps/master-data/pages/materials/*` - 物料管理页面（待迁移）
- [ ] `apps/master-data/pages/process/*` - 工艺管理页面（部分已迁移：operations、defect-types、routes已使用模板）

### 阶段3: 系统级页面（继续）

- [ ] `pages/system/dashboard/index.tsx` - 工作台首页（使用 `DashboardTemplate`）
- [ ] `pages/system/files/list/index.tsx` - 文件管理（使用 `TwoColumnLayout`）

---

**最后更新**: 2025-12-30

