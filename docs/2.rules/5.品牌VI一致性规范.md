# RiverEdge SaaS 多组织框架 - 品牌VI一致性规范

## 📋 概述

本文档定义 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的**统一品牌视觉识别（VI）规范**，基于 Ant Design Pro 的 B 端产品设计最佳实践，确保整个产品的视觉风格统一、专业、现代。

**创建日期**：2025-11-17

## 🎯 设计原则

1. **一致性**：所有界面元素遵循统一的设计规范
2. **专业性**：符合 B 端产品的专业形象
3. **可读性**：确保信息清晰易读，提升用户体验
4. **可访问性**：遵循 WCAG 2.1 无障碍设计标准
5. **品牌识别**：保持品牌视觉元素的一致性
6. **绝对禁止 PowerShell**：全程严禁使用 PowerShell 进行任何操作 ⭐ **重要**

## 🎨 颜色规范

### 主色调（Primary Colors）

**品牌主色**：`#1890ff`（Ant Design 默认蓝色）

**使用场景**：
- 主要操作按钮
- 链接文字
- 选中状态
- 重要信息提示

**主色变体**：
```typescript
const primaryColors = {
  primary: '#1890ff',      // 主色
  primaryHover: '#40a9ff', // 悬停状态
  primaryActive: '#096dd9', // 激活状态
  primaryLight: '#e6f7ff', // 浅色背景
};
```

### 语义色（Semantic Colors）

**成功色（Success）**：`#52c41a`
- 使用场景：成功提示、完成状态、正向操作结果

**警告色（Warning）**：`#faad14`
- 使用场景：警告提示、需要关注的信息

**错误色（Error）**：`#ff4d4f`
- 使用场景：错误提示、危险操作、删除操作

**信息色（Info）**：`#1890ff`
- 使用场景：信息提示、一般性通知

### 中性色（Neutral Colors）

**文本颜色**：
```typescript
const textColors = {
  heading: 'rgba(0, 0, 0, 0.85)',    // 标题文本
  primary: 'rgba(0, 0, 0, 0.65)',    // 正文文本
  secondary: 'rgba(0, 0, 0, 0.45)',   // 次要文本
  disabled: 'rgba(0, 0, 0, 0.25)',    // 禁用文本
  placeholder: 'rgba(0, 0, 0, 0.25)', // 占位符文本
};
```

**背景颜色**：
```typescript
const backgroundColors = {
  base: '#ffffff',                    // 基础背景
  component: '#fafafa',               // 组件背景
  layout: '#f0f2f5',                  // 布局背景
  hover: 'rgba(0, 0, 0, 0.02)',       // 悬停背景
  selected: 'rgba(24, 144, 255, 0.1)', // 选中背景
};
```

**边框颜色**：
```typescript
const borderColors = {
  base: '#d9d9d9',                    // 基础边框
  split: '#f0f0f0',                   // 分割线
  light: '#f0f0f0',                   // 浅色边框
};
```

### 主题配置

**Ant Design 主题配置**（`config/theme.ts`）：
```typescript
export default {
  token: {
    // 主色
    colorPrimary: '#1890ff',
    
    // 语义色
    colorSuccess: '#52c41a',
    colorWarning: '#faad14',
    colorError: '#ff4d4f',
    colorInfo: '#1890ff',
    
    // 中性色
    colorText: 'rgba(0, 0, 0, 0.65)',
    colorTextSecondary: 'rgba(0, 0, 0, 0.45)',
    colorTextTertiary: 'rgba(0, 0, 0, 0.25)',
    colorTextDisabled: 'rgba(0, 0, 0, 0.25)',
    
    // 背景色
    colorBgBase: '#ffffff',
    colorBgContainer: '#fafafa',
    colorBgLayout: '#f0f2f5',
    
    // 边框色
    colorBorder: '#d9d9d9',
    colorBorderSecondary: '#f0f0f0',
    
    // 字体
    fontFamily: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial,
      'Noto Sans', sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol',
      'Noto Color Emoji'`,
    fontSize: 14,
    fontSizeHeading1: 38,
    fontSizeHeading2: 30,
    fontSizeHeading3: 24,
    fontSizeHeading4: 20,
    fontSizeHeading5: 16,
    
    // 圆角
    borderRadius: 6,
    borderRadiusLG: 8,
    borderRadiusSM: 4,
    
    // 间距
    padding: 16,
    paddingLG: 24,
    paddingSM: 12,
    paddingXS: 8,
    
    // 阴影
    boxShadow: '0 2px 8px rgba(0, 0, 0, 0.15)',
    boxShadowSecondary: '0 2px 4px rgba(0, 0, 0, 0.12)',
  },
};
```

## 📝 字体规范

### 字体族（Font Family）

**系统字体栈**（优先使用系统默认字体）：
```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 
  'Helvetica Neue', Arial, 'Noto Sans', sans-serif, 
  'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 
  'Noto Color Emoji';
```

**代码字体**：
```css
font-family: 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, Courier, monospace;
```

### 字号系统（Font Size）

**基础字号**：`14px`（Ant Design 默认）

**字号层级**：
```typescript
const fontSize = {
  xs: 12,    // 极小号（辅助信息）
  sm: 13,    // 小号（次要文本）
  base: 14,  // 基础字号（正文）
  md: 16,    // 中号（强调文本）
  lg: 18,    // 大号（小标题）
  xl: 20,    // 特大号（标题）
  xxl: 24,   // 超大号（大标题）
  xxxl: 30,  // 极大号（页面标题）
  huge: 38,  // 巨大号（主标题）
};
```

**标题字号**：
```typescript
const headingSize = {
  h1: 38,  // 一级标题
  h2: 30,  // 二级标题
  h3: 24,  // 三级标题
  h4: 20,  // 四级标题
  h5: 16,  // 五级标题
};
```

### 字重（Font Weight）

```typescript
const fontWeight = {
  normal: 400,  // 常规
  medium: 500,  // 中等
  semibold: 600, // 半粗
  bold: 700,     // 粗体
};
```

**使用规则**：
- 正文：`400`（normal）
- 强调文本：`500`（medium）
- 标题：`600`（semibold）或 `700`（bold）

### 行高（Line Height）

```typescript
const lineHeight = {
  tight: 1.2,   // 紧凑（标题）
  normal: 1.5,  // 常规（正文）
  relaxed: 1.75, // 宽松（长文本）
};
```

## 📏 间距规范

### 基础间距单位

**基础单位**：`8px`（Ant Design 使用 8px 网格系统）

**间距值**：
```typescript
const spacing = {
  xs: 4,   // 4px - 极小间距
  sm: 8,   // 8px - 小间距
  md: 16,  // 16px - 中等间距（默认）
  lg: 24,  // 24px - 大间距
  xl: 32,  // 32px - 特大间距
  xxl: 48, // 48px - 超大间距
};
```

### 组件间距

**内边距（Padding）**：
- 按钮：`8px 16px`（垂直 8px，水平 16px）
- 输入框：`4px 12px`
- 卡片：`24px`
- 表格单元格：`16px 16px`

**外边距（Margin）**：
- 组件之间：`16px` 或 `24px`
- 区块之间：`24px` 或 `32px`
- 页面边距：`24px`

### 布局间距

**页面布局**：
- 页面内边距：`24px`
- 内容区域最大宽度：`1200px`（居中）
- 侧边栏宽度：`256px`（收起时 `80px`）
- 头部高度：`64px`

## 🔲 圆角规范

```typescript
const borderRadius = {
  none: 0,    // 无圆角
  sm: 2,      // 小圆角（标签）
  base: 4,    // 基础圆角（按钮、输入框）
  md: 6,      // 中等圆角（卡片）
  lg: 8,      // 大圆角（大卡片）
  xl: 12,     // 特大圆角（模态框）
  full: 9999, // 完全圆角（圆形按钮）
};
```

**使用规则**：
- 按钮：`4px`（base）
- 输入框：`4px`（base）
- 卡片：`6px`（md）
- 标签：`2px`（sm）
- 头像：`50%`（圆形）

## 🌑 阴影规范

```typescript
const boxShadow = {
  none: 'none',
  sm: '0 1px 2px rgba(0, 0, 0, 0.05)',           // 小阴影
  base: '0 2px 8px rgba(0, 0, 0, 0.15)',        // 基础阴影（卡片）
  md: '0 4px 12px rgba(0, 0, 0, 0.15)',         // 中等阴影（下拉菜单）
  lg: '0 8px 24px rgba(0, 0, 0, 0.12)',         // 大阴影（模态框）
  xl: '0 12px 48px rgba(0, 0, 0, 0.15)',        // 特大阴影（抽屉）
};
```

**使用规则**：
- 卡片：`base`
- 下拉菜单：`md`
- 模态框：`lg`
- 抽屉：`xl`

## 🎯 图标规范

### 图标库

**统一使用**：`@ant-design/icons`

**图标尺寸**：
```typescript
const iconSize = {
  xs: 12,   // 极小图标
  sm: 14,   // 小图标
  base: 16, // 基础图标（默认）
  md: 20,   // 中等图标
  lg: 24,   // 大图标
  xl: 32,   // 特大图标
};
```

### 图标使用规则

**按钮图标**：
- 图标 + 文字：图标 `16px`，文字 `14px`
- 仅图标按钮：图标 `16px`，按钮 `32px × 32px`

**导航图标**：
- 侧边栏菜单：`16px`
- 顶部导航：`16px`

**表格图标**：
- 操作列图标：`16px`
- 状态图标：`16px`

## 🧩 组件样式规范

### 按钮（Button）

**尺寸**：
- 大（Large）：高度 `40px`，内边距 `10px 24px`
- 中（Middle，默认）：高度 `32px`，内边距 `8px 16px`
- 小（Small）：高度 `24px`，内边距 `4px 12px`

**类型**：
- Primary：主色背景，白色文字
- Default：白色背景，灰色边框
- Dashed：虚线边框
- Text：文字按钮
- Link：链接按钮

### 输入框（Input）

**尺寸**：
- 大：高度 `40px`
- 中（默认）：高度 `32px`
- 小：高度 `24px`

**状态样式**：
- 默认：边框 `#d9d9d9`
- 悬停：边框 `#40a9ff`
- 聚焦：边框 `#1890ff`，阴影 `0 0 0 2px rgba(24, 144, 255, 0.2)`
- 错误：边框 `#ff4d4f`
- 禁用：背景 `#f5f5f5`，文字 `rgba(0, 0, 0, 0.25)`

### 表格（Table）

**行高**：
- 紧凑：`32px`
- 常规（默认）：`48px`
- 宽松：`64px`

**边框**：
- 表头边框：`#f0f0f0`
- 单元格边框：`#f0f0f0`
- 无边框模式：仅使用分割线

**斑马纹**：
- 偶数行背景：`#fafafa`（可选）

### 卡片（Card）

**内边距**：`24px`

**阴影**：`0 2px 8px rgba(0, 0, 0, 0.15)`

**圆角**：`6px`

**标题**：
- 字号：`16px`
- 字重：`600`
- 下边距：`16px`

## 📐 布局规范

### 栅格系统

**24 栅格系统**（Ant Design 标准）：
- 总列数：`24`
- 栅格间隔：`16px` 或 `24px`
- 响应式断点：见下方

**使用示例**：
```tsx
<Row gutter={16}>
  <Col span={12}>50% 宽度</Col>
  <Col span={12}>50% 宽度</Col>
</Row>

<Row gutter={24}>
  <Col xs={24} sm={12} md={8} lg={6}>
    响应式列
  </Col>
</Row>
```

### 响应式断点

```typescript
const breakpoints = {
  xs: 480,   // 超小屏（手机）
  sm: 576,   // 小屏（手机横屏）
  md: 768,   // 中屏（平板）
  lg: 992,   // 大屏（桌面）
  xl: 1200,  // 超大屏（大桌面）
  xxl: 1600, // 超超大屏（超大桌面）
};
```

**布局适配**：
- `xs`：单列布局，侧边栏收起
- `sm`：单列布局，侧边栏收起
- `md`：双列布局，侧边栏可展开
- `lg+`：多列布局，侧边栏展开

### 页面布局

**标准布局结构**：
```
┌─────────────────────────────────────┐
│ Header (64px)                        │
├──────────┬──────────────────────────┤
│          │                          │
│ Sidebar  │  Content Area            │
│ (256px)  │  (padding: 24px)         │
│          │                          │
│          │                          │
└──────────┴──────────────────────────┘
```

**内容区域**：
- 最大宽度：`1200px`（居中）
- 内边距：`24px`
- 背景色：`#f0f2f5`

## 🎨 品牌元素

### Logo

**使用规则**：
- 顶部导航：高度 `32px`，保持宽高比
- 侧边栏：高度 `32px`，收起时仅显示图标
- 登录页：高度 `48px`

**文件格式**：
- SVG（推荐，矢量图）
- PNG（备用，透明背景）

### 品牌色

**主品牌色**：`#1890ff`（Ant Design 蓝色）

**辅助色**：
- 深色模式主色：`#177ddc`
- 浅色背景：`#e6f7ff`

**使用场景**：
- Logo 颜色
- 主操作按钮
- 链接文字
- 品牌标识

## 🌓 主题模式

### 浅色模式（Light Mode）

**默认主题**，使用上述所有颜色规范。

### 深色模式（Dark Mode）

**背景色**：
```typescript
const darkBackground = {
  base: '#141414',           // 基础背景
  component: '#1f1f1f',     // 组件背景
  layout: '#000000',         // 布局背景
  hover: 'rgba(255, 255, 255, 0.08)', // 悬停背景
};
```

**文本颜色**：
```typescript
const darkText = {
  heading: 'rgba(255, 255, 255, 0.85)',
  primary: 'rgba(255, 255, 255, 0.65)',
  secondary: 'rgba(255, 255, 255, 0.45)',
  disabled: 'rgba(255, 255, 255, 0.25)',
};
```

**主题切换**：
- 支持用户手动切换
- 支持跟随系统设置
- 使用 Ant Design 的 `ConfigProvider` 配置

## 📱 移动端适配

### 断点适配

**移动端优先**：
- 默认使用移动端布局
- 使用响应式断点逐步增强

**布局调整**：
- 侧边栏：移动端收起，点击菜单按钮展开
- 表格：移动端使用卡片列表展示
- 表单：移动端单列布局

### 触摸优化

**触摸目标尺寸**：
- 最小触摸区域：`44px × 44px`
- 按钮高度：最小 `32px`
- 输入框高度：最小 `32px`

## ✅ 检查清单

设计界面时，请确保：

- [ ] 颜色使用符合规范（主色、语义色、中性色）
- [ ] 字体使用系统字体栈
- [ ] 字号符合字号系统
- [ ] 间距使用 8px 基础单位
- [ ] 圆角使用统一规范
- [ ] 阴影使用统一规范
- [ ] 图标使用 `@ant-design/icons`
- [ ] 组件样式符合 Ant Design 规范
- [ ] 布局使用 24 栅格系统
- [ ] 响应式断点正确设置
- [ ] 品牌元素（Logo、品牌色）正确使用

## 📚 相关文档

- [1.框架命名哲学.md](./1.框架命名哲学.md) - 框架命名哲学
- [2.字段命名规范.md](./2.字段命名规范.md) - 命名规范
- [5.项目结构规范.md](./5.项目结构规范.md) - 项目结构规范
- [6.API设计规范.md](./6.API设计规范.md) - API 设计规范
- [AGENTS.md](./AGENTS.md) - AI 助手开发规范
- [1.最终技术选型.md](../1.plan/1.最终技术选型.md) - 技术选型（包含 Ant Design 说明）

## 🔗 参考资源

- [Ant Design 设计语言](https://ant.design/docs/spec/introduce-cn)
- [Ant Design Pro 设计规范](https://pro.ant.design/zh-CN/docs/design)
- [Ant Design 主题定制](https://ant.design/docs/react/customize-theme-cn)
- [WCAG 2.1 无障碍指南](https://www.w3.org/WAI/WCAG21/quickref/)

---

**最后更新**：2025-11-18

