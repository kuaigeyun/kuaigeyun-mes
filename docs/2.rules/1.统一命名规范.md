# RiverEdge SaaS 多组织框架 - 统一命名规范

## 📋 概述

本文档定义 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的**统一命名规范**，按照**平台级、系统级、应用级**三个层级，统一规范前端、后端、数据库、API端点的命名，确保整个项目在开源时便于其他开发者理解，使整个项目统一规范优雅。

**创建日期**：2025-01-04

## 🎯 设计原则

1. **层级清晰**：按照平台级、系统级、应用级三个层级进行规范
2. **统一性**：同类事物使用相同的命名风格
3. **可读性**：命名清晰表达意图，见名知意
4. **简洁性**：避免过长的命名，但不过度缩写
5. **规范性**：遵循语言和框架的命名约定（Python: snake_case, TypeScript: camelCase/PascalCase）
6. **最佳实践**：遵循业界最佳实践（RESTful API 使用 kebab-case，React 目录使用 kebab-case）
7. **开源友好**：命名规范优雅，便于其他开发者理解
8. **向后兼容**：考虑现有代码，允许渐进式迁移

## 🏗️ 三层架构体系

### 架构层级定义

```
┌─────────────────────────────────────────────────────────┐
│                    平台级 (Platform Level)                 │
│  - 平台整体运营和管理                                      │
│  - 目标用户：超级管理员                                    │
│  - 架构：传统分层架构 (Controller → Service → Repository) │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                    系统级 (System Level)                  │
│  - 当前组织的系统管理                                      │
│  - 目标用户：组织管理员、普通用户                          │
│  - 架构：传统分层架构 (Controller → Service → Repository) │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                    应用级 (Application Level)             │
│  - 业务应用功能实现                                        │
│  - 目标用户：业务用户                                      │
│  - 架构：DDD (领域驱动设计) 架构                          │
└─────────────────────────────────────────────────────────┘
```

### 层级职责说明

| 层级 | 定位 | 目标用户 | 架构设计 | 核心职责 | 典型功能 |
|------|------|----------|----------|----------|----------|
| **平台级** | 平台整体运营 | 超级管理员 | 传统分层架构 | 平台运营和管理 | 运营看板、组织管理、系统监控、套餐管理 |
| **系统级** | 组织系统管理 | 组织管理员、普通用户 | 传统分层架构 | 组织内系统管理 | 仪表盘、用户管理、角色管理、权限管理 |
| **应用级** | 业务应用功能 | 业务用户 | DDD 架构 | 业务功能实现 | MES、ERP、WMS、CRM 等业务插件 |

---

## 🔧 后端命名规范

### 目录结构规范

#### 平台级后端 (`infra/`)

```
riveredge-backend/src/infra/
├── api/                    # 平台级 API 路由
│   ├── tenants/           # 租户管理 API
│   ├── packages/          # 套餐管理 API
│   ├── platform_superadmin/  # 平台超级管理员 API
│   ├── monitoring/        # 监控统计 API
│   └── auth/              # 平台认证 API
├── models/                 # 平台级数据模型
├── schemas/                # 平台级数据验证
├── services/               # 平台级业务服务
├── domain/                 # 平台级领域逻辑
├── config/                 # 平台级配置
├── infrastructure/         # 基础设施
└── exceptions/             # 异常定义
```

#### 系统级后端 (`core/`)

```
riveredge-backend/src/core/
├── api/                    # 系统级 API 路由
│   ├── users/             # 用户管理 API
│   ├── roles/             # 角色管理 API
│   ├── permissions/       # 权限管理 API
│   ├── departments/       # 部门管理 API
│   └── ...                # 其他系统级 API
├── models/                 # 系统级数据模型
├── schemas/                # 系统级数据验证
├── services/               # 系统级业务服务
├── middleware/             # 系统级中间件
├── context/                # 上下文管理
└── utils/                  # 工具函数
```

#### 应用级后端 (`apps/`)

```
riveredge-backend/src/apps/
├── kuaimes/                # MES 应用插件
│   ├── api/               # MES API 路由
│   ├── models/            # MES 数据模型
│   ├── schemas/           # MES 数据验证
│   └── services/          # MES 业务服务
├── kuaicrm/                # CRM 应用插件
├── kuaiwms/                # WMS 应用插件
└── ...                     # 其他应用插件
```

#### 后端静态文件目录

```
riveredge-backend/
├── static/               # 静态文件目录（项目根目录，统一使用 static/）
│   └── swagger-ui/       # Swagger UI 静态资源
├── src/                  # 源代码目录
└── uploads/              # 用户上传文件目录（按组织隔离）
    └── {tenant_id}/
        └── {year}/
            └── {month}/
```

### 文件命名规范

#### 模型文件 (Models)

**规则**：使用 `snake_case`，单数形式

| 层级 | 示例 | 说明 |
|------|------|------|
| 平台级 | `infra/models/tenant.py` | 租户模型 |
| 平台级 | `infra/models/package.py` | 套餐模型 |
| 系统级 | `core/models/user.py` | 用户模型 |
| 系统级 | `core/models/role.py` | 角色模型 |
| 应用级 | `apps/kuaimes/models/order.py` | MES 订单模型 |
| 应用级 | `apps/kuaicrm/models/customer.py` | CRM 客户模型 |

#### API 文件 (API Routes)

**规则**：使用 `snake_case`，复数形式，按资源组织目录

| 层级 | 示例 | 说明 |
|------|------|------|
| 平台级 | `infra/api/tenants/tenants.py` | 租户管理 API |
| 平台级 | `infra/api/packages/packages.py` | 套餐管理 API |
| 系统级 | `core/api/users/users.py` | 用户管理 API |
| 系统级 | `core/api/roles/roles.py` | 角色管理 API |
| 应用级 | `apps/kuaimes/api/orders/orders.py` | MES 订单 API |
| 应用级 | `apps/kuaicrm/api/customers/customers.py` | CRM 客户 API |

**目录结构说明**：
- ✅ 每个资源一个目录：`users/`, `roles/`, `orders/`
- ✅ 目录内包含 `__init__.py` 和主文件：`users/users.py`
- ✅ 主文件使用复数形式：`users.py`, `orders.py`

#### Service 文件 (Services)

**规则**：使用 `snake_case` + `_service` 后缀

| 层级 | 示例 | 说明 |
|------|------|------|
| 平台级 | `infra/services/tenant_service.py` | 租户服务 |
| 平台级 | `infra/services/package_service.py` | 套餐服务 |
| 系统级 | `core/services/user_service.py` | 用户服务 |
| 系统级 | `core/services/role_service.py` | 角色服务 |
| 应用级 | `apps/kuaimes/services/order_service.py` | MES 订单服务 |
| 应用级 | `apps/kuaicrm/services/customer_service.py` | CRM 客户服务 |

#### Schema 文件 (Schemas)

**规则**：使用 `snake_case` + `_schema` 后缀（可选），或直接使用资源名

| 层级 | 示例 | 说明 |
|------|------|------|
| 平台级 | `infra/schemas/tenant.py` | 租户 Schema |
| 平台级 | `infra/schemas/package.py` | 套餐 Schema |
| 系统级 | `core/schemas/user.py` | 用户 Schema |
| 系统级 | `core/schemas/role.py` | 角色 Schema |
| 应用级 | `apps/kuaimes/schemas/order.py` | MES 订单 Schema |
| 应用级 | `apps/kuaicrm/schemas/customer.py` | CRM 客户 Schema |

### 类命名规范

#### 模型类 (Models)

**规则**：使用 `PascalCase`，单数形式

| 层级 | 示例 | 说明 |
|------|------|------|
| 平台级 | `class Tenant(BaseModel):` | 租户模型 |
| 平台级 | `class Package(BaseModel):` | 套餐模型 |
| 系统级 | `class User(BaseModel):` | 用户模型 |
| 系统级 | `class Role(BaseModel):` | 角色模型 |
| 应用级 | `class Order(BaseModel):` | MES 订单模型 |
| 应用级 | `class Customer(BaseModel):` | CRM 客户模型 |

#### Schema 类 (Schemas)

**规则**：使用 `PascalCase`，动词+名词格式

| 类型 | 示例 | 说明 |
|------|------|------|
| 创建 | `class UserCreate(BaseModel):` | 用户创建 Schema |
| 更新 | `class UserUpdate(BaseModel):` | 用户更新 Schema |
| 响应 | `class UserResponse(BaseModel):` | 用户响应 Schema |
| 列表响应 | `class UserListResponse(BaseModel):` | 用户列表响应 Schema |

#### Service 类 (Services)

**规则**：使用 `PascalCase` + `Service` 后缀

| 层级 | 示例 | 说明 |
|------|------|------|
| 平台级 | `class TenantService:` | 租户服务 |
| 平台级 | `class PackageService:` | 套餐服务 |
| 系统级 | `class UserService:` | 用户服务 |
| 系统级 | `class RoleService:` | 角色服务 |
| 应用级 | `class OrderService:` | MES 订单服务 |
| 应用级 | `class CustomerService:` | CRM 客户服务 |

### 函数命名规范

**规则**：使用 `snake_case`，动词开头

#### CRUD 操作

| 操作 | 示例 | 说明 |
|------|------|------|
| 创建 | `async def create_user(...)` | 创建用户 |
| 查询单个 | `async def get_user_by_id(...)` | 根据 ID 获取用户 |
| 查询列表 | `async def list_users(...)` | 获取用户列表 |
| 更新 | `async def update_user(...)` | 更新用户 |
| 删除 | `async def delete_user(...)` | 删除用户 |

#### 业务操作

| 操作 | 示例 | 说明 |
|------|------|------|
| 激活 | `async def activate_user(...)` | 激活用户 |
| 停用 | `async def deactivate_user(...)` | 停用用户 |
| 重置密码 | `async def reset_password(...)` | 重置密码 |
| 搜索 | `async def search_users(...)` | 搜索用户 |

### 变量命名规范

**规则**：使用 `snake_case`

| 类型 | 示例 | 说明 |
|------|------|------|
| 普通变量 | `user_id`, `order_no`, `total_amount` | 使用下划线分隔 |
| 布尔变量 | `is_active`, `has_permission` | 使用 `is_` 或 `has_` 前缀 |
| 常量 | `MAX_RETRY_COUNT`, `DEFAULT_PAGE_SIZE` | 全大写 + 下划线 |

---

## 🎨 前端命名规范

### 目录结构规范

#### 平台级前端

```
riveredge-frontend/src/pages/platform/
├── tenants/                # 租户管理页面
│   ├── index.tsx          # 租户列表页
│   └── form.tsx           # 租户表单页
├── packages/               # 套餐管理页面
├── monitoring/             # 监控统计页面
└── admin/                  # 平台超级管理员页面
```

#### 系统级前端

```
riveredge-frontend/src/pages/system/
├── users/                  # 用户管理页面
│   ├── index.tsx          # 用户列表页
│   └── form.tsx           # 用户表单页
├── roles/                  # 角色管理页面
├── permissions/            # 权限管理页面
├── departments/            # 部门管理页面
└── dashboard/              # 仪表盘页面
```

#### 应用级前端

```
riveredge-frontend/src/apps/
├── kuaimes/                # MES 应用插件
│   ├── index.tsx          # MES 应用入口
│   ├── pages/             # MES 页面
│   │   ├── orders/       # 订单管理页面
│   │   └── products/     # 产品管理页面
│   └── services/          # MES API 服务
├── kuaicrm/                # CRM 应用插件
└── ...                     # 其他应用插件
```

### 文件命名规范

#### 页面组件文件

**规则**：使用 `PascalCase`，功能描述 + 页面类型

| 层级 | 示例 | 说明 |
|------|------|------|
| 平台级 | `pages/platform/tenants/index.tsx` | 租户列表页 |
| 平台级 | `pages/platform/tenants/form.tsx` | 租户表单页 |
| 系统级 | `pages/system/users/index.tsx` | 用户列表页 |
| 系统级 | `pages/system/users/form.tsx` | 用户表单页 |
| 应用级 | `apps/kuaimes/pages/orders/index.tsx` | MES 订单列表页 |
| 应用级 | `apps/kuaicrm/pages/customers/index.tsx` | CRM 客户列表页 |

#### 组件文件

**规则**：使用 `PascalCase`，功能描述

| 类型 | 示例 | 说明 |
|------|------|------|
| 公共组件 | `components/uni_table/UniTable.tsx` | 统一表格组件 |
| 公共组件 | `components/tenant_selector/TenantSelector.tsx` | 租户选择器组件 |
| 业务组件 | `apps/kuaimes/components/OrderCard.tsx` | MES 订单卡片组件 |

#### 服务文件 (Services)

**规则**：使用 `camelCase`，资源名

| 层级 | 示例 | 说明 |
|------|------|------|
| 系统级 | `services/user.ts` | 用户服务 |
| 系统级 | `services/role.ts` | 角色服务 |
| 应用级 | `apps/kuaimes/services/order.ts` | MES 订单服务 |
| 应用级 | `apps/kuaicrm/services/customer.ts` | CRM 客户服务 |

#### 类型文件 (Types)

**规则**：使用 `camelCase` + `Types` 后缀（可选）

| 层级 | 示例 | 说明 |
|------|------|------|
| 系统级 | `types/user.ts` | 用户类型定义 |
| 系统级 | `types/role.ts` | 角色类型定义 |
| 应用级 | `apps/kuaimes/types/order.ts` | MES 订单类型定义 |

### 组件命名规范

**规则**：使用 `PascalCase`

| 类型 | 示例 | 说明 |
|------|------|------|
| 页面组件 | `UserList`, `UserForm` | 功能描述 + 页面类型 |
| 业务组件 | `OrderCard`, `CustomerTable` | 功能描述 |
| 基础组件 | `BaseButton`, `BaseInput` | Base 前缀 |

### 函数命名规范

**规则**：使用 `camelCase`，动词开头

#### API 函数

| 操作 | 示例 | 说明 |
|------|------|------|
| 获取列表 | `getUserList(...)` | 获取用户列表 |
| 获取单个 | `getUserById(...)` | 根据 ID 获取用户 |
| 创建 | `createUser(...)` | 创建用户 |
| 更新 | `updateUser(...)` | 更新用户 |
| 删除 | `deleteUser(...)` | 删除用户 |

#### 事件处理函数

| 类型 | 示例 | 说明 |
|------|------|------|
| 提交 | `handleSubmit(...)` | 处理提交（handle 前缀） |
| 变更 | `handleChange(...)` | 处理变更（handle 前缀） |
| 点击 | `handleClick(...)` | 处理点击（handle 前缀） |

#### Hook 函数

| 类型 | 示例 | 说明 |
|------|------|------|
| 自定义 Hook | `useUser(...)` | 用户 Hook（use 前缀） |
| 自定义 Hook | `useOrder(...)` | 订单 Hook（use 前缀） |

### 变量命名规范

**规则**：使用 `camelCase`

| 类型 | 示例 | 说明 |
|------|------|------|
| 普通变量 | `userId`, `orderNo`, `totalAmount` | 驼峰命名 |
| 布尔变量 | `isActive`, `hasPermission` | `is_` 或 `has_` 前缀 |
| 常量 | `MAX_RETRY_COUNT`, `DEFAULT_PAGE_SIZE` | 全大写 + 下划线 |

### 目录命名规范

**规则**：优先使用 `kebab-case`，兼容 `snake_case`（保持现有代码一致性）

> **最佳实践说明**：React/TypeScript 社区更倾向于使用 kebab-case，但考虑到项目已有代码使用 snake_case，允许两种方式并存，新代码优先使用 kebab-case。

| 类型 | 推荐 | 兼容 | 说明 |
|------|------|------|------|
| 功能模块 | `user/`, `role/`, `dashboard/` | `user/`, `role/` | 单数形式，推荐 kebab-case |
| 功能集合 | `components/`, `services/`, `stores/` | `components/`, `services/` | 复数形式，推荐 kebab-case |
| 组件目录 | `uni-table/`, `tenant-selector/` | `uni_table/`, `tenant_selector/` | 推荐 kebab-case，兼容 snake_case |
| 页面目录 | `user-profile/`, `order-list/` | `user_profile/`, `order_list/` | 推荐 kebab-case，兼容 snake_case |

**迁移建议**：
- 新代码优先使用 `kebab-case`
- 现有代码可以保持 `snake_case`，逐步迁移
- 同一模块内保持命名风格一致

---

## 🗄️ 数据库命名规范

### 表命名规范

**规则**：使用 `snake_case`，复数形式，必须包含模块前缀

#### 模块前缀规则

| 层级 | 后端文件夹 | 前缀 | 示例 | 说明 |
|------|-----------|------|------|------|
| 平台级 | `infra/` | `infra_` | `infra_tenants` | 平台级表（对应 infra/ 文件夹） |
| 系统级 | `core/` | `core_` | `core_users` | 系统级表（对应 core/ 文件夹） |
| 应用级 | `apps/` | `apps_插件名_` | `apps_mes_orders` | 应用插件表（对应 apps/ 文件夹） |

#### 表命名示例

**平台级表**（对应 `infra/` 文件夹）：
- ✅ `infra_tenants` - 租户表
- ✅ `infra_packages` - 套餐表
- ✅ `infra_monitoring_statistics` - 监控统计表

**系统级表**（对应 `core/` 文件夹）：
- ✅ `core_users` - 用户表
- ✅ `core_roles` - 角色表
- ✅ `core_permissions` - 权限表
- ✅ `core_departments` - 部门表

**应用插件表**（对应 `apps/` 文件夹）：
- ✅ `apps_mes_orders` - MES 订单表
- ✅ `apps_mes_order_items` - MES 订单明细表
- ✅ `apps_crm_customers` - CRM 客户表
- ✅ `apps_wms_inventory` - WMS 库存表

**关联表**：
- ✅ `core_user_roles` - 用户角色关联表
- ✅ `core_role_permissions` - 角色权限关联表
- ✅ `apps_mes_order_items` - MES 订单明细表

### 字段命名规范

**规则**：使用 `snake_case`

| 类型 | 示例 | 说明 |
|------|------|------|
| 主键 | `id` | 统一使用 `id` |
| 组织字段 | `tenant_id` | 所有表必须包含 |
| 外键 | `user_id`, `order_id` | 表名_id 格式 |
| 时间字段 | `created_at`, `updated_at` | `_at` 后缀 |
| 布尔字段 | `is_active`, `is_deleted` | `is_` 前缀 |
| 状态字段 | `status` | 统一使用 `status` |
| 数量字段 | `quantity`, `total_amount` | 明确单位 |

### 索引命名规范

**规则**：使用 `idx_表名_字段名`（表名包含模块前缀）

| 类型 | 示例 | 说明 |
|------|------|------|
| 普通索引 | `idx_infra_tenants_domain` | 租户表域名索引（平台级） |
| 普通索引 | `idx_core_users_tenant_id` | 用户表组织 ID 索引（系统级） |
| 普通索引 | `idx_apps_mes_orders_status` | MES 订单表状态索引（应用级） |
| 唯一索引 | `uk_core_users_email` | 用户表邮箱唯一索引（系统级） |
| 唯一索引 | `uk_apps_mes_orders_order_no` | MES 订单表订单号唯一索引（应用级） |

### 约束命名规范

**规则**：使用 `ck_表名_字段名`（检查约束）、`fk_表名_字段名`（外键约束）

| 类型 | 示例 | 说明 |
|------|------|------|
| 检查约束 | `ck_infra_tenants_domain_format` | 租户表域名格式检查（平台级） |
| 检查约束 | `ck_core_users_email_format` | 用户表邮箱格式检查（系统级） |
| 检查约束 | `ck_apps_mes_orders_status_valid` | MES 订单表状态有效性检查（应用级） |
| 外键约束 | `fk_apps_mes_order_items_order_id` | MES 订单明细表订单 ID 外键（应用级） |

---

## 🌐 API 端点命名规范

### API 路径规范

**规则**：所有 API 路径必须以 `/api/v1/` 开头，按层级组织，使用 **kebab-case**（连字符分隔）

> **最佳实践说明**：RESTful API 路径使用 kebab-case 是业界标准，符合 URL 规范，更易读且跨平台兼容性好。

#### 路径前缀规则

| 层级 | 后端文件夹 | 前缀 | 示例 | 说明 |
|------|-----------|------|------|------|
| 平台级 | `infra/` | `/api/v1/infra/` | `/api/v1/infra/tenants` | 平台级 API（对应 infra/ 文件夹） |
| 系统级 | `core/` | `/api/v1/core/` | `/api/v1/core/users` | 系统级 API（对应 core/ 文件夹） |
| 应用级 | `apps/` | `/api/v1/apps/插件名/` | `/api/v1/apps/kuaimes/orders` | 应用插件 API（对应 apps/ 文件夹） |

#### API 路径示例

**平台级 API**（对应 `infra/` 文件夹）：
- ✅ `GET /api/v1/infra/tenants` - 获取租户列表
- ✅ `POST /api/v1/infra/tenants` - 创建租户
- ✅ `GET /api/v1/infra/packages` - 获取套餐列表
- ✅ `GET /api/v1/infra/monitoring/statistics` - 获取监控统计
- ✅ `GET /api/v1/infra/saved-searches` - 获取保存的搜索（kebab-case）

**系统级 API**（对应 `core/` 文件夹）：
- ✅ `GET /api/v1/core/users` - 获取用户列表
- ✅ `POST /api/v1/core/users` - 创建用户
- ✅ `GET /api/v1/core/roles` - 获取角色列表
- ✅ `GET /api/v1/core/departments` - 获取部门列表
- ✅ `GET /api/v1/core/user-roles` - 获取用户角色关联（kebab-case）
- ✅ `GET /api/v1/core/data-dictionaries` - 获取数据字典（kebab-case）
- ✅ `GET /api/v1/core/system-parameters` - 获取系统参数（kebab-case）

**应用级 API**（对应 `apps/` 文件夹）：
- ✅ `GET /api/v1/apps/kuaimes/orders` - 获取 MES 订单列表
- ✅ `POST /api/v1/apps/kuaimes/orders` - 创建 MES 订单
- ✅ `GET /api/v1/apps/kuaicrm/customers` - 获取 CRM 客户列表
- ✅ `GET /api/v1/apps/kuaiwms/inventory` - 获取 WMS 库存列表
- ✅ `GET /api/v1/apps/kuaimes/order-items` - 获取订单明细（kebab-case）

**嵌套资源**：
- ✅ `GET /api/v1/core/users/{user_id}/roles` - 获取用户的角色列表
- ✅ `GET /api/v1/apps/kuaimes/orders/{order_id}/items` - 获取订单的明细列表

**路径参数命名**：
- ✅ `{user_id}` - 使用 snake_case（Python 后端约定）
- ✅ `{order_id}` - 使用 snake_case（Python 后端约定）

### HTTP 方法使用规范

| 方法 | 使用场景 | 示例 |
|------|----------|------|
| `GET` | 查询资源 | `GET /api/v1/core/users` |
| `POST` | 创建资源 | `POST /api/v1/core/users` |
| `PUT` | 完整更新资源 | `PUT /api/v1/core/users/{user_id}` |
| `PATCH` | 部分更新资源 | `PATCH /api/v1/core/users/{user_id}` |
| `DELETE` | 删除资源 | `DELETE /api/v1/core/users/{user_id}` |

### API Tags 规范

**规则**：使用英文，首字母大写，按层级组织，使用空格分隔多词

| 层级 | 示例 | 说明 |
|------|------|------|
| 平台级 | `Platform Tenants`, `Platform Packages`, `Platform Monitoring` | 平台级标签 |
| 系统级 | `System Users`, `System Roles`, `System Permissions` | 系统级标签 |
| 应用级 | `MES Orders`, `CRM Customers`, `WMS Inventory` | 应用级标签 |

**命名原则**：
- ✅ 使用英文，首字母大写
- ✅ 多个单词使用空格分隔
- ✅ 保持简洁明了
- ✅ 与 API 路径资源名对应

---

## 📋 命名规范总结表

### 后端命名规范总结

| 类型 | 平台级 | 系统级 | 应用级 |
|------|--------|--------|--------|
| **目录** | `infra/` | `core/` | `apps/插件名/` |
| **模型文件** | `infra/models/tenant.py` | `core/models/user.py` | `apps/kuaimes/models/order.py` |
| **API 文件** | `infra/api/tenants/tenants.py` | `core/api/users/users.py` | `apps/kuaimes/api/orders/orders.py` |
| **Service 文件** | `infra/services/tenant_service.py` | `core/services/user_service.py` | `apps/kuaimes/services/order_service.py` |
| **Schema 文件** | `infra/schemas/tenant.py` | `core/schemas/user.py` | `apps/kuaimes/schemas/order.py` |
| **模型类** | `class Tenant(BaseModel):` | `class User(BaseModel):` | `class Order(BaseModel):` |
| **Service 类** | `class TenantService:` | `class UserService:` | `class OrderService:` |
| **函数** | `async def create_tenant(...)` | `async def create_user(...)` | `async def create_order(...)` |

### 前端命名规范总结

| 类型 | 平台级 | 系统级 | 应用级 |
|------|--------|--------|--------|
| **目录** | `pages/platform/` | `pages/system/` | `apps/插件名/` |
| **页面文件** | `pages/platform/tenants/index.tsx` | `pages/system/users/index.tsx` | `apps/kuaimes/pages/orders/index.tsx` |
| **服务文件** | `services/tenant.ts` | `services/user.ts` | `apps/kuaimes/services/order.ts` |
| **组件** | `TenantList`, `TenantForm` | `UserList`, `UserForm` | `OrderList`, `OrderForm` |
| **函数** | `getTenantList(...)` | `getUserList(...)` | `getOrderList(...)` |

### 数据库命名规范总结

| 类型 | 平台级 | 系统级 | 应用级 |
|------|--------|--------|--------|
| **后端文件夹** | `infra/` | `core/` | `apps/` |
| **表前缀** | `infra_` | `core_` | `apps_插件名_` |
| **表名** | `infra_tenants` | `core_users` | `apps_mes_orders` |
| **索引** | `idx_infra_tenants_xxx` | `idx_core_users_xxx` | `idx_apps_mes_orders_xxx` |
| **约束** | `ck_infra_tenants_xxx` | `ck_core_users_xxx` | `ck_apps_mes_orders_xxx` |

### API 端点命名规范总结

| 类型 | 平台级 | 系统级 | 应用级 |
|------|--------|--------|--------|
| **后端文件夹** | `infra/` | `core/` | `apps/` |
| **路径前缀** | `/api/v1/infra/` | `/api/v1/core/` | `/api/v1/apps/插件名/` |
| **路径格式** | kebab-case（连字符分隔） | kebab-case（连字符分隔） | kebab-case（连字符分隔） |
| **示例** | `/api/v1/infra/tenants` | `/api/v1/core/users` | `/api/v1/apps/kuaimes/orders` |
| **多词示例** | `/api/v1/infra/saved-searches` | `/api/v1/core/user-roles` | `/api/v1/apps/kuaimes/order-items` |
| **Tags** | `Platform Tenants` | `System Users` | `MES Orders` |

---

## ✅ 检查清单

### 后端检查清单

- [ ] 目录结构是否符合层级规范？（`infra/`, `core/`, `apps/`）
- [ ] 文件命名是否符合规范？（`snake_case`，单数/复数正确）
- [ ] 类命名是否符合规范？（`PascalCase`）
- [ ] 函数命名是否符合规范？（`snake_case`，动词开头）
- [ ] 变量命名是否符合规范？（`snake_case`）

### 前端检查清单

- [ ] 目录结构是否符合层级规范？（`pages/platform/`, `pages/system/`, `apps/`）
- [ ] 文件命名是否符合规范？（`PascalCase` 组件，`camelCase` 服务）
- [ ] 组件命名是否符合规范？（`PascalCase`）
- [ ] 函数命名是否符合规范？（`camelCase`，动词开头）
- [ ] 变量命名是否符合规范？（`camelCase`）
- [ ] 目录命名是否符合规范？（优先 `kebab-case`，兼容 `snake_case`）

### 数据库检查清单

- [ ] 表名是否包含模块前缀？（`infra_` 对应 `infra/` 文件夹，`core_` 对应 `core/` 文件夹，`apps_插件名_` 对应 `apps/` 文件夹）
- [ ] 表名是否使用复数形式？
- [ ] 字段命名是否符合规范？（`snake_case`）
- [ ] 索引命名是否符合规范？（`idx_表名_字段名`，表名包含前缀）
- [ ] 约束命名是否符合规范？（`ck_表名_字段名`，`fk_表名_字段名`）

### API 端点检查清单

- [ ] API 路径是否以 `/api/v1/` 开头？
- [ ] API 路径是否包含层级前缀？（`infra/` 对应 `infra/` 文件夹，`core/` 对应 `core/` 文件夹，`apps/` 对应 `apps/` 文件夹）
- [ ] API 路径是否使用 kebab-case？（多个单词使用连字符分隔）
- [ ] 资源名是否使用复数形式？
- [ ] API Tags 是否使用英文，首字母大写？
- [ ] HTTP 方法使用是否正确？（GET, POST, PUT, PATCH, DELETE）

---

## 📁 静态文件目录规范（必须严格遵循）

### 统一命名规范

**规则**：前端和后端统一使用 `static/` 作为静态文件夹名称，保持命名一致性。

### 后端静态文件目录

**位置**：`riveredge-backend/static/`（项目根目录，与前端保持一致）

**用途**：
- 存放后端静态资源文件（如 Swagger UI 资源）
- 通过 FastAPI 的 `StaticFiles` 挂载到 `/static` 路径

**示例**：
```python
# 在 main.py 中挂载静态文件
app.mount("/static", StaticFiles(directory="static"), name="static")
```

**目录结构**：
```
riveredge-backend/static/
└── swagger-ui/           # Swagger UI 静态资源
    ├── swagger-ui.css
    └── swagger-ui-bundle.js
```

### 前端静态文件目录

**位置**：`riveredge-frontend/static/`（项目根目录）

**用途**：
- 存放前端静态资源文件（图片、图标、字体等）
- 在构建时会被 Vite 复制到输出目录
- 可通过 `/static/` 路径直接访问

**配置**：
```typescript
// vite.config.ts
export default defineConfig({
  publicDir: resolve(__dirname, 'static'),  // 指向 static/ 目录
  // ...
})
```

**目录结构**：
```
riveredge-frontend/static/
├── img/                  # 图片资源
│   ├── login.png
│   └── logo.png
└── social/               # 社交图标
    ├── dingtalk.svg
    ├── feishu.svg
    └── ...
```

### 用户上传文件目录

**位置**：`riveredge-backend/uploads/`

**用途**：
- 存放用户上传的文件（按组织隔离）
- 不通过静态文件服务提供，需要通过 API 访问

**目录结构**：
```
riveredge-backend/uploads/
└── {tenant_id}/          # 按组织隔离
    └── {year}/           # 按年份组织
        └── {month}/      # 按月份组织
            └── {filename}
```

### 命名规范总结

| 类型 | 后端 | 前端 | 说明 |
|------|------|------|------|
| **静态文件目录** | `static/` | `static/` | 统一使用 `static/` 名称，统一放在项目根目录（路径深度一致） |
| **路径深度** | 1 层（项目根目录） | 1 层（项目根目录） | 路径深度保持一致 |
| **挂载路径** | `/static` | `/static` | 统一使用 `/static` 路径 |
| **用户上传目录** | `uploads/` | - | 仅后端存在 |

### 检查清单

- [ ] 后端静态文件目录是否为 `static/`（项目根目录）？
- [ ] 前端静态文件目录是否为 `static/`（项目根目录）？
- [ ] 是否统一使用 `static/` 作为文件夹名称？
- [ ] Vite 配置是否正确指向 `static/` 目录？
- [ ] 路径深度是否一致（都放在项目根目录，深度为 1）？
- [ ] FastAPI 是否正确挂载 `static/` 到 `/static` 路径？

---

## 📚 相关文档

- [1.框架命名哲学.md](./1.框架命名哲学.md) - 框架命名哲学
- [2.字段命名规范.md](./2.字段命名规范.md) - 详细字段命名规范
- [3.数据库命名规范.md](./3.数据库命名规范.md) - 详细数据库命名规范
- [5.项目结构规范.md](./5.项目结构规范.md) - 项目结构规范
- [6.API设计规范.md](./6.API设计规范.md) - API 设计规范

---

---

## 🔄 迁移指南

### 从旧规范迁移到新规范

#### API 路径迁移

**旧规范**（snake_case + 旧前缀）：
- `/api/v1/platform/tenants`（旧平台级前缀）
- `/api/v1/system/user_roles`（旧系统级前缀 + snake_case）
- `/api/v1/system/data_dictionaries`（旧系统级前缀 + snake_case）

**新规范**（kebab-case + 新前缀，对应后端文件夹）：
- `/api/v1/infra/tenants`（新平台级前缀，对应 `infra/` 文件夹）
- `/api/v1/core/user-roles`（新系统级前缀，对应 `core/` 文件夹 + kebab-case）
- `/api/v1/core/data-dictionaries`（新系统级前缀，对应 `core/` 文件夹 + kebab-case）

**迁移步骤**：
1. 保持旧 API 路径向后兼容（添加别名路由）
2. 新功能使用 kebab-case
3. 逐步迁移现有 API，更新文档和前端调用

#### 前端目录迁移

**旧规范**（snake_case）：
- `components/uni_table/`
- `pages/user_profile/`

**新规范**（kebab-case）：
- `components/uni-table/`
- `pages/user-profile/`

**迁移步骤**：
1. 新代码优先使用 kebab-case
2. 现有代码可以保持 snake_case
3. 同一模块内保持命名风格一致
4. 逐步重构，不强制一次性迁移

---

## 📁 静态文件目录规范（必须严格遵循）

### 统一命名规范

**规则**：前端和后端统一使用 `static/` 作为静态文件夹名称，保持命名一致性。

### 后端静态文件目录

**位置**：`riveredge-backend/static/`（项目根目录，与前端保持一致）

**用途**：
- 存放后端静态资源文件（如 Swagger UI 资源）
- 通过 FastAPI 的 `StaticFiles` 挂载到 `/static` 路径

**示例**：
```python
# 在 main.py 中挂载静态文件
app.mount("/static", StaticFiles(directory="static"), name="static")
```

**目录结构**：
```
riveredge-backend/static/
└── swagger-ui/           # Swagger UI 静态资源
    ├── swagger-ui.css
    └── swagger-ui-bundle.js
```

### 前端静态文件目录

**位置**：`riveredge-frontend/static/`（项目根目录）

**用途**：
- 存放前端静态资源文件（图片、图标、字体等）
- 在构建时会被 Vite 复制到输出目录
- 可通过 `/static/` 路径直接访问

**配置**：
```typescript
// vite.config.ts
export default defineConfig({
  publicDir: resolve(__dirname, 'static'),  // 指向 static/ 目录
  // ...
})
```

**目录结构**：
```
riveredge-frontend/static/
├── img/                  # 图片资源
│   ├── login.png
│   └── logo.png
└── social/               # 社交图标
    ├── dingtalk.svg
    ├── feishu.svg
    └── ...
```

### 用户上传文件目录

**位置**：`riveredge-backend/uploads/`

**用途**：
- 存放用户上传的文件（按组织隔离）
- 不通过静态文件服务提供，需要通过 API 访问

**目录结构**：
```
riveredge-backend/uploads/
└── {tenant_id}/          # 按组织隔离
    └── {year}/           # 按年份组织
        └── {month}/      # 按月份组织
            └── {filename}
```

### 命名规范总结

| 类型 | 后端 | 前端 | 说明 |
|------|------|------|------|
| **静态文件目录** | `static/` | `static/` | 统一使用 `static/` 名称，统一放在项目根目录（路径深度一致） |
| **路径深度** | 1 层（项目根目录） | 1 层（项目根目录） | 路径深度保持一致 |
| **挂载路径** | `/static` | `/static` | 统一使用 `/static` 路径 |
| **用户上传目录** | `uploads/` | - | 仅后端存在 |

### 检查清单

- [ ] 后端静态文件目录是否为 `static/`（项目根目录）？
- [ ] 前端静态文件目录是否为 `static/`（项目根目录）？
- [ ] 是否统一使用 `static/` 作为文件夹名称？
- [ ] Vite 配置是否正确指向 `static/` 目录？
- [ ] 路径深度是否一致（都放在项目根目录，深度为 1）？
- [ ] FastAPI 是否正确挂载 `static/` 到 `/static` 路径？

---

**最后更新**：2025-01-04  
**状态**：统一命名规范（平台级、系统级、应用级）- 基于最佳实践优化

