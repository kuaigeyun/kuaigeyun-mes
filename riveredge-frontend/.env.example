# RiverEdge SaaS 多组织框架 - 前端环境变量配置示例
# 
# 使用方法：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 根据实际环境修改配置值
# 3. .env 文件已加入 .gitignore，不会被提交到版本库
#
# 注意：Vite 要求环境变量必须以 VITE_ 开头才能在前端代码中访问

# ========================================
# 前端服务配置
# ========================================
# 前端服务主机地址（Windows 使用 127.0.0.1，Linux/Mac 使用 0.0.0.0）
VITE_HOST=
# 前端服务端口（避免与主流项目常用端口 3000、5173 冲突）
VITE_PORT=8100

# ========================================
# 后端服务配置（用于 API 代理）
# ========================================
# 后端服务主机地址
VITE_BACKEND_HOST=127.0.0.1
# 后端服务端口
VITE_BACKEND_PORT=8200
# 后端 API 完整地址（可选，如果不设置则自动从 VITE_BACKEND_HOST 和 VITE_BACKEND_PORT 生成）
# VITE_API_TARGET=http://127.0.0.1:8200

# ========================================
# Inngest 服务配置
# ========================================
VITE_INNGEST_HOST=127.0.0.1
VITE_INNGEST_PORT=8300

# ========================================
# HMR 配置（热模块替换）
# ========================================
# HMR 主机地址（可选，默认使用 localhost）
# VITE_HMR_HOST=localhost
