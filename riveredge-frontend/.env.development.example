# 复制为 .env.development 后按需修改（可选；多数场景用默认即可）
#
# 开发监听：0.0.0.0 → localhost / 127.0.0.1 / 局域网 IP 均可打开前端
# VITE_HOST=0.0.0.0
# VITE_PORT=8100
#
# Vite 代理目标（指向本机后端；用 127.0.0.1 避免 Windows 上 localhost→::1 问题）
# VITE_BACKEND_HOST=127.0.0.1
# VITE_BACKEND_PORT=8200
# 或一次性指定：
# VITE_API_TARGET=http://127.0.0.1:8200
#
# 本地自定义域名（非 IP）访问 Vite 时需放行 Host：
# VITE_ALLOWED_HOSTS=dev.example.com,.local
