# RiverEdge SaaS 多组织框架 - 后端依赖 (Python 3.11.9 + pip 24.x)
# 锁定版本 - 确保可重现的构建环境
# 生成时间: 2025-11-29
# 第一阶段升级时间: 2025-01-XX（后端核心升级）

# Web 框架 - 第一阶段升级（2025-01-XX）
fastapi>=0.115.0,<0.116.0  # 升级：0.104.1 → 0.115.0+（性能优化，向后兼容）
uvicorn[standard]>=0.32.0,<0.33.0  # 升级：0.30.0 → 0.32.0+（性能优化，WebSocket 改进）

# 数据验证 - 第一阶段升级（2025-01-XX）
pydantic>=2.9.0,<2.10.0  # 升级：2.8.0 → 2.9.0+（性能优化，Bug 修复）
pydantic-settings>=2.6.0,<2.7.0  # 升级：2.5.0 → 2.6.0+（与 Pydantic 2.9+ 同步）

# ORM 和数据库 - 稳定兼容版本（第二阶段升级）
tortoise-orm==0.20.1  # 保持当前版本，与FastAPI 0.115+兼容，第二阶段升级到0.21.1
aerich==0.7.1
asyncpg==0.29.0  # Python 3.11+ 预编译包
psycopg2-binary==2.9.9  # 同步备选方案

# 缓存
redis[hiredis]==5.0.1

# 认证和安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# HTTP 客户端 - 第一阶段升级（2025-01-XX）
httpx>=0.27.0,<0.28.0  # 升级：0.25.2 → 0.27.0+（性能优化，更好的异步支持）

# 日志 - 第一阶段升级（2025-01-XX）
loguru>=0.7.3,<0.8.0  # 升级：0.7.2 → 0.7.3+（Bug 修复，性能优化）

# 系统监控
psutil==5.9.8  # 跨平台系统资源监控库（CPU、内存、磁盘、网络等）

# 测试框架
pytest==7.4.3
pytest-asyncio==0.21.1

# 环境变量管理（必需，用于加载 .env 文件）
python-dotenv>=1.0.0

# 中文拼音支持（可选，用于拼音首字母搜索）
pypinyin>=0.51.0  # 中文拼音转换库，支持拼音首字母提取

# 异步文件操作（第三阶段：文件管理）
aiofiles>=23.2.1  # 异步文件操作库，用于文件上传、下载等操作

# 工作流引擎（第四阶段：流程管理）
inngest>=0.3.0  # Inngest Python SDK，用于工作流编排和事件驱动

