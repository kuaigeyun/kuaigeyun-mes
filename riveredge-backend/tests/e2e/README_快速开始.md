# 端到端测试快速开始

## 🚀 快速运行

### 方法1：使用便捷脚本（推荐）

```bash
# 在项目根目录
./run-e2e-tests.sh
```

### 方法2：手动运行

```bash
cd riveredge-backend

# 1. 安装测试依赖
uv sync --extra dev

# 2. 运行所有测试
python tests/e2e/run_all_tests.py

# 或使用pytest直接运行
pytest tests/e2e/ -v --asyncio-mode=auto
```

## 📋 测试分类运行

### 运行特定类型的测试

```bash
# 只运行MTS流程测试
pytest tests/e2e/test_mts_workflow.py -v -m mts

# 只运行MTO流程测试
pytest tests/e2e/test_mto_workflow.py -v -m mto

# 只运行采购流程测试
pytest tests/e2e/test_purchase_workflow.py -v -m purchase

# 只运行质量管理流程测试
pytest tests/e2e/test_quality_workflow.py -v -m quality

# 只运行财务协同流程测试
pytest tests/e2e/test_finance_workflow.py -v -m finance
```

## 📊 查看测试报告

测试运行后会生成：

1. **控制台输出**：实时显示测试进度和结果
2. **JUnit XML**：`test_results.xml`（用于CI/CD）
3. **HTML报告**：`test_report.html`（详细报告）

## ⚙️ 配置说明

### 测试数据库

建议使用独立的测试数据库，修改 `.env` 文件：

```env
# 测试数据库配置
TEST_DATABASE_URL=postgresql://user:password@localhost:5432/riveredge_test
```

### 测试数据

测试会自动创建和清理测试数据，无需手动准备。

## 🔍 常见问题

### Q: 测试失败，提示模块未找到

**A**: 确保已安装测试依赖：
```bash
uv sync --extra dev
```

### Q: 测试失败，提示数据库连接错误

**A**: 
1. 检查数据库是否运行
2. 检查数据库连接配置
3. 确保测试数据库已创建

### Q: 认证失败

**A**: 检查 `conftest.py` 中的认证逻辑，确保实现了真实的登录流程。

## 📚 更多信息

查看 [README_端到端测试.md](./README_端到端测试.md) 获取详细文档。

