# RiverEdge SaaS 多组织框架 - 后端环境变量配置示例
# 
# 使用方法：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 根据实际环境修改配置值
# 3. .env 文件已加入 .gitignore，不会被提交到版本库

# ========================================
# 应用配置
# ========================================
APP_NAME=RiverEdge SaaS Framework
APP_VERSION=1.0.0
DEBUG=false
ENVIRONMENT=development

# ========================================
# 服务器配置
# ========================================
# 后端服务主机地址（Windows 使用 127.0.0.1，Linux/Mac 使用 0.0.0.0）
HOST=127.0.0.1
# 后端服务端口（避免与系统保留端口和主流项目常用端口冲突）
PORT=8200

# ========================================
# 前端服务配置（用于 CORS 配置）
# ========================================
FRONTEND_HOST=127.0.0.1
FRONTEND_PORT=8100

# ========================================
# Inngest 服务配置
# ========================================
INNGEST_HOST=127.0.0.1
# INNGEST_PORT 使用 Inngest 默认端口 8288，不需要配置
# Inngest 事件 API 地址（可选，如果不设置则自动从 INNGEST_HOST 和默认端口生成）
# INNGEST_EVENT_API_URL=http://127.0.0.1:8288
INNGEST_APP_ID=riveredge
INNGEST_IS_PRODUCTION=false

# ========================================
# 数据库配置 (PostgreSQL)
# ========================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=riveredge

# ========================================
# Redis 配置
# ========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ========================================
# JWT 配置
# ========================================
# ⚠️ 重要：生产环境必须修改此密钥
JWT_SECRET_KEY=your-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ========================================
# CORS 配置
# ========================================
# CORS 允许的来源（多个值用逗号分隔，如果不设置则自动从前端配置生成）
# CORS_ORIGINS=http://127.0.0.1:8100,http://localhost:8100
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*

# ========================================
# 日志配置
# ========================================
LOG_LEVEL=INFO
LOG_FILE=logs/riveredge.log

# ========================================
# 时区配置
# ========================================
USE_TZ=false
TIMEZONE=UTC

# ========================================
# 平台超级管理员配置
# ========================================
PLATFORM_SUPERADMIN_USERNAME=infra_admin
# ⚠️ 重要：生产环境必须设置强密码
PLATFORM_SUPERADMIN_PASSWORD=
PLATFORM_SUPERADMIN_EMAIL=infra_admin@riveredge.cn
PLATFORM_SUPERADMIN_FULL_NAME=平台超级管理员

# ========================================
# 文件管理配置
# ========================================
FILE_UPLOAD_DIR=./uploads
MAX_FILE_SIZE=104857600
# 基础URL（用于生成文件下载链接，如果不设置则自动从 HOST 和 PORT 生成）
# BASE_URL=http://127.0.0.1:8200
KKFILEVIEW_URL=http://localhost:8400
