# 移动端 API 地址（可选）
# 未配置时：Web/iOS 用 localhost:8200，Android 模拟器用 10.0.2.2:8200
# 真机/内网调试时必填，使用电脑局域网 IP（手机与电脑需在同一 WiFi）：
# EXPO_PUBLIC_API_BASE_URL=http://192.168.10.101:8200/api/v1
#
# 注意：修改后需重启 Expo (npx expo start)
# 后端需设置 HOST=0.0.0.0 才能接受内网连接（见 riveredge-backend/.env）
