# RiverEdge SaaS 多租户框架 - 后端依赖 (Python 3.11.9 + pip 24.x)
# 锁定版本 - 确保可重现的构建环境
# 生成时间: 2025-11-18

# Web 框架 - 稳定兼容版本
fastapi==0.110.0
uvicorn[standard]==0.30.0

# 数据验证 - 稳定兼容版本
pydantic==2.7.0
pydantic-settings==2.5.0

# ORM 和数据库 - 稳定兼容版本
tortoise-orm==0.21.1  # 与FastAPI 0.110.0完美兼容
aerich==0.7.1
asyncpg==0.28.0  # Python 3.11+ 预编译包
psycopg2-binary==2.9.7  # 同步备选方案

# 缓存
redis[hiredis]==5.0.1

# 认证和安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# HTTP 客户端
httpx==0.25.2

# 日志
loguru==0.7.2

# 测试框架
pytest==7.4.3
pytest-asyncio==0.21.1
