# 数据集管理最佳实践

## 📋 模块概述

**模块名称**：数据集管理（Dataset Management）  
**优先级**：⭐⭐⭐⭐（高优先级）  
**预计时间**：2-3 周  
**依赖**：数据源管理（数据集需要数据源）

**功能范围**：
- 数据集定义（数据集名称、关联数据源、查询语句）
- 数据集管理（CRUD）
- 数据集查询执行（执行查询、预览数据）
- 数据集数据预览（分页、排序、筛选）

---

## 🔍 库选择评估

### 数据集管理实现方案

**结论**：✅ **使用自定义实现 + pandas（可选）**

#### 方案一：自定义实现 + pandas ⭐⭐⭐⭐（推荐）

**优势**：
- ✅ 功能完整，支持 SQL 查询和 API 查询
- ✅ pandas 支持数据分析（可选）
- ✅ 多租户隔离天然支持（tenant_id 字段）
- ✅ 完全符合框架规范（命名、注释、代码风格）
- ✅ 支持数据预览和导出

**劣势**：
- ⚠️ pandas 是可选依赖，增加项目复杂度
- ⚠️ 需要自己实现查询执行逻辑

**适用场景**：
- ✅ 需要执行 SQL 查询
- ✅ 需要数据分析功能
- ✅ 需要多租户数据集隔离

**实现复杂度**：⭐⭐⭐（中等，2-3 周可完成）

---

#### 方案二：仅支持 SQL 查询 ⭐⭐⭐（简化方案）

**优势**：
- ✅ 实现简单，使用 Tortoise ORM 或 SQLAlchemy
- ✅ 无需额外依赖
- ✅ 多租户隔离天然支持

**劣势**：
- ⚠️ 不支持数据分析功能

**适用场景**：
- ✅ 仅需支持 SQL 查询
- ✅ 需要快速实现

**实现复杂度**：⭐⭐（简单，1-2 周可完成）

---

### 最终推荐方案

**推荐**：✅ **优先支持 SQL 查询，可选支持数据分析**

**理由**：
1. ✅ **SQL 查询是核心功能**，优先支持
2. ✅ **数据分析可选**，根据实际需求决定是否支持
3. ✅ **pandas 可选**，仅在需要数据分析时安装
4. ✅ **多租户是核心需求**，自定义实现天然支持
5. ✅ **完全符合框架规范**，代码风格统一

---

## 🗄️ 数据库设计

### 数据集表（root_datasets）

```sql
CREATE TABLE root_datasets (
    id SERIAL PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,        -- 业务ID（UUID，对外暴露，安全且唯一）
    tenant_id INTEGER NOT NULL,
    data_source_id INTEGER NOT NULL,         -- 关联数据源ID（内部使用自增ID）
    
    -- 数据集基本信息
    name VARCHAR(100) NOT NULL,              -- 数据集名称
    code VARCHAR(50) NOT NULL,               -- 数据集代码（唯一，用于程序识别）
    description TEXT,                         -- 数据集描述
    
    -- 查询配置
    query_type VARCHAR(20) NOT NULL,          -- 查询类型（sql、api）
    query_config JSONB NOT NULL,              -- 查询配置（JSON格式）
    
    -- 数据集状态
    is_active BOOLEAN DEFAULT TRUE,          -- 是否启用
    last_executed_at TIMESTAMP NULL,          -- 最后执行时间
    last_error TEXT,                          -- 最后执行错误信息
    
    -- 标准字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,
    
    -- 索引
    CONSTRAINT uk_root_datasets_tenant_code UNIQUE (tenant_id, code),
    INDEX idx_root_datasets_tenant_id (tenant_id),
    INDEX idx_root_datasets_uuid (uuid),
    INDEX idx_root_datasets_code (code),
    INDEX idx_root_datasets_data_source_id (data_source_id),
    INDEX idx_root_datasets_created_at (created_at),
    
    -- 外键
    FOREIGN KEY (data_source_id) REFERENCES root_data_sources(id) ON DELETE RESTRICT
);
```

**设计要点**：
- ✅ **混合ID方案**：`id`（自增ID，内部使用）+ `uuid`（UUID，对外暴露）
- ✅ `code` 字段用于程序识别，与 `name` 分离（支持多语言）
- ✅ `data_source_id` 关联数据源（内部使用自增ID）
- ✅ `query_type` 字段标识查询类型（sql、api）
- ✅ `query_config` 字段使用 JSONB 存储查询配置（根据类型不同而不同）
- ✅ `tenant_id + code` 唯一约束，确保组织内数据集代码唯一
- ✅ 软删除支持（`deleted_at`）

**查询配置示例**：

**SQL 查询**：
```json
{
  "sql": "SELECT * FROM users WHERE status = :status",
  "parameters": {
    "status": "active"
  }
}
```

**API 查询**：
```json
{
  "endpoint": "/api/users",
  "method": "GET",
  "params": {
    "status": "active"
  },
  "headers": {
    "Authorization": "Bearer token"
  }
}
```

---

## 🐍 后端模型设计

### 数据集模型（Dataset）

```python
# riveredge-backend/src/tree_root/models/dataset.py
from tortoise.models import Model
from tortoise import fields
from typing import Optional, Dict, Any
from .base import BaseModel
from .data_source import DataSource


class Dataset(BaseModel):
    """
    数据集模型
    
    用于定义和管理组织内的数据集，支持 SQL 查询和 API 查询。
    支持多组织隔离，每个组织的数据集相互独立。
    
    注意：继承自 BaseModel，自动包含 uuid、tenant_id、created_at、updated_at 字段。
    """
    id = fields.IntField(pk=True, description="数据集ID（主键，自增ID，内部使用）")
    # uuid 字段由 BaseModel 提供
    # tenant_id 字段由 BaseModel 提供
    
    data_source: fields.ForeignKeyRelation[DataSource] = fields.ForeignKeyField(
        "models.DataSource",
        related_name="datasets",
        description="关联数据源（内部使用自增ID）"
    )
    data_source_id = fields.IntField(description="数据源ID（内部使用自增ID）")
    
    name = fields.CharField(max_length=100, description="数据集名称")
    code = fields.CharField(max_length=50, description="数据集代码（唯一，用于程序识别）")
    description = fields.TextField(null=True, description="数据集描述")
    
    query_type = fields.CharField(max_length=20, description="查询类型（sql、api）")
    query_config = fields.JSONField(description="查询配置（JSON格式）")
    
    is_active = fields.BooleanField(default=True, description="是否启用")
    last_executed_at = fields.DatetimeField(null=True, description="最后执行时间")
    last_error = fields.TextField(null=True, description="最后执行错误信息")
    
    class Meta:
        table = "root_datasets"
        indexes = [
            ("tenant_id", "code"),  # 唯一索引
            ("data_source_id",),
        ]
        unique_together = [("tenant_id", "code")]
    
    def __str__(self):
        return f"{self.name} ({self.query_type})"
```

---

## 📝 Schema 设计

### 数据集 Schema

```python
# riveredge-backend/src/tree_root/schemas/dataset.py
from pydantic import BaseModel, Field, ConfigDict, field_validator
from typing import Optional, Dict, Any
from datetime import datetime
from uuid import UUID


class DatasetBase(BaseModel):
    """数据集基础 Schema"""
    name: str = Field(..., max_length=100, description="数据集名称")
    code: str = Field(..., max_length=50, description="数据集代码")
    description: Optional[str] = Field(None, description="数据集描述")
    query_type: str = Field(..., max_length=20, description="查询类型")
    query_config: Dict[str, Any] = Field(..., description="查询配置")
    is_active: bool = Field(True, description="是否启用")
    
    @field_validator('query_type')
    def validate_query_type(cls, v):
        """验证查询类型"""
        allowed_types = ['sql', 'api']
        if v not in allowed_types:
            raise ValueError(f'查询类型必须是 {allowed_types} 之一')
        return v


class DatasetCreate(DatasetBase):
    """创建数据集 Schema"""
    data_source_uuid: UUID = Field(..., description="数据源UUID")


class DatasetUpdate(BaseModel):
    """更新数据集 Schema"""
    name: Optional[str] = Field(None, max_length=100, description="数据集名称")
    code: Optional[str] = Field(None, max_length=50, description="数据集代码")
    description: Optional[str] = Field(None, description="数据集描述")
    query_type: Optional[str] = Field(None, max_length=20, description="查询类型")
    query_config: Optional[Dict[str, Any]] = Field(None, description="查询配置")
    is_active: Optional[bool] = Field(None, description="是否启用")


class DatasetResponse(DatasetBase):
    """数据集响应 Schema"""
    uuid: UUID = Field(..., description="数据集UUID")
    tenant_id: int = Field(..., description="组织ID")
    data_source_uuid: UUID = Field(..., description="数据源UUID")
    last_executed_at: Optional[datetime] = Field(None, description="最后执行时间")
    last_error: Optional[str] = Field(None, description="最后执行错误信息")
    created_at: datetime = Field(..., description="创建时间")
    updated_at: datetime = Field(..., description="更新时间")
    
    model_config = ConfigDict(from_attributes=True)


class ExecuteQueryRequest(BaseModel):
    """执行查询请求 Schema"""
    parameters: Optional[Dict[str, Any]] = Field(None, description="查询参数（覆盖数据集定义）")
    limit: Optional[int] = Field(100, description="限制返回行数")
    offset: Optional[int] = Field(0, description="偏移量")


class ExecuteQueryResponse(BaseModel):
    """执行查询响应 Schema"""
    success: bool = Field(..., description="查询是否成功")
    data: list = Field(..., description="查询结果数据")
    total: Optional[int] = Field(None, description="总行数（如果支持）")
    columns: Optional[list] = Field(None, description="列信息")
    elapsed_time: float = Field(..., description="查询耗时（秒）")
    error: Optional[str] = Field(None, description="错误信息")
```

---

## 🔧 Service 层设计

### 数据集服务（DatasetService）

**核心功能**：
1. 数据集 CRUD（创建、读取、更新、删除）
2. 查询执行（根据类型执行 SQL 或 API 查询）
3. 数据预览（分页、排序、筛选）
4. 执行状态跟踪（更新执行时间、错误信息）

**关键实现要点**：
- ✅ SQL 查询：使用 Tortoise ORM 或 SQLAlchemy 执行查询
- ✅ API 查询：使用 httpx 执行查询
- ✅ 查询结果包含数据、列信息、总行数、耗时等信息
- ✅ 查询参数支持覆盖数据集定义的参数
- ✅ 查询结果支持分页、排序、筛选

---

## 🌐 API 设计

### 数据集管理 API

**路由前缀**：`/api/tree-root/datasets`

**接口列表**：
1. `POST /` - 创建数据集
2. `GET /list` - 获取数据集列表（分页、搜索、筛选）
3. `GET /{uuid}` - 获取数据集详情
4. `PUT /{uuid}` - 更新数据集
5. `DELETE /{uuid}` - 删除数据集
6. `POST /{uuid}/execute` - 执行数据集查询
7. `GET /{uuid}/preview` - 预览数据集数据（分页、排序、筛选）

---

## 🎨 前端页面设计

### 数据集管理页面

**页面路径**：`pages/system/datasets/list/index.tsx`

**核心功能**：
1. 数据集列表展示（UniTable，支持表格视图）
2. 数据集创建/编辑（Modal + ProForm，根据查询类型动态渲染配置表单）
3. 数据集查询执行（执行查询、预览数据）
4. 数据集数据预览（分页、排序、筛选）
5. 数据集删除（单个删除、批量删除）
6. 数据集搜索和筛选（按名称、查询类型、数据源）

**布局规范**：
- ✅ 使用 `UniTable` 组件展示列表
- ✅ 使用高级搜索（`showAdvancedSearch={true}`）
- ✅ 使用 `searchFormValues` 获取搜索条件
- ✅ 创建/编辑使用 Modal + ProForm
- ✅ 数据预览使用 Drawer 或 Modal 展示查询结果

---

## ⚠️ 重要注意事项

### 1. 吸取第一、第二阶段经验教训

**必须遵守的规范**：
1. ✅ **禁止假设和猜测**：必须直接查看实际代码，使用完全相同的值
2. ✅ **禁止擅作主张**：只做用户明确要求的事，不做额外"优化"
3. ✅ **数据库迁移规范**：必须使用 Aerich 迁移系统，禁止直接使用 SQL
4. ✅ **前端搜索规范**：必须使用高级搜索（`showAdvancedSearch={true}`），使用 `searchFormValues` 获取搜索条件
5. ✅ **UniTable 规范**：`request` 函数必须接收四个参数：`(params, sort, _filter, searchFormValues)`

### 2. 数据集查询执行注意事项

**查询安全**：
- SQL 查询仅限 SELECT 语句，禁止执行 DDL、DML 语句（防止 SQL 注入）
- 查询参数使用参数化查询，防止 SQL 注入
- 查询结果限制返回行数，防止内存溢出
- 查询超时时间合理设置

**查询性能**：
- 大数据集查询支持分页
- 查询结果缓存（可选）
- 查询执行时间限制

**数据源依赖**：
- 数据集依赖数据源，数据源删除时需检查数据集依赖
- 数据源连接失败时，数据集查询执行失败

---

## 📚 相关文档

- [前端页面布局规范.md](./前端页面布局规范.md)
- [系统级功能文件结构规划.md](./系统级功能文件结构规划.md)
- [建设进度.md](./建设进度.md)
- [3.数据源管理最佳实践.md](./3.数据源管理最佳实践.md)

---

**最后更新**：2025-01-XX

