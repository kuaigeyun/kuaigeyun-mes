# 我的任务最佳实践

## 📋 模块概述

**模块名称**：我的任务（My Tasks）  
**优先级**：⭐⭐⭐（中优先级）  
**预计时间**：1 周  
**依赖**：审批流程（任务来自审批）

**功能范围**：
- 任务分类查询（我发起的、我的待办、我的已办、我的抄送）
- 任务列表展示（支持筛选、搜索）
- 任务详情查看（查看审批流程详情、审批历史）
- 任务处理（审批、驳回、转交等）
- 任务统计（待办任务数量等）

**核心特点**：
- ✅ **复用审批实例模型**：不需要新建模型，复用 ApprovalInstance
- ✅ **任务分类查询**：我发起的、我的待办、我的已办、我的抄送
- ✅ **任务统计**：提供待办任务数量等统计信息

---

## 🔍 库选择评估

### 我的任务实现方案

**结论**：✅ **复用审批实例模型（推荐）**

#### 方案一：复用审批实例模型 ⭐⭐⭐⭐⭐（强烈推荐）

**优势**：
- ✅ 无需新建表，直接复用审批实例模型
- ✅ 数据一致性好，任务信息和审批流程信息在同一张表
- ✅ 查询性能好，无需关联查询
- ✅ 多租户隔离天然支持（tenant_id 字段）
- ✅ 完全符合框架规范（命名、注释、代码风格）

**劣势**：
- ⚠️ 需要根据用户角色和状态筛选任务

**适用场景**：
- ✅ 任务来自审批流程
- ✅ 需要按用户角色和状态查询任务
- ✅ 需要多租户数据隔离

**实现复杂度**：⭐（简单，1 周可完成）

---

## 🗄️ 数据库设计

### 复用审批实例表（root_approval_instances）

**注意**：不需要新建表，直接复用审批流程模块的 `ApprovalInstance` 模型。

**查询逻辑**：
- **我发起的**：`created_by_id = current_user.id`
- **我的待办**：`current_approver_id = current_user.id AND status = 'pending'`
- **我的已办**：`approval_history` 中包含当前用户且状态为 `approved` 或 `rejected`
- **我的抄送**：`cc_user_ids` 中包含当前用户ID

---

## 🔧 后端实现

### 1. 创建 Schema

**文件位置**：`riveredge-backend/src/tree_root/schemas/my_task.py`

```python
"""
我的任务 Schema 模块

定义我的任务相关的 Pydantic Schema，用于数据验证和序列化。
"""

from pydantic import BaseModel, Field
from typing import Optional, List
from enum import Enum


class TaskType(str, Enum):
    """任务类型枚举"""
    MY_CREATED = "my_created"      # 我发起的
    MY_PENDING = "my_pending"      # 我的待办
    MY_COMPLETED = "my_completed"  # 我的已办
    MY_CC = "my_cc"                # 我的抄送


class MyTaskListParams(BaseModel):
    """我的任务列表查询参数"""
    task_type: Optional[TaskType] = Field(None, description="任务类型")
    skip: int = Field(0, ge=0, description="跳过数量")
    limit: int = Field(100, ge=1, le=1000, description="限制数量")
```

### 2. 创建 Service

**文件位置**：`riveredge-backend/src/tree_root/services/my_task_service.py`

```python
"""
我的任务管理服务模块

提供我的任务的查询和统计功能。
"""

from typing import List, Optional
from enum import Enum

from tree_root.models.approval_instance import ApprovalInstance
from tree_root.schemas.my_task import TaskType, MyTaskListParams
from soil.exceptions.exceptions import NotFoundError


class MyTaskService:
    """
    我的任务管理服务类
    
    提供我的任务的查询和统计功能。
    """
    
    @staticmethod
    async def list_my_tasks(
        tenant_id: int,
        user_id: int,
        task_type: Optional[TaskType] = None,
        skip: int = 0,
        limit: int = 100
    ) -> List[ApprovalInstance]:
        """
        获取我的任务列表
        
        Args:
            tenant_id: 组织ID
            user_id: 用户ID
            task_type: 任务类型（可选）
            skip: 跳过数量
            limit: 限制数量
            
        Returns:
            List[ApprovalInstance]: 任务列表
        """
        query = ApprovalInstance.filter(tenant_id=tenant_id)
        
        if task_type == TaskType.MY_CREATED:
            # 我发起的
            query = query.filter(created_by_id=user_id)
        elif task_type == TaskType.MY_PENDING:
            # 我的待办
            query = query.filter(
                current_approver_id=user_id,
                status="pending"
            )
        elif task_type == TaskType.MY_COMPLETED:
            # 我的已办（需要查询审批历史）
            # 这里需要根据实际的审批历史结构查询
            # 假设审批历史存储在 approval_history JSON 字段中
            query = query.filter(
                approval_history__contains=[{"user_id": user_id, "status": ["approved", "rejected"]}]
            )
        elif task_type == TaskType.MY_CC:
            # 我的抄送
            query = query.filter(cc_user_ids__contains=[user_id])
        else:
            # 所有任务（我发起的 + 我的待办 + 我的已办 + 我的抄送）
            # 这里需要组合多个查询条件
            pass
        
        return await query.order_by("-created_at").offset(skip).limit(limit).all()
    
    @staticmethod
    async def get_my_task_by_uuid(
        tenant_id: int,
        user_id: int,
        uuid: str
    ) -> ApprovalInstance:
        """
        获取我的任务详情
        
        Args:
            tenant_id: 组织ID
            user_id: 用户ID
            uuid: 任务UUID
            
        Returns:
            ApprovalInstance: 任务对象
            
        Raises:
            NotFoundError: 当任务不存在或不属于当前用户时抛出
        """
        task = await ApprovalInstance.filter(
            tenant_id=tenant_id,
            uuid=uuid
        ).first()
        
        if not task:
            raise NotFoundError("任务不存在")
        
        # 验证任务是否属于当前用户
        if (task.created_by_id != user_id and 
            task.current_approver_id != user_id and
            user_id not in (task.cc_user_ids or [])):
            # 还需要检查审批历史
            # 这里需要根据实际的审批历史结构判断
            raise NotFoundError("无权访问此任务")
        
        return task
    
    @staticmethod
    async def get_my_task_statistics(
        tenant_id: int,
        user_id: int
    ) -> dict:
        """
        获取我的任务统计
        
        Args:
            tenant_id: 组织ID
            user_id: 用户ID
            
        Returns:
            dict: 任务统计信息
        """
        # 我发起的数量
        my_created_count = await ApprovalInstance.filter(
            tenant_id=tenant_id,
            created_by_id=user_id
        ).count()
        
        # 我的待办数量
        my_pending_count = await ApprovalInstance.filter(
            tenant_id=tenant_id,
            current_approver_id=user_id,
            status="pending"
        ).count()
        
        # 我的已办数量（需要查询审批历史）
        # 这里需要根据实际的审批历史结构统计
        
        # 我的抄送数量
        my_cc_count = await ApprovalInstance.filter(
            tenant_id=tenant_id,
            cc_user_ids__contains=[user_id]
        ).count()
        
        return {
            "my_created": my_created_count,
            "my_pending": my_pending_count,
            "my_completed": 0,  # 需要根据实际审批历史统计
            "my_cc": my_cc_count
        }
```

### 3. 创建 API

**文件位置**：`riveredge-backend/src/tree_root/api/my_tasks/my_tasks.py`

```python
"""
我的任务管理 API 路由

提供我的任务的查询和统计功能。
"""

from typing import Optional, List
from fastapi import APIRouter, Depends, HTTPException, status, Query

from tree_root.schemas.my_task import TaskType, MyTaskListParams
from tree_root.services.my_task_service import MyTaskService
from tree_root.api.deps.deps import get_current_tenant
from soil.api.deps.deps import get_current_user
from soil.models.user import User
from soil.exceptions.exceptions import NotFoundError

router = APIRouter(prefix="/my-tasks", tags=["MyTasks"])


@router.get("", response_model=List[dict])
async def list_my_tasks(
    task_type: Optional[TaskType] = Query(None, description="任务类型"),
    skip: int = Query(0, ge=0, description="跳过数量"),
    limit: int = Query(100, ge=1, le=1000, description="限制数量"),
    current_user: User = Depends(get_current_user),
    tenant_id: int = Depends(get_current_tenant),
):
    """
    获取我的任务列表
    
    Args:
        task_type: 任务类型（可选）
        skip: 跳过数量
        limit: 限制数量
        current_user: 当前用户
        tenant_id: 当前组织ID
        
    Returns:
        List[dict]: 任务列表
    """
    tasks = await MyTaskService.list_my_tasks(
        tenant_id=tenant_id,
        user_id=current_user.id,
        task_type=task_type,
        skip=skip,
        limit=limit
    )
    return [task.model_dump() for task in tasks]


@router.get("/{uuid}", response_model=dict)
async def get_my_task(
    uuid: str,
    current_user: User = Depends(get_current_user),
    tenant_id: int = Depends(get_current_tenant),
):
    """
    获取我的任务详情
    
    Args:
        uuid: 任务UUID
        current_user: 当前用户
        tenant_id: 当前组织ID
        
    Returns:
        dict: 任务对象
        
    Raises:
        HTTPException: 当任务不存在或无权访问时抛出
    """
    try:
        task = await MyTaskService.get_my_task_by_uuid(
            tenant_id=tenant_id,
            user_id=current_user.id,
            uuid=uuid
        )
        return task.model_dump()
    except NotFoundError as e:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=str(e)
        )


@router.get("/statistics", response_model=dict)
async def get_my_task_statistics(
    current_user: User = Depends(get_current_user),
    tenant_id: int = Depends(get_current_tenant),
):
    """
    获取我的任务统计
    
    Args:
        current_user: 当前用户
        tenant_id: 当前组织ID
        
    Returns:
        dict: 任务统计信息
    """
    return await MyTaskService.get_my_task_statistics(
        tenant_id=tenant_id,
        user_id=current_user.id
    )
```

---

## 🌐 API 设计

### 我的任务管理 API

**路由前缀**：`/api/v1/personal/my-tasks`

**接口列表**：
1. `GET /` - 获取我的任务列表（支持筛选任务类型）
2. `GET /{uuid}` - 获取任务详情
3. `GET /statistics` - 获取任务统计（待办数量等）

**注意**：任务处理（审批、驳回、转交等）应该在审批流程模块的 API 中实现。

---

## 🎨 前端页面设计

### 我的任务页面

**页面路径**：`pages/personal/tasks/index.tsx`

**核心功能**：
1. 任务分类（我发起的、我的待办、我的已办、我的抄送）
2. 任务列表展示（UniTable，支持筛选、搜索）
3. 任务详情查看（查看审批流程详情、审批历史）
4. 任务处理（审批、驳回、转交等，调用审批流程 API）
5. 待办任务数量提示（右上角角标）

**布局规范**：
- ✅ 使用 Tabs 组件分类任务（我发起的、我的待办、我的已办、我的抄送）
- ✅ 使用 `UniTable` 组件展示列表
- ✅ 使用高级搜索（`showAdvancedSearch={true}`）
- ✅ 使用 `searchFormValues` 获取搜索条件
- ✅ 任务详情使用 Drawer 或 Modal 展示
- ✅ 待办任务数量使用 Badge 组件显示

---

## ⚠️ 重要注意事项

### 1. 任务查询注意事项

**要点**：
- 我发起的：查询 `created_by_id = current_user.id`
- 我的待办：查询 `current_approver_id = current_user.id AND status = 'pending'`
- 我的已办：需要查询审批历史，判断当前用户是否已审批
- 我的抄送：查询 `cc_user_ids` 中包含当前用户ID

### 2. 任务权限控制注意事项

**要点**：
- 用户只能查看自己相关的任务（我发起的、我的待办、我的已办、我的抄送）
- 任务详情查询时需要验证任务是否属于当前用户
- 任务处理需要调用审批流程模块的 API

### 3. 多租户支持

**要点**：
- 任务按组织隔离（tenant_id）
- 用户只能查看自己组织内的任务
- API 自动过滤当前用户和组织，无需手动过滤

---

## 📚 相关文档

- [前端页面布局规范.md](./前端页面布局规范.md)
- [系统级功能文件结构规划.md](./系统级功能文件结构规划.md)
- [建设进度.md](./建设进度.md)
- [3.审批流程最佳实践.md](../sys第四阶段/3.审批流程最佳实践.md)

---

**最后更新**：2025-01-XX

