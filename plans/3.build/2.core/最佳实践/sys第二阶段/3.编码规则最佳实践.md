# 编码规则最佳实践

## 📋 模块概述

**模块名称**：编码规则（Code Rule Management）  
**优先级**：⭐⭐⭐⭐（高优先级）  
**预计时间**：1-2 周  
**依赖**：数据字典（可能需要字典支持）

**功能范围**：
- 编码规则管理（CRUD）
- 编码生成服务（根据规则生成编码）
- 编码规则测试（测试生成编码）
- 支持变量、日期、序号等

---

## 🔍 库选择评估

### 编码生成实现方案

**结论**：✅ **使用自定义实现（推荐）**

经过调研，编码生成功能主要是字符串模板处理和序号管理：

#### 方案一：自定义实现 ⭐⭐⭐⭐⭐（强烈推荐）

**优势**：
- ✅ 功能相对简单，实现成本低（1-2 周可完成）
- ✅ 与 Tortoise ORM 完美集成（原生支持）
- ✅ 多租户隔离天然支持（tenant_id 字段）
- ✅ 完全符合框架规范（命名、注释、代码风格）
- ✅ 完全控制，易于定制和扩展
- ✅ 支持灵活的表达式语法（变量、日期、序号等）
- ✅ 无需引入额外依赖

**劣势**：
- ⚠️ 需要自己实现表达式解析和序号管理

**适用场景**：
- ✅ 标准的编码规则需求（变量、日期、序号）
- ✅ 需要与现有框架深度集成
- ✅ 需要完全符合项目规范
- ✅ 多租户是核心需求

**实现复杂度**：⭐⭐（中等，1-2 周可完成）

---

#### 方案二：使用第三方库 ⭐⭐（不推荐）

**调研结果**：
- ❌ Python 生态中没有专门的编码生成库
- ❌ 有 `python-sequence` 等序号生成库，但功能单一，不满足需求
- ❌ 有模板引擎（如 Jinja2），但需要额外封装，增加复杂度

**结论**：编码生成功能相对简单，使用第三方库反而增加复杂度，不推荐。

---

### 最终推荐方案

**推荐**：✅ **使用自定义实现**

**理由**：
1. ✅ **功能相对简单**，自定义实现成本低（1-2 周）
2. ✅ **无需额外依赖**，减少项目复杂度
3. ✅ **多租户是核心需求**，自定义实现天然支持
4. ✅ **完全符合框架规范**，代码风格统一
5. ✅ **维护成本低**，团队完全掌控代码
6. ✅ **支持灵活的表达式语法**，易于扩展

---

## 🔍 技术要点

### 编码规则表达式

**支持的变量**：
- `{YYYY}` - 4位年份
- `{YY}` - 2位年份
- `{MM}` - 月份（01-12）
- `{DD}` - 日期（01-31）
- `{SEQ}` - 序号（自动递增）
- `{SEQ:4}` - 序号（4位，不足补0）
- `{DICT:字典代码}` - 字典值（需要数据字典支持）

**示例**：
- `ORDER-{YYYY}{MM}{DD}-{SEQ:4}` → `ORDER-20250115-0001`
- `{DICT:ORDER_TYPE}-{SEQ:6}` → `PURCHASE-000001`

---

## 🗄️ 数据库设计

### 编码规则表（root_code_rules）

```sql
CREATE TABLE root_code_rules (
    id SERIAL PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,        -- 业务ID（UUID，对外暴露，安全且唯一）
    tenant_id INTEGER NOT NULL,
    
    -- 规则基本信息
    name VARCHAR(100) NOT NULL,              -- 规则名称
    code VARCHAR(50) NOT NULL,               -- 规则代码（唯一，用于程序识别）
    expression VARCHAR(200) NOT NULL,        -- 规则表达式
    description TEXT,                         -- 规则描述
    
    -- 序号配置
    seq_start INTEGER DEFAULT 1,              -- 序号起始值
    seq_step INTEGER DEFAULT 1,               -- 序号步长
    seq_reset_rule VARCHAR(20),               -- 序号重置规则：never、daily、monthly、yearly
    
    -- 状态
    is_system BOOLEAN DEFAULT FALSE,          -- 是否系统规则
    is_active BOOLEAN DEFAULT TRUE,           -- 是否启用
    
    -- 标准字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,
    
    -- 索引
    CONSTRAINT uk_root_code_rules_tenant_code UNIQUE (tenant_id, code),
    INDEX idx_root_code_rules_tenant_id (tenant_id),
    INDEX idx_root_code_rules_uuid (uuid),
    INDEX idx_root_code_rules_code (code),
    INDEX idx_root_code_rules_created_at (created_at)
);
```

### 编码序号表（root_code_sequences）

```sql
CREATE TABLE root_code_sequences (
    id SERIAL PRIMARY KEY,
    code_rule_id INTEGER NOT NULL,            -- 关联编码规则ID（内部使用自增ID）
    tenant_id INTEGER NOT NULL,
    
    -- 序号信息
    current_seq INTEGER DEFAULT 0,           -- 当前序号
    reset_date DATE,                           -- 重置日期（用于按日/月/年重置）
    
    -- 标准字段
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    -- 索引
    CONSTRAINT uk_root_code_sequences_rule UNIQUE (code_rule_id, tenant_id),
    INDEX idx_root_code_sequences_tenant_id (tenant_id),
    INDEX idx_root_code_sequences_code_rule_id (code_rule_id),
    
    -- 外键
    FOREIGN KEY (code_rule_id) REFERENCES root_code_rules(id) ON DELETE CASCADE
);
```

**设计要点**：
- ✅ `expression` 存储编码规则表达式
- ✅ `seq_reset_rule` 控制序号重置策略
- ✅ `CodeSequence` 表存储每个规则的当前序号和重置日期

---

## 🐍 后端模型设计

### 编码规则模型（CodeRule）

```python
# riveredge-backend/src/tree-root/models/code_rule.py
from tortoise.models import Model
from tortoise import fields
from .base import BaseModel
import re
from datetime import datetime


class CodeRule(BaseModel):
    """
    编码规则模型
    
    用于定义自动编码生成规则，支持变量、日期、序号等。
    支持多组织隔离，每个组织可以定义自己的编码规则。
    
    注意：继承自 BaseModel，自动包含 uuid、tenant_id、created_at、updated_at 字段。
    """
    id = fields.IntField(pk=True, description="规则ID（主键，自增ID，内部使用）")
    
    name = fields.CharField(max_length=100, description="规则名称")
    code = fields.CharField(max_length=50, description="规则代码（唯一，用于程序识别）")
    expression = fields.CharField(max_length=200, description="规则表达式")
    description = fields.TextField(null=True, description="规则描述")
    
    seq_start = fields.IntField(default=1, description="序号起始值")
    seq_step = fields.IntField(default=1, description="序号步长")
    seq_reset_rule = fields.CharField(
        max_length=20,
        null=True,
        description="序号重置规则：never、daily、monthly、yearly"
    )
    
    is_system = fields.BooleanField(default=False, description="是否系统规则")
    is_active = fields.BooleanField(default=True, description="是否启用")
    
    class Meta:
        table = "root_code_rules"
        unique_together = [("tenant_id", "code")]
    
    def validate_expression(self) -> bool:
        """验证表达式是否有效"""
        # 检查表达式格式
        pattern = r'\{[A-Z0-9_:]+\}'
        matches = re.findall(pattern, self.expression)
        # 验证变量是否支持
        supported_vars = ['YYYY', 'YY', 'MM', 'DD', 'SEQ', 'DICT']
        for match in matches:
            var = match.strip('{}').split(':')[0]
            if var not in supported_vars:
                return False
        return True
```

### 编码序号模型（CodeSequence）

```python
# riveredge-backend/src/tree-root/models/code_sequence.py
from tortoise.models import Model
from tortoise import fields
from datetime import date


class CodeSequence(Model):
    """
    编码序号模型
    
    用于存储每个编码规则的当前序号和重置信息。
    """
    id = fields.IntField(pk=True, description="序号ID（主键，自增ID，内部使用）")
    
    code_rule_id = fields.IntField(description="关联编码规则ID（内部使用自增ID）")
    tenant_id = fields.IntField(description="组织ID")
    
    current_seq = fields.IntField(default=0, description="当前序号")
    reset_date = fields.DateField(null=True, description="重置日期")
    
    updated_at = fields.DatetimeField(auto_now=True, description="更新时间")
    
    class Meta:
        table = "root_code_sequences"
        unique_together = [("code_rule_id", "tenant_id")]
```

---

## 🔧 Service 层设计

### 编码生成服务（CodeGenerationService）

```python
# riveredge-backend/src/tree-root/services/code_generation_service.py
from typing import Optional, Dict
from datetime import datetime, date
import re
from tortoise.transactions import in_transaction

from ..models.code_rule import CodeRule
from ..models.code_sequence import CodeSequence
from ...soil.services.data_dictionary_service import DataDictionaryService


class CodeGenerationService:
    """编码生成服务"""
    
    @staticmethod
    async def generate_code(
        tenant_id: int,
        rule_code: str,
        context: Optional[Dict] = None
    ) -> str:
        """
        根据规则生成编码
        
        Args:
            tenant_id: 组织ID
            rule_code: 规则代码
            context: 上下文变量（可选，用于自定义变量）
            
        Returns:
            生成的编码
        """
        # 获取编码规则
        rule = await CodeRule.get_or_none(
            tenant_id=tenant_id,
            code=rule_code,
            deleted_at__isnull=True,
            is_active=True
        )
        if not rule:
            raise ValueError(f"编码规则 {rule_code} 不存在或未启用")
        
        # 获取或创建序号记录
        async with in_transaction():
            sequence = await CodeSequence.get_or_none(
                code_rule_id=rule.id,
                tenant_id=tenant_id
            )
            if not sequence:
                sequence = await CodeSequence.create(
                    code_rule_id=rule.id,
                    tenant_id=tenant_id,
                    current_seq=rule.seq_start - rule.seq_step
                )
            
            # 检查是否需要重置序号
            if rule.seq_reset_rule:
                now = date.today()
                if sequence.reset_date != now:
                    if rule.seq_reset_rule == "daily":
                        sequence.current_seq = rule.seq_start - rule.seq_step
                        sequence.reset_date = now
                    elif rule.seq_reset_rule == "monthly":
                        if not sequence.reset_date or sequence.reset_date.month != now.month:
                            sequence.current_seq = rule.seq_start - rule.seq_step
                            sequence.reset_date = now
                    elif rule.seq_reset_rule == "yearly":
                        if not sequence.reset_date or sequence.reset_date.year != now.year:
                            sequence.current_seq = rule.seq_start - rule.seq_step
                            sequence.reset_date = now
            
            # 递增序号
            sequence.current_seq += rule.seq_step
            await sequence.save()
        
        # 生成编码
        code = rule.expression
        now = datetime.now()
        
        # 替换日期变量
        code = code.replace("{YYYY}", now.strftime("%Y"))
        code = code.replace("{YY}", now.strftime("%y"))
        code = code.replace("{MM}", now.strftime("%m"))
        code = code.replace("{DD}", now.strftime("%d"))
        
        # 替换序号变量
        seq_pattern = r'\{SEQ(?::(\d+))?\}'
        def replace_seq(match):
            width = int(match.group(1)) if match.group(1) else 0
            seq_str = str(sequence.current_seq)
            if width > 0:
                seq_str = seq_str.zfill(width)
            return seq_str
        
        code = re.sub(seq_pattern, replace_seq, code)
        
        # 替换字典变量（需要数据字典支持）
        dict_pattern = r'\{DICT:([^}]+)\}'
        def replace_dict(match):
            dict_code = match.group(1)
            # 从数据字典获取值（需要实现）
            # dict_value = await DataDictionaryService.get_dict_value(tenant_id, dict_code)
            # return dict_value or ""
            return ""  # 待实现
        
        code = re.sub(dict_pattern, replace_dict, code)
        
        # 替换上下文变量
        if context:
            for key, value in context.items():
                code = code.replace(f"{{{key}}}", str(value))
        
        return code
    
    @staticmethod
    async def test_generate_code(
        tenant_id: int,
        rule_code: str,
        context: Optional[Dict] = None
    ) -> str:
        """
        测试生成编码（不更新序号）
        
        Args:
            tenant_id: 组织ID
            rule_code: 规则代码
            context: 上下文变量（可选）
            
        Returns:
            生成的编码（测试用）
        """
        # 类似 generate_code，但不更新序号
        # 使用当前序号值，不递增
        pass
```

---

## 🌐 API 设计

### 编码规则 API

```python
# riveredge-backend/src/tree-root/api/code_rules/code_rules.py
from fastapi import APIRouter, Depends, Query, Body
from typing import Optional, List, Dict
from uuid import UUID

from ....soil.api.deps import get_current_tenant
from ...schemas.code_rule import (
    CodeRuleCreate,
    CodeRuleUpdate,
    CodeRuleResponse
)
from ...services.code_rule_service import CodeRuleService
from ...services.code_generation_service import CodeGenerationService

router = APIRouter(prefix="/code-rules", tags=["编码规则"])


@router.post("", response_model=CodeRuleResponse, status_code=201)
async def create_rule(
    data: CodeRuleCreate,
    tenant_id: int = Depends(get_current_tenant)
):
    """创建编码规则"""
    rule = await CodeRuleService.create_rule(tenant_id, data)
    return rule


@router.get("", response_model=List[CodeRuleResponse])
async def list_rules(
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    is_active: Optional[bool] = Query(None),
    tenant_id: int = Depends(get_current_tenant)
):
    """获取编码规则列表"""
    rules = await CodeRuleService.list_rules(tenant_id, skip, limit, is_active)
    return rules


@router.post("/{rule_code}/generate", response_model=Dict[str, str])
async def generate_code(
    rule_code: str,
    context: Optional[Dict] = Body(None),
    tenant_id: int = Depends(get_current_tenant)
):
    """生成编码"""
    code = await CodeGenerationService.generate_code(tenant_id, rule_code, context)
    return {"code": code}


@router.post("/{rule_code}/test", response_model=Dict[str, str])
async def test_generate_code(
    rule_code: str,
    context: Optional[Dict] = Body(None),
    tenant_id: int = Depends(get_current_tenant)
):
    """测试生成编码（不更新序号）"""
    code = await CodeGenerationService.test_generate_code(tenant_id, rule_code, context)
    return {"code": code}
```

---

## 🎨 前端页面设计

**参考**：复用第一阶段的页面布局规范

**功能**：
- 编码规则列表（表格展示）
- 创建规则（Modal，表达式编辑器）
- 编辑规则（Modal）
- 测试生成（测试按钮，显示生成的编码）
- 表达式帮助（变量说明）

**页面路径**：`riveredge-frontend/src/tree-stem/pages/system/code-rules/list/index.tsx`

---

## 📚 相关文档

- [系统级功能建设计划.md](../系统级功能建设计划.md)
- [前端页面布局规范.md](../sys第一阶段/前端页面布局规范.md)

---

**最后更新**：2025-01-XX

