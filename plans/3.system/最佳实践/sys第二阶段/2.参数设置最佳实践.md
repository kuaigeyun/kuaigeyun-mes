# 参数设置最佳实践

## 📋 模块概述

**模块名称**：参数设置（System Parameter Management）  
**优先级**：⭐⭐⭐⭐（高优先级）  
**预计时间**：1 周  
**依赖**：无（独立功能）

**功能范围**：
- 系统参数管理（CRUD）
- 参数缓存（Redis）
- 参数批量更新
- 参数类型支持（string、number、boolean、json）

---

## 🔍 库选择评估

### 参数设置实现方案

**结论**：✅ **使用自定义实现 + redis-py（已使用）**

经过调研，参数设置功能相对简单，主要是 CRUD 和缓存：

#### 方案一：自定义实现 + redis-py ⭐⭐⭐⭐⭐（强烈推荐）

**优势**：
- ✅ 功能简单，实现成本低（1 周可完成）
- ✅ 与 Tortoise ORM 完美集成（原生支持）
- ✅ 多租户隔离天然支持（tenant_id 字段）
- ✅ 完全符合框架规范（命名、注释、代码风格）
- ✅ 完全控制，易于定制和扩展
- ✅ 已有 redis-py（已使用），无需额外依赖

**劣势**：
- ⚠️ 需要自己实现缓存逻辑（但逻辑简单）

**适用场景**：
- ✅ 标准的参数管理需求
- ✅ 需要与现有框架深度集成
- ✅ 需要完全符合项目规范
- ✅ 多租户是核心需求

**实现复杂度**：⭐（简单，1 周可完成）

---

#### 方案二：使用第三方库 ⭐⭐（不推荐）

**调研结果**：
- ❌ Tortoise ORM 生态中没有专门的参数管理库
- ❌ Django 生态有 `django-constance`，但不适用于 FastAPI + Tortoise ORM
- ❌ 其他通用库功能过于复杂，不符合简单需求

**结论**：参数设置功能简单，使用第三方库反而增加复杂度，不推荐。

---

### 最终推荐方案

**推荐**：✅ **使用自定义实现 + redis-py（已使用）**

**理由**：
1. ✅ **功能简单**，自定义实现成本低（1 周）
2. ✅ **已有 redis-py**，无需额外依赖
3. ✅ **多租户是核心需求**，自定义实现天然支持
4. ✅ **完全符合框架规范**，代码风格统一
5. ✅ **维护成本低**，团队完全掌控代码

**所需库**：
- **redis-py**（已使用，参数缓存）

---

## 🔍 技术要点

### Redis 缓存策略

**缓存键格式**：`system_parameter:{tenant_id}:{key}`

**缓存策略**：
- ✅ 参数读取时优先从 Redis 读取
- ✅ 参数更新时同步更新 Redis
- ✅ 参数删除时清除 Redis 缓存
- ✅ 支持缓存预热（启动时加载所有参数）

---

## 🗄️ 数据库设计

### 系统参数表（root_system_parameters）

```sql
CREATE TABLE root_system_parameters (
    id SERIAL PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,        -- 业务ID（UUID，对外暴露，安全且唯一）
    tenant_id INTEGER NOT NULL,
    
    -- 参数基本信息
    key VARCHAR(100) NOT NULL,               -- 参数键（唯一，用于程序识别）
    value TEXT NOT NULL,                      -- 参数值（JSON 字符串）
    type VARCHAR(20) NOT NULL,               -- 参数类型：string、number、boolean、json
    description TEXT,                         -- 参数描述
    is_system BOOLEAN DEFAULT FALSE,          -- 是否系统参数（系统参数不可删除）
    is_active BOOLEAN DEFAULT TRUE,          -- 是否启用
    
    -- 标准字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,
    
    -- 索引
    CONSTRAINT uk_root_system_parameters_tenant_key UNIQUE (tenant_id, key),
    INDEX idx_root_system_parameters_tenant_id (tenant_id),
    INDEX idx_root_system_parameters_uuid (uuid),
    INDEX idx_root_system_parameters_key (key),
    INDEX idx_root_system_parameters_created_at (created_at)
);
```

**设计要点**：
- ✅ `key` 用于程序识别，`tenant_id + key` 唯一约束
- ✅ `value` 存储为 TEXT，支持复杂 JSON 结构
- ✅ `type` 用于类型验证和前端展示
- ✅ `is_system` 标记系统参数，防止误删

---

## 🐍 后端模型设计

### 系统参数模型（SystemParameter）

```python
# riveredge-backend/src/tree-root/models/system_parameter.py
from tortoise.models import Model
from tortoise import fields
from .base import BaseModel
import json


class SystemParameter(BaseModel):
    """
    系统参数模型
    
    用于存储系统配置参数，支持多种数据类型。
    支持多组织隔离，每个组织可以定义自己的参数。
    支持 Redis 缓存，提升读取性能。
    
    注意：继承自 BaseModel，自动包含 uuid、tenant_id、created_at、updated_at 字段。
    """
    id = fields.IntField(pk=True, description="参数ID（主键，自增ID，内部使用）")
    
    key = fields.CharField(max_length=100, description="参数键（唯一，用于程序识别）")
    value = fields.TextField(description="参数值（JSON 字符串）")
    type = fields.CharField(max_length=20, description="参数类型：string、number、boolean、json")
    description = fields.TextField(null=True, description="参数描述")
    is_system = fields.BooleanField(default=False, description="是否系统参数")
    is_active = fields.BooleanField(default=True, description="是否启用")
    
    class Meta:
        table = "root_system_parameters"
        unique_together = [("tenant_id", "key")]
    
    def get_value(self):
        """获取解析后的参数值"""
        if self.type == "json":
            return json.loads(self.value)
        elif self.type == "number":
            return float(self.value) if "." in self.value else int(self.value)
        elif self.type == "boolean":
            return self.value.lower() in ("true", "1", "yes")
        else:
            return self.value
    
    def set_value(self, value):
        """设置参数值（自动序列化）"""
        if self.type == "json":
            self.value = json.dumps(value, ensure_ascii=False)
        else:
            self.value = str(value)
```

---

## 📝 Schema 设计

### 系统参数 Schema

```python
# riveredge-backend/src/tree-root/schemas/system_parameter.py
from pydantic import BaseModel, Field, ConfigDict, field_validator
from typing import Optional, Union, Any
from datetime import datetime
from uuid import UUID
import json


class SystemParameterBase(BaseModel):
    """系统参数基础 Schema"""
    key: str = Field(..., max_length=100, description="参数键")
    value: Union[str, int, float, bool, dict, list] = Field(..., description="参数值")
    type: str = Field(..., description="参数类型：string、number、boolean、json")
    description: Optional[str] = Field(None, description="参数描述")
    is_system: bool = Field(False, description="是否系统参数")
    is_active: bool = Field(True, description="是否启用")
    
    @field_validator("type")
    @classmethod
    def validate_type(cls, v):
        if v not in ("string", "number", "boolean", "json"):
            raise ValueError("参数类型必须是 string、number、boolean 或 json")
        return v


class SystemParameterCreate(SystemParameterBase):
    """创建系统参数 Schema"""
    pass


class SystemParameterUpdate(BaseModel):
    """更新系统参数 Schema"""
    value: Optional[Union[str, int, float, bool, dict, list]] = Field(None, description="参数值")
    description: Optional[str] = Field(None, description="参数描述")
    is_active: Optional[bool] = Field(None, description="是否启用")


class SystemParameterResponse(BaseModel):
    """系统参数响应 Schema"""
    uuid: UUID = Field(..., description="参数UUID")
    tenant_id: int = Field(..., description="组织ID")
    key: str = Field(..., description="参数键")
    value: Union[str, int, float, bool, dict, list] = Field(..., description="参数值")
    type: str = Field(..., description="参数类型")
    description: Optional[str] = Field(None, description="参数描述")
    is_system: bool = Field(..., description="是否系统参数")
    is_active: bool = Field(..., description="是否启用")
    created_at: datetime = Field(..., description="创建时间")
    updated_at: datetime = Field(..., description="更新时间")
    
    model_config = ConfigDict(from_attributes=True)
```

---

## 🔧 Service 层设计

### 系统参数服务（SystemParameterService）

```python
# riveredge-backend/src/tree-root/services/system_parameter_service.py
from typing import Optional, List, Dict, Any
from uuid import UUID
from tortoise.exceptions import DoesNotExist, IntegrityError
from fastapi import HTTPException, status
import json
import redis.asyncio as redis

from ..models.system_parameter import SystemParameter
from ..schemas.system_parameter import SystemParameterCreate, SystemParameterUpdate
from ....soil.core.config import settings

# Redis 客户端（需要配置）
redis_client = redis.from_url(settings.REDIS_URL)


class SystemParameterService:
    """系统参数服务"""
    
    @staticmethod
    def _get_cache_key(tenant_id: int, key: str) -> str:
        """获取缓存键"""
        return f"system_parameter:{tenant_id}:{key}"
    
    @staticmethod
    async def _get_from_cache(tenant_id: int, key: str) -> Optional[Any]:
        """从缓存获取参数"""
        cache_key = SystemParameterService._get_cache_key(tenant_id, key)
        value = await redis_client.get(cache_key)
        if value:
            return json.loads(value)
        return None
    
    @staticmethod
    async def _set_to_cache(tenant_id: int, key: str, value: Any) -> None:
        """设置缓存"""
        cache_key = SystemParameterService._get_cache_key(tenant_id, key)
        await redis_client.set(cache_key, json.dumps(value, ensure_ascii=False), ex=3600)
    
    @staticmethod
    async def _delete_from_cache(tenant_id: int, key: str) -> None:
        """删除缓存"""
        cache_key = SystemParameterService._get_cache_key(tenant_id, key)
        await redis_client.delete(cache_key)
    
    @staticmethod
    async def get_parameter(
        tenant_id: int,
        key: str,
        use_cache: bool = True
    ) -> Optional[SystemParameter]:
        """
        获取参数（优先从缓存读取）
        
        Args:
            tenant_id: 组织ID
            key: 参数键
            use_cache: 是否使用缓存
            
        Returns:
            参数对象，如果不存在返回 None
        """
        if use_cache:
            cached_value = await SystemParameterService._get_from_cache(tenant_id, key)
            if cached_value:
                # 从缓存构建参数对象（简化版）
                parameter = await SystemParameter.get_or_none(
                    tenant_id=tenant_id,
                    key=key,
                    deleted_at__isnull=True
                )
                if parameter:
                    return parameter
        
        parameter = await SystemParameter.get_or_none(
            tenant_id=tenant_id,
            key=key,
            deleted_at__isnull=True,
            is_active=True
        )
        
        if parameter and use_cache:
            await SystemParameterService._set_to_cache(
                tenant_id, key, parameter.get_value()
            )
        
        return parameter
    
    @staticmethod
    async def create_parameter(
        tenant_id: int,
        data: SystemParameterCreate
    ) -> SystemParameter:
        """创建参数"""
        try:
            parameter = SystemParameter(
                tenant_id=tenant_id,
                key=data.key,
                type=data.type,
                description=data.description,
                is_system=data.is_system,
                is_active=data.is_active
            )
            parameter.set_value(data.value)
            await parameter.save()
            
            # 写入缓存
            await SystemParameterService._set_to_cache(
                tenant_id, data.key, parameter.get_value()
            )
            
            return parameter
        except IntegrityError:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail=f"参数键 {data.key} 已存在"
            )
    
    @staticmethod
    async def update_parameter(
        tenant_id: int,
        uuid: UUID,
        data: SystemParameterUpdate
    ) -> SystemParameter:
        """更新参数"""
        parameter = await SystemParameter.get_or_none(
            tenant_id=tenant_id,
            uuid=uuid,
            deleted_at__isnull=True
        )
        if not parameter:
            raise HTTPException(
                status_code=status.HTTP_404_NOT_FOUND,
                detail="参数不存在"
            )
        
        if data.value is not None:
            parameter.set_value(data.value)
        if data.description is not None:
            parameter.description = data.description
        if data.is_active is not None:
            parameter.is_active = data.is_active
        
        await parameter.save()
        
        # 更新缓存
        await SystemParameterService._set_to_cache(
            tenant_id, parameter.key, parameter.get_value()
        )
        
        return parameter
    
    @staticmethod
    async def batch_update_parameters(
        tenant_id: int,
        updates: Dict[str, Any]
    ) -> List[SystemParameter]:
        """批量更新参数"""
        parameters = []
        for key, value in updates.items():
            parameter = await SystemParameterService.get_parameter(
                tenant_id, key, use_cache=False
            )
            if parameter:
                parameter.set_value(value)
                await parameter.save()
                parameters.append(parameter)
                
                # 更新缓存
                await SystemParameterService._set_to_cache(
                    tenant_id, key, parameter.get_value()
                )
        
        return parameters
```

---

## 🌐 API 设计

### 系统参数 API

```python
# riveredge-backend/src/tree-root/api/system_parameters/system_parameters.py
from fastapi import APIRouter, Depends, Query, Body
from typing import Optional, List, Dict, Any
from uuid import UUID

from ....soil.api.deps import get_current_tenant
from ...schemas.system_parameter import (
    SystemParameterCreate,
    SystemParameterUpdate,
    SystemParameterResponse
)
from ...services.system_parameter_service import SystemParameterService

router = APIRouter(prefix="/system-parameters", tags=["系统参数"])


@router.post("", response_model=SystemParameterResponse, status_code=201)
async def create_parameter(
    data: SystemParameterCreate,
    tenant_id: int = Depends(get_current_tenant)
):
    """创建系统参数"""
    parameter = await SystemParameterService.create_parameter(tenant_id, data)
    return parameter


@router.get("", response_model=List[SystemParameterResponse])
async def list_parameters(
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    is_active: Optional[bool] = Query(None),
    tenant_id: int = Depends(get_current_tenant)
):
    """获取参数列表"""
    # 实现列表查询逻辑
    pass


@router.get("/{uuid}", response_model=SystemParameterResponse)
async def get_parameter(
    uuid: UUID,
    tenant_id: int = Depends(get_current_tenant)
):
    """获取参数详情"""
    # 实现详情查询逻辑
    pass


@router.get("/key/{key}", response_model=SystemParameterResponse)
async def get_parameter_by_key(
    key: str,
    tenant_id: int = Depends(get_current_tenant)
):
    """根据键获取参数（优先从缓存读取）"""
    parameter = await SystemParameterService.get_parameter(tenant_id, key)
    if not parameter:
        raise HTTPException(status_code=404, detail="参数不存在")
    return parameter


@router.put("/{uuid}", response_model=SystemParameterResponse)
async def update_parameter(
    uuid: UUID,
    data: SystemParameterUpdate,
    tenant_id: int = Depends(get_current_tenant)
):
    """更新参数"""
    parameter = await SystemParameterService.update_parameter(tenant_id, uuid, data)
    return parameter


@router.post("/batch-update", response_model=List[SystemParameterResponse])
async def batch_update_parameters(
    updates: Dict[str, Any] = Body(...),
    tenant_id: int = Depends(get_current_tenant)
):
    """批量更新参数"""
    parameters = await SystemParameterService.batch_update_parameters(
        tenant_id, updates
    )
    return parameters
```

---

## 🎨 前端页面设计

**参考**：复用第一阶段的页面布局规范

**功能**：
- 参数列表（表格展示）
- 创建参数（Modal，支持类型选择）
- 编辑参数（Modal）
- 批量更新（批量编辑功能）
- 参数值预览（根据类型格式化显示）

**页面路径**：`riveredge-frontend/src/tree-stem/pages/system/system-parameters/list/index.tsx`

---

## 📚 相关文档

- [系统级功能建设计划.md](../系统级功能建设计划.md)
- [前端页面布局规范.md](../sys第一阶段/前端页面布局规范.md)

---

**最后更新**：2025-01-XX

