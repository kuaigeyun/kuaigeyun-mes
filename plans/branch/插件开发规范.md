# RiverEdge SaaS 多组织框架 - 插件开发规范

## 📋 概述

本文档定义 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的插件开发规范，包括插件结构、开发流程、API 规范等。

## 插件结构规范

### 标准目录结构

插件采用前后端分离但统一管理的结构，前后端代码放在同一个插件目录下：

```
seed-example/
├── frontend/                 # 前端代码
│   ├── src/
│   │   ├── index.ts         # 前端插件入口文件
│   │   ├── routes.ts        # 前端路由定义
│   │   ├── components/      # 前端组件目录
│   │   │   ├── ExamplePage.tsx
│   │   │   └── ExampleForm.tsx
│   │   ├── services/        # 前端服务层
│   │   │   └── api.ts
│   │   ├── types/           # 前端类型定义
│   │   │   └── index.ts
│   │   └── utils/           # 前端工具函数
│   │       └── index.ts
│   ├── public/              # 前端静态资源
│   ├── package.json         # 前端依赖配置
│   ├── tsconfig.json        # TypeScript 配置
│   └── vite.config.ts       # 前端构建配置
├── backend/                 # 后端代码
│   ├── src/
│   │   ├── __init__.py
│   │   ├── plugin.py        # 后端插件入口
│   │   ├── api/
│   │   │   └── routes.py    # API 路由
│   │   ├── models/
│   │   │   └── example.py   # 数据模型（Tortoise ORM）
│   │   ├── schemas/
│   │   │   └── example.py   # Pydantic Schema
│   │   └── services/
│   │       └── example.py   # 业务逻辑
│   └── requirements.txt     # Python 依赖
├── shared/                  # 前后端共享代码（可选）
│   └── types/              # 共享类型定义
│       └── index.ts
├── db/                     # 数据库迁移脚本（可选）
│   └── migration/
│       └── V1__create_example_table.sql
├── manifest.json           # 插件清单（必需）
├── package.json            # 插件根配置（可选，用于统一管理）
└── README.md               # 插件文档
```

### 目录说明

- **frontend/**: 前端代码目录，包含所有前端相关的代码和配置
- **backend/**: 后端代码目录，包含所有后端相关的代码和配置
- **shared/**: 前后端共享代码（如类型定义），可选
- **db/**: 数据库迁移脚本，可选
- **manifest.json**: 插件清单文件，定义插件的前后端配置
- **README.md**: 插件使用文档

## manifest.json 规范

### 完整示例

```json
{
  "id": "seed-example",
  "name": "示例应用",
  "version": "1.0.0",
  "description": "这是一个示例插件",
  "author": "Your Name",
  "icon": "./frontend/public/icon.png",
  "frontend": {
    "enabled": true,
    "entry": "./frontend/dist/index.js",
    "routes": [
      {
        "path": "/example",
        "name": "示例页面",
        "component": "ExamplePage",
        "icon": "AppstoreOutlined",
        "order": 1,
        "permissions": ["example:read"]
      },
      {
        "path": "/example/detail/:id",
        "name": "详情页",
        "component": "ExampleDetail",
        "hidden": true
      }
    ],
    "menu": {
      "title": "示例模块",
      "icon": "AppstoreOutlined",
      "order": 10
    }
  },
  "backend": {
    "enabled": true,
    "basePath": "/api/example",
    "pluginClass": "ExamplePlugin",
    "routes": [
      {
        "path": "/list",
        "method": "GET",
        "handler": "list_examples",
        "permissions": ["example:read"]
      },
      {
        "path": "/{id}",
        "method": "GET",
        "handler": "get_example_detail",
        "permissions": ["example:read"]
      },
      {
        "path": "/create",
        "method": "POST",
        "handler": "create_example",
        "permissions": ["example:write"]
      },
      {
        "path": "/{id}",
        "method": "PUT",
        "handler": "update_example",
        "permissions": ["example:write"]
      },
      {
        "path": "/{id}",
        "method": "DELETE",
        "handler": "delete_example",
        "permissions": ["example:delete"]
      }
    ],
    "entities": [
      "ExampleModel"
    ],
    "migrations": [
      "001_initial.py"
    ]
  },
  "permissions": [
    "example:read",
    "example:write",
    "example:delete"
  ],
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "peerDependencies": {
    "@riveredge/shell": "^1.0.0"
  },
  "config": {
    "apiBaseUrl": "/api/example"
  }
}
```

### 字段说明

#### 基础字段

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| id | string | ✅ | 插件唯一标识，使用 kebab-case |
| name | string | ✅ | 插件显示名称 |
| version | string | ✅ | 语义化版本号 |
| description | string | ❌ | 插件描述 |
| author | string | ❌ | 作者信息 |
| icon | string | ❌ | 插件图标路径 |
| permissions | array | ❌ | 插件所需权限列表 |
| dependencies | object | ❌ | 前端插件依赖 |
| peerDependencies | object | ❌ | 共享依赖 |
| config | object | ❌ | 插件配置项 |

#### 前端配置 (frontend)

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| frontend.enabled | boolean | ✅ | 是否启用前端 |
| frontend.entry | string | ✅ | 前端插件入口文件路径 |
| frontend.routes | array | ✅ | 前端路由配置数组 |
| frontend.menu | object | ❌ | 前端菜单配置 |

#### 后端配置 (backend)

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| backend.enabled | boolean | ✅ | 是否启用后端 |
| backend.basePath | string | ✅ | 后端 API 基础路径 |
| backend.pluginClass | string | ✅ | 后端插件类全限定名 |
| backend.routes | array | ✅ | 后端路由配置数组 |
| backend.entities | array | ❌ | 数据实体配置 |
| backend.migrations | array | ❌ | 数据库迁移脚本路径 |

### 路由配置

```typescript
interface RouteConfig {
  path: string;              // 路由路径
  name: string;              // 路由名称
  component: string;         // 组件名称
  icon?: string;             // 图标（Ant Design Icon）
  order?: number;            // 排序
  hidden?: boolean;          // 是否在菜单中隐藏
  permissions?: string[];     // 路由所需权限
  children?: RouteConfig[];   // 子路由
}
```

## 插件入口文件规范

### React 插件示例

```typescript
// src/index.ts
import { PluginRegistry } from '@riveredge/shell';
import { ExamplePage } from './components/ExamplePage';
import { ExampleDetail } from './components/ExampleDetail';
import routes from './routes';

// 插件注册函数
export default function registerPlugin(registry: PluginRegistry) {
  // 注册路由
  registry.registerRoutes(routes);
  
  // 注册组件
  registry.registerComponent('ExamplePage', ExamplePage);
  registry.registerComponent('ExampleDetail', ExampleDetail);
  
  // 注册服务
  registry.registerService('exampleApi', {
    getList: () => fetch('/api/example/list'),
    getDetail: (id: string) => fetch(`/api/example/${id}`),
  });
  
  // 注册菜单项
  registry.registerMenu({
    key: 'example',
    label: '示例模块',
    icon: 'AppstoreOutlined',
    children: [
      {
        key: 'example-list',
        label: '示例列表',
        path: '/example',
      },
    ],
  });
  
  // 生命周期钩子
  return {
    onMount: () => {
      console.log('Example plugin mounted');
    },
    onUnmount: () => {
      console.log('Example plugin unmounted');
    },
  };
}
```


## 组件开发规范

### 使用 Shell 提供的组件和工具

```typescript
// src/components/ExamplePage.tsx
import React from 'react';
import { useShell } from '@riveredge/shell';
import { Button, Table } from 'antd';

export const ExamplePage: React.FC = () => {
  const { api, message, navigate } = useShell();
  
  const handleClick = async () => {
    try {
      const data = await api.get('/example/list');
      message.success('加载成功');
    } catch (error) {
      message.error('加载失败');
    }
  };
  
  return (
    <div>
      <Button onClick={handleClick}>加载数据</Button>
      <Table dataSource={[]} columns={[]} />
    </div>
  );
};
```

### 使用 Shell 的状态管理

```typescript
import { useShellStore } from '@riveredge/shell';

export const ExampleComponent: React.FC = () => {
  const { user, permissions } = useShellStore();
  
  // 检查权限
  if (!permissions.includes('example:read')) {
    return <div>无权限访问</div>;
  }
  
  return <div>Hello, {user.name}</div>;
};
```

## API 调用规范

### 统一使用 Shell 的 API 客户端

```typescript
// ✅ 正确：使用 Shell 提供的 API 客户端
import { useShell } from '@riveredge/shell';

const { api } = useShell();
const data = await api.get('/api/example/list');

// ❌ 错误：直接使用 fetch 或 axios
const data = await fetch('/api/example/list');
```

### API 路径规范

- 使用相对路径，Shell 会自动添加 base URL
- 路径格式：`/api/{module}/{resource}`
- 示例：`/api/example/users`, `/api/example/users/:id`

## 样式规范

### 使用 Shell 的主题系统

```typescript
import { useTheme } from '@riveredge/shell';

export const ExampleComponent: React.FC = () => {
  const theme = useTheme();
  
  return (
    <div style={{ color: theme.primaryColor }}>
      Themed Component
    </div>
  );
};
```

### CSS 命名规范

- 使用 CSS Modules 或 styled-components
- 类名使用 BEM 命名规范
- 避免全局样式污染

```typescript
// ✅ 使用 CSS Modules
import styles from './ExamplePage.module.css';

<div className={styles.container}>
  <div className={styles.header}>Header</div>
</div>

// ✅ 使用 styled-components
import styled from 'styled-components';

const Container = styled.div`
  padding: 16px;
`;
```

## 类型定义规范

### 导出类型定义

```typescript
// src/types/index.ts
export interface ExampleItem {
  id: string;
  name: string;
  description?: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface ExampleListResponse {
  items: ExampleItem[];
  total: number;
  page: number;
  pageSize: number;
}
```

## 测试规范

### 单元测试

```typescript
// src/components/__tests__/ExamplePage.test.tsx
import { render, screen } from '@testing-library/react';
import { ExamplePage } from '../ExamplePage';

describe('ExamplePage', () => {
  it('should render correctly', () => {
    render(<ExamplePage />);
    expect(screen.getByText('示例页面')).toBeInTheDocument();
  });
});
```

## 构建配置

### Vite 配置示例

```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  build: {
    lib: {
      entry: './src/index.ts',
      name: 'SeedExample',
      fileName: 'index',
      formats: ['es'],
    },
    rollupOptions: {
      external: ['react', 'react-dom', '@riveredge/shell'],
    },
  },
});
```

## 版本管理

### 语义化版本

- **主版本号**: 不兼容的 API 修改
- **次版本号**: 向下兼容的功能性新增
- **修订号**: 向下兼容的问题修正

示例：`1.2.3`

### 版本兼容性

- 插件版本需与 Shell 版本兼容
- 在 `peerDependencies` 中声明 Shell 版本要求
- 使用版本范围：`^1.0.0` 表示兼容 1.x.x

## 发布流程

1. **开发**: 在 `riveredge-seed/seed-xxx` 目录开发
2. **测试**: 本地测试插件加载和功能
3. **构建**: 执行构建命令生成 dist
4. **打包**: 打包插件文件
5. **注册**: 在后端注册插件信息
6. **部署**: 部署插件静态资源
7. **启用**: 在 Shell 中启用插件

## 最佳实践

1. **保持插件独立**: 插件不应依赖其他插件
2. **使用 Shell API**: 通过 Shell 提供的 API 进行交互
3. **权限检查**: 在组件中检查用户权限
4. **错误处理**: 妥善处理 API 错误和异常
5. **性能优化**: 使用懒加载、代码分割
6. **类型安全**: 使用 TypeScript 确保类型安全
7. **文档完善**: 提供清晰的 README 和使用文档

