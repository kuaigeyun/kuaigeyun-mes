# RiverEdge SaaS 多组织框架 - 表单自定义字段实现方案

## 📋 需求概述

在 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 中实现表单自定义字段功能，允许用户根据业务需求动态配置表单字段，包括字段类型、验证规则、显示顺序等。

## 🎯 技术栈基础

- **前端**: React 19.2 + Umi V4 + Ant Design v6 + Pro Components
- **后端**: Python 3.13 + FastAPI + Pydantic v2 + Tortoise ORM

## 🔍 方案对比

### 方案一：Ant Design 官方原生方案（推荐基础场景）

#### 优势
- ✅ 无需引入额外依赖
- ✅ 与现有技术栈完美集成
- ✅ 官方维护，稳定可靠
- ✅ 学习成本低

#### 适用场景
- 简单的动态字段添加/删除
- 字段类型相对固定
- 不需要可视化表单设计器

#### 实现方式

**1. 使用 Form.List 动态添加字段**

```tsx
import { ProForm, ProFormText, ProFormSelect } from '@ant-design/pro-components';
import { Button, Space } from 'antd';

export default function DynamicForm() {
  const [form] = ProForm.useForm();
  
  return (
    <ProForm
      form={form}
      onFinish={async (values) => {
        console.log('表单值:', values);
      }}
    >
      {/* 固定字段 */}
      <ProFormText name="title" label="标题" rules={[{ required: true }]} />
      
      {/* 自定义字段列表 */}
      <ProForm.Item label="自定义字段">
        <ProForm.List name="customFields">
          {(fields, { add, remove }) => (
            <>
              {fields.map(({ key, name, ...restField }) => (
                <Space key={key} style={{ display: 'flex', marginBottom: 8 }} align="baseline">
                  <ProFormText
                    {...restField}
                    name={[name, 'fieldName']}
                    placeholder="字段名称"
                    rules={[{ required: true, message: '请输入字段名称' }]}
                  />
                  <ProFormSelect
                    {...restField}
                    name={[name, 'fieldType']}
                    placeholder="字段类型"
                    options={[
                      { label: '文本', value: 'text' },
                      { label: '数字', value: 'number' },
                      { label: '日期', value: 'date' },
                    ]}
                    rules={[{ required: true }]}
                  />
                  <ProFormText
                    {...restField}
                    name={[name, 'fieldValue']}
                    placeholder="字段值"
                  />
                  <Button type="link" danger onClick={() => remove(name)}>
                    删除
                  </Button>
                </Space>
              ))}
              <Button type="dashed" onClick={() => add()} block>
                + 添加自定义字段
              </Button>
            </>
          )}
        </ProForm.List>
      </ProForm.Item>
    </ProForm>
  );
}
```

**2. 基于配置的动态表单渲染**

```tsx
import { ProForm, ProFormText, ProFormDigit, ProFormDatePicker } from '@ant-design/pro-components';
import { useMemo } from 'react';

interface FieldConfig {
  name: string;
  label: string;
  type: 'text' | 'number' | 'date';
  required?: boolean;
  placeholder?: string;
}

export default function ConfigurableForm({ fieldConfigs }: { fieldConfigs: FieldConfig[] }) {
  const renderField = (config: FieldConfig) => {
    const commonProps = {
      name: config.name,
      label: config.label,
      rules: config.required ? [{ required: true }] : [],
      placeholder: config.placeholder,
    };

    switch (config.type) {
      case 'text':
        return <ProFormText key={config.name} {...commonProps} />;
      case 'number':
        return <ProFormDigit key={config.name} {...commonProps} />;
      case 'date':
        return <ProFormDatePicker key={config.name} {...commonProps} />;
      default:
        return null;
    }
  };

  return (
    <ProForm onFinish={async (values) => console.log(values)}>
      {fieldConfigs.map(renderField)}
    </ProForm>
  );
}

// 使用示例
const configs: FieldConfig[] = [
  { name: 'name', label: '姓名', type: 'text', required: true },
  { name: 'age', label: '年龄', type: 'number' },
  { name: 'birthday', label: '生日', type: 'date' },
];

<ConfigurableForm fieldConfigs={configs} />
```

**3. 使用 useWatch 实现字段联动**

```tsx
import { ProForm, ProFormSelect, ProFormText } from '@ant-design/pro-components';
import { Form } from 'antd';

export default function LinkedForm() {
  const [form] = Form.useForm();
  const fieldType = Form.useWatch('fieldType', form);

  return (
    <ProForm
      form={form}
      onFinish={async (values) => console.log(values)}
    >
      <ProFormSelect
        name="fieldType"
        label="字段类型"
        options={[
          { label: '文本', value: 'text' },
          { label: '数字', value: 'number' },
          { label: '邮箱', value: 'email' },
        ]}
      />
      
      {fieldType === 'text' && (
        <ProFormText
          name="textValue"
          label="文本值"
          rules={[{ required: true }]}
        />
      )}
      
      {fieldType === 'number' && (
        <ProFormDigit
          name="numberValue"
          label="数字值"
          rules={[{ required: true }]}
        />
      )}
      
      {fieldType === 'email' && (
        <ProFormText
          name="emailValue"
          label="邮箱"
          rules={[
            { required: true },
            { type: 'email', message: '请输入有效的邮箱地址' },
          ]}
        />
      )}
    </ProForm>
  );
}
```

### 方案二：Formily（阿里系，推荐复杂场景）

**Formily** 是阿里巴巴开源的企业级表单解决方案，功能强大，适合复杂的表单自定义场景。

#### 优势
- ✅ 阿里系，与 Ant Design 生态一致
- ✅ 功能强大，支持复杂表单场景
- ✅ 提供可视化表单设计器
- ✅ 支持字段联动、条件渲染
- ✅ 完善的文档和社区支持

#### 适用场景
- 复杂的表单自定义需求
- 需要可视化表单设计器
- 大量字段联动和条件渲染
- 企业级表单应用

#### 核心特性

1. **表单设计器** - 可视化拖拽设计表单
2. **动态渲染** - 基于 JSON Schema 动态渲染表单
3. **字段联动** - 强大的字段联动机制
4. **验证规则** - 灵活的验证规则配置
5. **性能优化** - 按需渲染，性能优异

#### 安装和使用

```bash
# 安装 Formily
npm install @formily/core @formily/react @formily/antd-v5
```

**基础使用示例**:

```tsx
import { createForm } from '@formily/core';
import { FormProvider, FormConsumer, Field } from '@formily/react';
import { FormItem, Input, Select } from '@formily/antd-v5';

const form = createForm({
  initialValues: {},
});

export default function FormilyForm() {
  return (
    <FormProvider form={form}>
      <Field
        name="name"
        title="姓名"
        required
        component={[Input, { placeholder: '请输入姓名' }]}
      />
      <Field
        name="type"
        title="类型"
        component={[Select, {
          options: [
            { label: '类型A', value: 'a' },
            { label: '类型B', value: 'b' },
          ],
        }]}
      />
      <FormConsumer>
        {() => (
          <div>
            <pre>{JSON.stringify(form.values, null, 2)}</pre>
          </div>
        )}
      </FormConsumer>
    </FormProvider>
  );
}
```

**基于 Schema 的动态表单**:

```tsx
import { createForm } from '@formily/core';
import { FormProvider, createSchemaField } from '@formily/react';
import { FormItem, Input, Select, DatePicker } from '@formily/antd-v5';

const SchemaField = createSchemaField({
  components: {
    FormItem,
    Input,
    Select,
    DatePicker,
  },
});

const form = createForm();

const schema = {
  type: 'object',
  properties: {
    name: {
      type: 'string',
      title: '姓名',
      required: true,
      'x-component': 'Input',
      'x-decorator': 'FormItem',
    },
    type: {
      type: 'string',
      title: '类型',
      'x-component': 'Select',
      'x-decorator': 'FormItem',
      'x-component-props': {
        options: [
          { label: '类型A', value: 'a' },
          { label: '类型B', value: 'b' },
        ],
      },
    },
    date: {
      type: 'string',
      title: '日期',
      'x-component': 'DatePicker',
      'x-decorator': 'FormItem',
    },
  },
};

export default function SchemaForm() {
  return (
    <FormProvider form={form}>
      <SchemaField schema={schema} />
    </FormProvider>
  );
}
```

#### 表单设计器

Formily 提供可视化表单设计器，可以通过拖拽方式设计表单：

```bash
# 安装表单设计器
npm install @formily/form-designer
```

### 方案三：Form Render（阿里系，推荐快速开发）

**Form Render** 是阿里巴巴开源的基于 JSON Schema 的表单渲染引擎，简单易用。

#### 优势
- ✅ 阿里系，与 Ant Design 集成良好
- ✅ 基于 JSON Schema，配置简单
- ✅ 开箱即用，学习成本低
- ✅ 支持 Ant Design 所有组件

#### 适用场景
- 需要快速实现动态表单
- 基于配置的表单生成
- 不需要复杂的表单设计器

#### 安装和使用

```bash
# 安装 Form Render
npm install form-render
```

**基础使用示例**:

```tsx
import FormRender from 'form-render';
import { Button } from 'antd';

export default function FormRenderExample() {
  const schema = {
    type: 'object',
    properties: {
      name: {
        title: '姓名',
        type: 'string',
        required: true,
      },
      age: {
        title: '年龄',
        type: 'number',
        min: 0,
        max: 150,
      },
      email: {
        title: '邮箱',
        type: 'string',
        format: 'email',
      },
      department: {
        title: '部门',
        type: 'string',
        enum: ['生产部', '质量部', '设备部'],
        enumNames: ['生产部', '质量部', '设备部'],
      },
    },
  };

  const onFinish = (formData) => {
    console.log('表单数据:', formData);
  };

  return (
    <FormRender
      schema={schema}
      onFinish={onFinish}
      footer={
        <Button type="primary" htmlType="submit">
          提交
        </Button>
      }
    />
  );
}
```

**动态 Schema 示例**:

```tsx
import { useState, useEffect } from 'react';
import FormRender from 'form-render';
import { useQuery } from '@tanstack/react-query';

export default function DynamicFormRender() {
  const [schema, setSchema] = useState({ type: 'object', properties: {} });

  useEffect(() => {
    // 从后端获取表单配置
    request('/api/form-config').then((config) => {
      setSchema(config.schema);
    });
  }, []);

  return (
    <FormRender
      schema={schema}
      onFinish={async (formData) => {
        await request('/api/submit', {
          method: 'POST',
          data: formData,
        });
      }}
    />
  );
}
```

### 方案四：ant-customize-form（第三方，轻量级）

**ant-customize-form** 是基于 Ant Design 的动态表单库，轻量级，易于使用。

#### 优势
- ✅ 轻量级，体积小
- ✅ 基于 Ant Design，集成简单
- ✅ API 简洁，学习成本低

#### 适用场景
- 简单的动态表单需求
- 不需要复杂的表单设计器
- 快速开发场景

#### 安装和使用

```bash
npm install ant-customize-form
```

**使用示例**:

```tsx
import { CustomizeForm, ItemTypes } from 'ant-customize-form';
import { Form } from 'antd';

export default function CustomizeFormExample() {
  const [form] = Form.useForm();

  const formItemConfig = [
    {
      itemProps: {
        name: 'name',
        label: '姓名',
        rules: [{ required: true, message: '请输入姓名' }],
      },
      type: ItemTypes.INPUT,
      typeProps: {
        placeholder: '请输入姓名',
        maxLength: 50,
      },
    },
    {
      itemProps: {
        name: 'age',
        label: '年龄',
        rules: [{ required: true }],
      },
      type: ItemTypes.INPUT_NUMBER,
      typeProps: {
        min: 0,
        max: 150,
      },
    },
    {
      itemProps: {
        name: 'department',
        label: '部门',
      },
      type: ItemTypes.SELECT,
      typeProps: {
        options: [
          { label: '生产部', value: 'production' },
          { label: '质量部', value: 'quality' },
        ],
      },
    },
  ];

  return (
    <CustomizeForm
      form={form}
      formItemConfig={formItemConfig}
      onFinish={(values) => {
        console.log('表单值:', values);
      }}
    />
  );
}
```

## 🏗️ 后端实现方案

### FastAPI + Pydantic 动态 Schema

#### 方案一：使用 Pydantic 动态模型

```python
from pydantic import BaseModel, create_model
from typing import Any, Dict, List
from fastapi import FastAPI, HTTPException

app = FastAPI()

# 字段配置模型
class FieldConfig(BaseModel):
    name: str
    type: str  # 'string', 'integer', 'float', 'date', 'boolean'
    required: bool = False
    default: Any = None
    validation: Dict[str, Any] = {}

# 动态创建 Pydantic 模型
def create_dynamic_model(fields: List[FieldConfig]):
    field_definitions = {}
    
    for field in fields:
        # 根据类型确定 Python 类型
        python_type = {
            'string': str,
            'integer': int,
            'float': float,
            'date': str,  # 或使用 datetime
            'boolean': bool,
        }.get(field.type, str)
        
        # 如果字段不是必需的，使用 Optional
        if not field.required:
            from typing import Optional
            python_type = Optional[python_type]
        
        field_definitions[field.name] = (python_type, field.default)
    
    return create_model('DynamicForm', **field_definitions)

# API 端点
@app.post("/api/dynamic-form/submit")
async def submit_dynamic_form(
    form_config: List[FieldConfig],
    form_data: Dict[str, Any]
):
    # 创建动态模型
    DynamicModel = create_dynamic_model(form_config)
    
    try:
        # 验证数据
        validated_data = DynamicModel(**form_data)
        return {"success": True, "data": validated_data.dict()}
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e))
```

#### 方案二：使用 JSON Schema 验证

```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel, ValidationError
import jsonschema

app = FastAPI()

class FormConfig(BaseModel):
    schema: dict  # JSON Schema

@app.post("/api/dynamic-form/validate")
async def validate_form_data(
    form_config: FormConfig,
    form_data: dict
):
    try:
        # 使用 JSON Schema 验证
        jsonschema.validate(instance=form_data, schema=form_config.schema)
        return {"success": True, "data": form_data}
    except jsonschema.ValidationError as e:
        raise HTTPException(status_code=400, detail=str(e))
```

#### 方案三：数据库存储字段配置

```python
from tortoise.models import Model
from tortoise import fields

class FormTemplate(Model):
    id = fields.IntField(pk=True)
    name = fields.CharField(max_length=100)
    description = fields.TextField(null=True)
    field_configs = fields.JSONField()  # 存储字段配置
    created_at = fields.DatetimeField(auto_now_add=True)
    updated_at = fields.DatetimeField(auto_now=True)

class FormData(Model):
    id = fields.IntField(pk=True)
    template_id = fields.IntField()
    data = fields.JSONField()  # 存储表单数据
    created_at = fields.DatetimeField(auto_now_add=True)
```

## 📊 方案对比总结

| 方案 | 复杂度 | 功能丰富度 | 学习成本 | 推荐场景 |
|------|--------|-----------|---------|---------|
| **Ant Design 原生** | ⭐⭐ | ⭐⭐⭐ | ⭐ | 简单动态字段 |
| **Formily** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 复杂表单、表单设计器 |
| **Form Render** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | 基于 Schema 的快速开发 |
| **ant-customize-form** | ⭐⭐ | ⭐⭐⭐ | ⭐ | 轻量级动态表单 |

## 🎯 最终推荐方案

**使用 Formily（最终技术选型）**

Formily 是 **RiverEdge SaaS 多组织框架**的最终技术选型，适用于所有表单自定义字段场景：

- ✅ 提供可视化表单设计器
- ✅ 支持复杂的字段联动和条件渲染
- ✅ 支持动态字段配置
- ✅ 企业级表单应用的最佳选择
- ✅ 与 Ant Design 完美集成
- ✅ 类型安全，开发体验好

## 🚀 实施建议

### 第一阶段：基础功能（使用 Formily）

1. 安装和配置 Formily
2. 实现字段配置管理（后端存储）
3. 实现动态表单渲染（前端）
4. 实现基础字段类型（文本、数字、日期、选择）

### 第二阶段：增强功能

1. 实现可视化表单设计器
2. 实现字段联动
3. 实现条件渲染
4. 实现表单模板管理

## 📝 数据库设计

### 表单模板表

```sql
CREATE TABLE form_templates (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    field_configs JSONB NOT NULL,  -- 字段配置
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 字段配置结构（JSON）

```json
{
  "fields": [
    {
      "id": "field_1",
      "name": "productName",
      "label": "产品名称",
      "type": "text",
      "required": true,
      "placeholder": "请输入产品名称",
      "validation": {
        "minLength": 2,
        "maxLength": 100
      },
      "order": 1
    },
    {
      "id": "field_2",
      "name": "quantity",
      "label": "数量",
      "type": "number",
      "required": true,
      "validation": {
        "min": 1,
        "max": 10000
      },
      "order": 2
    }
  ]
}
```

## ✅ 总结

**最终技术选型：Formily**

1. **最终方案**: 使用 Formily（提供表单设计器和强大功能）
   - 支持复杂表单场景
   - 提供可视化表单设计器
   - 支持字段联动和条件渲染
   - 企业级表单应用的最佳选择

2. **后端**: 使用 Pydantic 动态模型 + PostgreSQL JSONB 存储字段配置
3. **数据存储**: 使用 PostgreSQL JSONB 存储字段配置

**最终技术栈**: 
- **前端**: Formily (@formily/core + @formily/react + @formily/antd-v5)
- **后端**: FastAPI + Pydantic 动态模型 + PostgreSQL JSONB

