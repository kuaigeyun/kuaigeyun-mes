# RiverEdge SaaS 多组织框架 - 打印模板设计方案

## 📋 需求概述

在 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 中实现打印模板设计功能，允许用户自定义打印模板，支持报表、单据、标签等多种打印场景。

## 🔍 Ant Design Pro 原生方案

### 现状

**Ant Design Pro 没有专门的打印模板设计器原生组件**，但可以通过以下方式实现基础打印功能：

1. **浏览器原生打印** (`window.print()`)
2. **CSS 打印样式** (`@media print`)
3. **Ant Design 组件** 构建打印内容

### 基础实现方案

#### 方案一：浏览器原生打印（简单场景）

**适用场景**：
- 简单的报表打印
- 固定格式的打印模板
- 不需要复杂的打印设计器

**实现方式**：

```tsx
import { Button, Table, Card } from 'antd';
import { PrinterOutlined } from '@ant-design/icons';

export default function PrintPage() {
  const handlePrint = () => {
    window.print();
  };

  return (
    <div className="print-page">
      {/* 打印按钮（打印时隐藏） */}
      <div className="no-print">
        <Button 
          type="primary" 
          icon={<PrinterOutlined />}
          onClick={handlePrint}
        >
          打印
        </Button>
      </div>

      {/* 打印内容 */}
      <div className="print-content">
        <Card title="报表标题">
          <Table
            dataSource={data}
            columns={columns}
            pagination={false}
          />
        </Card>
      </div>
    </div>
  );
}
```

**CSS 打印样式**：

```css
/* 打印样式 */
@media print {
  /* 隐藏不需要打印的元素 */
  .no-print,
  .ant-layout-header,
  .ant-layout-sider,
  .ant-layout-footer,
  .ant-btn {
    display: none !important;
  }

  /* 打印内容样式 */
  .print-content {
    margin: 0;
    padding: 0;
    color: #000;
    background: #fff;
  }

  /* 分页控制 */
  .print-page {
    page-break-after: always;
  }

  /* 表格样式优化 */
  .ant-table {
    font-size: 12px;
  }
}
```

## 🎯 推荐方案

### 方案一：react-to-print（推荐，简单易用）

**react-to-print** 是专门用于 React 的打印库，与 Ant Design 完美集成。

#### 优势
- ✅ 轻量级，易于使用
- ✅ 支持 React 组件打印
- ✅ 与 Ant Design 组件兼容
- ✅ 支持打印预览
- ✅ 可自定义打印样式

#### 安装

```bash
npm install react-to-print
```

#### 使用示例

```tsx
import { useRef } from 'react';
import { useReactToPrint } from 'react-to-print';
import { Button, Table, Card } from 'antd';
import { PrinterOutlined } from '@ant-design/icons';

export default function PrintableReport() {
  const componentRef = useRef<HTMLDivElement>(null);

  const handlePrint = useReactToPrint({
    content: () => componentRef.current,
    documentTitle: '报表打印',
    pageStyle: `
      @page {
        size: A4;
        margin: 20mm;
      }
      @media print {
        body {
          -webkit-print-color-adjust: exact;
        }
      }
    `,
  });

  return (
    <div>
      <div className="no-print">
        <Button 
          type="primary" 
          icon={<PrinterOutlined />}
          onClick={handlePrint}
        >
          打印
        </Button>
      </div>

      <div ref={componentRef} className="print-content">
        <Card title="报表标题">
          <Table
            dataSource={data}
            columns={columns}
            pagination={false}
          />
        </Card>
      </div>
    </div>
  );
}
```

### 方案二：jsPDF + html2canvas（PDF 生成）

**适用场景**：
- 需要生成 PDF 文件
- 需要精确控制打印格式
- 需要离线打印

#### 优势
- ✅ 生成 PDF 文件
- ✅ 精确控制格式
- ✅ 支持复杂布局
- ✅ 可下载 PDF

#### 安装

```bash
npm install jspdf html2canvas
```

#### 使用示例

```tsx
import { useRef } from 'react';
import jsPDF from 'jspdf';
import html2canvas from 'html2canvas';
import { Button } from 'antd';

export default function PDFReport() {
  const componentRef = useRef<HTMLDivElement>(null);

  const handlePrint = async () => {
    if (!componentRef.current) return;

    const canvas = await html2canvas(componentRef.current, {
      scale: 2,
      useCORS: true,
    });

    const imgData = canvas.toDataURL('image/png');
    const pdf = new jsPDF('p', 'mm', 'a4');
    const imgWidth = 210;
    const pageHeight = 295;
    const imgHeight = (canvas.height * imgWidth) / canvas.width;
    let heightLeft = imgHeight;

    let position = 0;

    pdf.addImage(imgData, 'PNG', 0, position, imgWidth, imgHeight);
    heightLeft -= pageHeight;

    while (heightLeft >= 0) {
      position = heightLeft - imgHeight;
      pdf.addPage();
      pdf.addImage(imgData, 'PNG', 0, position, imgWidth, imgHeight);
      heightLeft -= pageHeight;
    }

    pdf.save('report.pdf');
  };

  return (
    <div>
      <Button onClick={handlePrint}>生成 PDF</Button>
      <div ref={componentRef}>
        {/* 打印内容 */}
      </div>
    </div>
  );
}
```

### 方案三：@ant-design/pro-editor（表单设计器，可扩展）

**适用场景**：
- 需要可视化设计打印模板
- 需要拖拽式模板设计
- 需要保存和复用模板

#### 说明
- Pro Editor 主要用于表单设计，但可以扩展用于打印模板设计
- 需要自定义开发打印模板设计器

### 方案四：form-render + 打印（推荐，基于配置）

**适用场景**：
- 需要基于配置的打印模板
- 需要动态生成打印内容
- 与表单自定义字段结合

#### 实现思路
1. 使用 form-render 的 Schema 定义打印模板
2. 使用 react-to-print 进行打印
3. 模板配置存储在数据库中

```tsx
import { useRef } from 'react';
import { useReactToPrint } from 'react-to-print';
import FormRender from 'form-render';
import { Button } from 'antd';

export default function ConfigurablePrint() {
  const componentRef = useRef<HTMLDivElement>(null);
  const [template, setTemplate] = useState({});

  // 从后端获取打印模板配置
  useEffect(() => {
    request('/api/print-template').then(setTemplate);
  }, []);

  const handlePrint = useReactToPrint({
    content: () => componentRef.current,
  });

  return (
    <div>
      <Button onClick={handlePrint}>打印</Button>
      <div ref={componentRef}>
        <FormRender schema={template.schema} formData={template.data} />
      </div>
    </div>
  );
}
```

## 🏗️ 打印模板设计器方案

### 方案一：基于 Ant Design 组件构建（推荐）

**实现思路**：
- 使用 Ant Design 组件构建打印模板设计器
- 支持拖拽式设计
- 模板配置存储为 JSON Schema

#### 核心功能

1. **模板设计器界面**
   - 左侧：组件库（文本、表格、图片、线条等）
   - 中间：设计画布
   - 右侧：属性配置面板

2. **支持的组件**
   - 文本组件
   - 表格组件
   - 图片组件
   - 线条/分割线
   - 二维码/条形码
   - 日期时间

3. **模板配置结构**

```typescript
interface PrintTemplate {
  id: string;
  name: string;
  pageSize: 'A4' | 'A3' | 'Letter';
  orientation: 'portrait' | 'landscape';
  margin: {
    top: number;
    right: number;
    bottom: number;
    left: number;
  };
  elements: PrintElement[];
}

interface PrintElement {
  id: string;
  type: 'text' | 'table' | 'image' | 'line' | 'barcode';
  position: { x: number; y: number };
  size: { width: number; height: number };
  style: Record<string, any>;
  dataSource?: string; // 数据源字段
}
```

### 方案二：集成第三方打印设计器

#### 推荐库：react-print-nb（不推荐，维护不活跃）

#### 推荐库：自定义开发（推荐）

基于 Ant Design 组件和 react-dnd（拖拽）开发自定义打印模板设计器。

## 📊 MES 系统打印场景

### 常见打印需求

1. **生产工单打印**
   - 工单信息
   - 产品信息
   - 工艺路线
   - 质量要求

2. **质量检验报告**
   - 检验项目
   - 检验结果
   - 合格判定
   - 签名栏

3. **设备标签打印**
   - 设备信息
   - 二维码
   - 条形码

4. **入库单/出库单**
   - 单据信息
   - 物料清单
   - 签名栏

5. **报表打印**
   - 生产报表
   - 质量报表
   - 设备报表

## 🛠️ 推荐技术方案

### 前端方案

**方案一：react-to-print（推荐）**
- 简单易用
- 与 Ant Design 完美集成
- 支持打印预览
- 适合大部分打印场景

**方案二：jsPDF + html2canvas（PDF 生成）**
- 生成 PDF 文件
- 精确控制格式
- 适合需要 PDF 文件的场景

**方案三：自定义打印模板设计器**
- 基于 Ant Design 组件
- 使用 react-dnd 实现拖拽
- 模板配置存储为 JSON

### 后端方案

**模板存储**：
- 使用 PostgreSQL JSONB 存储模板配置
- 使用 Pydantic 验证模板结构

**模板管理 API**：
- 创建模板
- 更新模板
- 删除模板
- 获取模板列表
- 预览模板

## 📝 实施建议

### 第一阶段：基础打印功能

1. 使用 `react-to-print` 实现基础打印
2. 使用 CSS `@media print` 优化打印样式
3. 实现常用打印模板（工单、报表等）

### 第二阶段：模板管理

1. 实现模板配置存储
2. 实现模板 CRUD 接口
3. 实现模板预览功能

### 第三阶段：可视化设计器（可选）

1. 开发拖拽式模板设计器
2. 支持组件库和属性配置
3. 实现模板保存和复用

## ✅ 总结

1. **Ant Design Pro 没有原生打印模板设计器**
2. **推荐方案**：
   - 简单场景：`react-to-print` + CSS 打印样式
   - PDF 生成：`jsPDF` + `html2canvas`
   - 模板设计器：基于 Ant Design 组件自定义开发
3. **技术栈统一**：全部使用 React 生态，与现有技术栈一致
4. **实施建议**：从基础打印功能开始，逐步完善模板管理功能

