# RiverEdge SaaS 多组织框架 - 插件目录结构说明

## 📋 概述

本文档说明 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的插件目录结构规范。

## 📁 统一目录结构

插件的前后端代码统一放在 `riveredge-seed` 目录下，每个插件是一个独立的目录，包含前端和后端代码。

## 🗂️ 标准结构

```
riveredge-seed/
├── seed-example/              # 示例插件
│   ├── frontend/              # 前端代码
│   │   ├── src/
│   │   │   ├── index.ts       # 前端插件入口
│   │   │   ├── routes.ts      # 前端路由
│   │   │   └── components/    # 前端组件
│   │   ├── package.json
│   │   └── vite.config.ts
│   ├── backend/               # 后端代码
│   │   ├── src/
│   │   │   ├── __init__.py
│   │   │   ├── plugin.py        # 后端插件入口
│   │   │   ├── api/
│   │   │   │   └── routes.py    # API 路由
│   │   │   ├── models/
│   │   │   │   └── example.py   # 数据模型（Tortoise ORM）
│   │   │   ├── schemas/
│   │   │   │   └── example.py   # Pydantic Schema
│   │   │   └── services/
│   │   │       └── example.py   # 业务逻辑
│   │   └── requirements.txt     # Python 依赖
│   ├── shared/                # 前后端共享代码（可选）
│   │   └── types/
│   ├── db/                    # 数据库迁移（可选）
│   │   └── migration/
│   ├── manifest.json           # 插件清单（必需）
│   └── README.md
```

## 📋 目录说明

### frontend/ - 前端代码

包含所有前端相关的代码、配置和资源。

- **src/**: 源代码目录
  - `index.ts`: 前端插件入口文件
  - `routes.ts`: 前端路由定义
  - `components/`: React 组件
  - `services/`: API 服务
  - `types/`: TypeScript 类型定义
  - `utils/`: 工具函数
- **public/**: 静态资源
- **package.json**: 前端依赖配置
- **vite.config.ts**: 构建配置
- **tsconfig.json**: TypeScript 配置

### backend/ - 后端代码

包含所有后端相关的代码和配置。

- **src/**: 源代码目录
  - `plugin.py`: 后端插件入口（继承 BasePlugin）
  - `api/routes.py`: FastAPI 路由定义
  - `models/`: Tortoise ORM 数据模型
  - `schemas/`: Pydantic 数据验证模型
  - `services/`: 业务逻辑服务
- **requirements.txt**: Python 依赖配置

### shared/ - 共享代码（可选）

前后端共享的代码，如类型定义、常量等。

### db/ - 数据库迁移（可选）

数据库迁移脚本，用于创建和更新数据库表结构。

### manifest.json - 插件清单

定义插件的前后端配置、路由、权限等信息。

### README.md - 插件文档

插件的使用说明和开发文档。

## 🔄 工作流程

### 开发流程

1. **创建插件目录**
   ```bash
   mkdir -p riveredge-seed/seed-example/{frontend,backend}
   ```

2. **初始化前端项目**
   ```bash
   cd riveredge-seed/seed-example/frontend
   npm init vite@latest . -- --template react-ts
   ```

3. **初始化后端项目**
   ```bash
   cd riveredge-seed/seed-example/backend
   # 创建 Python 项目结构
   mkdir -p src/{api,models,schemas,services}
   touch src/__init__.py src/plugin.py
   touch requirements.txt
   ```

4. **编写 manifest.json**
   - 定义插件 ID、名称、版本
   - 配置前端路由和后端 API
   - 定义权限要求

5. **开发功能**
   - 前端：开发 React 组件和页面
   - 后端：开发 FastAPI 路由、Tortoise ORM 模型、Pydantic Schema、业务服务

6. **构建和测试**
   - 前端：`npm run build`
   - 后端：安装依赖 `pip install -r requirements.txt`，运行测试 `pytest`

### 集成流程

#### 方式一：编译时集成（推荐）

1. 在 `riveredge-core` 中配置插件路径
2. 核心系统启动时自动扫描并注册插件

#### 方式二：运行时加载

1. 核心系统启动时扫描 `riveredge-seeds` 目录
2. 读取 `manifest.json` 配置
3. 动态加载插件类
4. 注册到插件注册表

## ✅ 优势

1. **统一管理**: 前后端代码在同一个目录下，便于管理
2. **独立开发**: 每个插件可以独立开发和测试
3. **版本一致**: 前后端版本号统一在 manifest.json 中管理
4. **易于部署**: 插件可以打包成独立的部署单元
5. **代码共享**: 通过 shared 目录共享类型定义等代码

## 📝 注意事项

1. **命名规范**: 插件目录名必须以 `seed-` 开头
2. **manifest.json**: 每个插件必须有 manifest.json 文件
3. **版本管理**: 前后端版本号应该保持一致
4. **依赖管理**: 前端和后端分别管理自己的依赖
5. **构建输出**: 前端构建输出到 `frontend/dist/`，后端代码直接作为 Python 模块使用

