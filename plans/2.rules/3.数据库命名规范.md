# RiverEdge SaaS 多组织框架 - 数据库命名规范

## 📋 概述

本文档定义 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的**数据库命名规范**，确保数据库设计规范统一，提升数据一致性和可维护性。

**命名哲学**：表名前缀遵循框架命名哲学（参考 [1.框架命名哲学.md](./1.框架命名哲学.md)）：
- `root_` 前缀对应系统级后端（根）- 如同植物的根系，提供基础支撑和养分
- `tree_` 前缀对应租户管理（树）- 每个租户单独管理
- `core_` 前缀对应应用级后端（核心）- 应用业务逻辑
- `seed_插件名_` 前缀对应 `riveredge-seed`（种子/应用插件，单数形式）- 如同植物的种子，可以生长成不同的功能模块

## 🎯 命名原则

1. **框架哲学一致性**：表名前缀必须符合框架命名哲学（root = 系统级后端，tree = 租户管理，core = 应用级后端，seed = 应用插件）⭐ **重要**
2. **模块化**：所有表名必须包含模块前缀，避免不同模块间的表名冲突 ⭐ **重要**
3. **一致性**：同类事物使用相同的命名风格
4. **可读性**：命名清晰表达含义，见名知意
5. **规范性**：遵循 PostgreSQL 和 Tortoise ORM 的命名约定
6. **多组织**：所有表必须包含组织隔离字段
7. **绝对禁止 PowerShell**：全程严禁使用 PowerShell 进行任何操作 ⭐ **重要**

## 📊 表命名规范

### 基本规则

**规则**：使用 `snake_case`（小写字母 + 下划线），**复数形式**，**必须包含模块前缀**

### 模块前缀规则 ⭐ 重要

**规则**：所有表名必须包含模块前缀，格式为 `模块前缀_表名`

#### 模块前缀定义（结合框架命名哲学）

**框架命名哲学映射**：
- `root` (根) → 系统级后端表前缀
- `tree` (树) → 租户管理表前缀
- `core` (核心) → 应用级后端表前缀
- `seed` (种子) → 应用插件表前缀

1. **系统级后端模块（root/根）**：`root_` 前缀 ⭐ **符合框架哲学**
   - 系统基础功能表（用户、角色、权限等）
   - 属于系统级后端（根系统）
   - **命名哲学**：如同植物的根系，提供基础支撑和养分

2. **租户管理模块（tree/树）**：`tree_` 前缀 ⭐ **符合框架哲学**
   - 租户管理表（组织、组织配置、组织活动日志等）
   - 每个租户单独管理
   - **命名哲学**：如同植物的树，每个租户是一棵树

3. **应用级后端模块（core/核心）**：`core_` 前缀 ⭐ **符合框架哲学**
   - 应用业务逻辑表（操作日志等）
   - 属于应用级后端
   - **命名哲学**：应用的核心功能

4. **应用插件模块（seed/种子）**：`seed_插件名_` 前缀 ⭐ **符合框架哲学**
   - 所有业务模块（MES、ERP、CRM 等）都属于 `riveredge-seed`（应用插件集合，单数形式）
   - MES 系统：`seed_mes_` 前缀（如 `seed_mes_orders`）
   - ERP 系统：`seed_erp_` 前缀（如 `seed_erp_invoices`）
   - CRM 系统：`seed_crm_` 前缀（如 `seed_crm_customers`）
   - 支付插件：`seed_payment_` 前缀（如 `seed_payment_records`）
   - 短信插件：`seed_sms_` 前缀（如 `seed_sms_templates`）
   - 邮件插件：`seed_email_` 前缀（如 `seed_email_logs`）
   - 所有插件表统一使用 `seed_插件名_表名` 格式
   - **命名哲学**：如同植物的种子，可以生长成不同的功能模块

**兼容性说明**：
- `sys_` 前缀作为 `root_` 的兼容别名，两者等价
- 推荐使用 `root_` 前缀，更符合新的命名哲学
- 现有使用 `sys_` 的代码可以继续使用，新代码建议使用 `root_`

#### 单表命名

**系统级表**（使用 `root_` 前缀，系统级后端）：
- ✅ `root_users` - 用户表（系统级后端）
- ✅ `root_roles` - 角色表（系统级后端）
- ✅ `root_permissions` - 权限表（系统级后端）

**租户管理表**（使用 `tree_` 前缀，租户管理）：
- ✅ `tree_tenants` - 组织表（租户管理）
- ✅ `tree_tenant_configs` - 组织配置表（租户管理）
- ✅ `tree_tenant_activity_logs` - 组织活动日志表（租户管理）

**应用级表**（使用 `core_` 前缀，应用级后端）：
- ✅ `core_operation_logs` - 操作日志表（应用级后端）

**兼容性说明**：
- `sys_` 前缀作为 `root_` 的兼容别名，两者等价
- 推荐使用 `root_` 前缀，更符合新的命名哲学
- 现有使用 `sys_` 的代码可以继续使用，新代码建议使用 `root_`
- ❌ `users` - 错误：缺少模块前缀
- ❌ `user` - 错误：应使用复数形式且包含前缀
- ❌ `User` - 错误：应使用小写且包含前缀

**应用插件表**（使用 `seed_插件名_` 前缀）：
- ✅ `seed_mes_orders` - MES 插件订单表
- ✅ `seed_mes_products` - MES 插件产品表
- ✅ `seed_mes_production_lines` - MES 插件生产线表
- ✅ `seed_erp_invoices` - ERP 插件发票表
- ✅ `seed_crm_customers` - CRM 插件客户表
- ✅ `seed_payment_records` - 支付插件记录表
- ✅ `seed_sms_templates` - 短信插件模板表
- ✅ `seed_email_logs` - 邮件插件日志表
- ❌ `mes_orders` - 错误：缺少 `seed_` 前缀
- ❌ `orders` - 错误：缺少模块前缀
- ❌ `seed_mes_order` - 错误：应使用复数形式

#### 关联表命名

**规则**：关联表同样需要模块前缀，格式为 `模块前缀_表1_表2`

**系统级关联表**（使用 `root_` 前缀）：
- ✅ `root_users_root_roles` - 用户角色关联表（系统级后端）
- ✅ `root_roles_root_permissions` - 角色权限关联表（系统级后端）
- ✅ `sys_user_roles` - 用户角色关联表（兼容别名，推荐使用 `root_users_root_roles`）
- ❌ `user_roles` - 错误：缺少模块前缀
- ❌ `user_role` - 错误：应使用复数形式且包含前缀

**应用插件关联表**：
- ✅ `seed_mes_order_items` - MES 插件订单明细表（主表_子表）
- ✅ `seed_mes_product_categories` - MES 插件产品分类关联表
- ✅ `seed_payment_order_items` - 支付插件订单明细表
- ❌ `mes_order_items` - 错误：缺少 `seed_` 前缀
- ❌ `order_items` - 错误：缺少模块前缀
- ❌ `seed_mes_order_item` - 错误：应使用复数形式

### 表命名示例

```sql
-- 系统级表（root_ 前缀，系统级后端）
root_users              -- 用户表（系统级后端）
root_roles              -- 角色表（系统级后端）
root_permissions        -- 权限表（系统级后端）

-- 租户管理表（tree_ 前缀，租户管理）
tree_tenants            -- 组织表（租户管理）
tree_tenant_configs     -- 组织配置表（租户管理）
tree_tenant_activity_logs -- 组织活动日志表（租户管理）

-- 应用级表（core_ 前缀，应用级后端）
core_operation_logs     -- 操作日志表（应用级后端）

-- 系统级关联表
root_users_root_roles         -- 用户角色关联表（系统级后端）
root_roles_root_permissions    -- 角色权限关联表（系统级后端）

-- 兼容别名（sys_ 前缀，推荐使用 root_）
sys_users               -- 用户表（兼容别名，推荐使用 root_users）
sys_roles               -- 角色表（兼容别名，推荐使用 root_roles）

-- MES 应用插件表（seed_mes_ 前缀）
seed_mes_orders             -- MES 插件订单表（主表）
seed_mes_order_items        -- MES 插件订单明细表（子表）
seed_mes_products           -- MES 插件产品表
seed_mes_categories         -- MES 插件分类表
seed_mes_production_lines   -- MES 插件生产线表
seed_mes_work_orders        -- MES 插件工单表

-- ERP 应用插件表（seed_erp_ 前缀）
seed_erp_invoices           -- ERP 插件发票表
seed_erp_invoice_items      -- ERP 插件发票明细表

-- CRM 应用插件表（seed_crm_ 前缀）
seed_crm_customers          -- CRM 插件客户表
seed_crm_contacts           -- CRM 插件联系人表

-- 其他应用插件表（seed_插件名_ 前缀）
seed_payment_records        -- 支付插件记录表
seed_sms_templates          -- 短信插件模板表
seed_email_logs             -- 邮件插件日志表
```

## 🔑 字段命名规范

### 基本规则

**规则**：使用 `snake_case`（小写字母 + 下划线）

### 主键字段

**规则**：统一使用 `id`

- ✅ `id` - 主键（INTEGER PRIMARY KEY 或 BIGSERIAL）
- ❌ `user_id` - 错误：主键应使用 `id`
- ❌ `userId` - 错误：应使用下划线
- ❌ `pk` - 错误：应使用 `id`

```sql
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,  -- ✅ 主键使用 id
    -- ...
);
```

### 组织字段

**规则**：所有表必须包含 `tenant_id`（多组织隔离）

- ✅ `tenant_id` - 组织 ID（所有表必须包含）
- ❌ `tenantId` - 错误：应使用下划线
- ❌ `company_id` - 错误：应统一使用 `tenant_id`

```sql
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,  -- ✅ 组织 ID（必须）
    -- ...
    INDEX idx_root_users_tenant_id (tenant_id)  -- ✅ 必须创建索引
);
```

### 外键字段

**规则**：使用 `表名_id` 格式

- ✅ `user_id` - 用户 ID 外键
- ✅ `order_id` - 订单 ID 外键
- ✅ `role_id` - 角色 ID 外键
- ✅ `tenant_id` - 组织 ID 外键
- ❌ `userId` - 错误：应使用下划线
- ❌ `orderId` - 错误：应使用下划线
- ❌ `user` - 错误：外键应使用 `_id` 后缀

```sql
CREATE TABLE seed_mes_order_items (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    order_id INTEGER NOT NULL,  -- ✅ 外键：订单 ID
    product_id INTEGER NOT NULL,  -- ✅ 外键：产品 ID
    -- ...
    FOREIGN KEY (order_id) REFERENCES seed_mes_orders(id),
    FOREIGN KEY (product_id) REFERENCES seed_mes_products(id)
);
```

### 时间字段

**规则**：使用 `_at` 后缀

- ✅ `created_at` - 创建时间（TIMESTAMP）
- ✅ `updated_at` - 更新时间（TIMESTAMP）
- ✅ `deleted_at` - 删除时间（TIMESTAMP，软删除）
- ✅ `published_at` - 发布时间（TIMESTAMP）
- ❌ `createTime` - 错误：应使用下划线和 `_at` 后缀
- ❌ `create_time` - 错误：应使用 `created_at`

```sql
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    -- ...
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- ✅ 创建时间
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- ✅ 更新时间
    deleted_at TIMESTAMP NULL  -- ✅ 删除时间（软删除）
);
```

### 布尔字段

**规则**：使用 `is_` 前缀

- ✅ `is_active` - 是否激活
- ✅ `is_deleted` - 是否删除（软删除标志）
- ✅ `is_superuser` - 是否超级管理员
- ✅ `is_verified` - 是否已验证
- ❌ `active` - 错误：布尔字段应使用 `is_` 前缀
- ❌ `isActive` - 错误：应使用下划线

```sql
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    -- ...
    is_active BOOLEAN DEFAULT TRUE,  -- ✅ 是否激活
    is_deleted BOOLEAN DEFAULT FALSE,  -- ✅ 是否删除
    is_superuser BOOLEAN DEFAULT FALSE  -- ✅ 是否超级管理员
);
```

### 状态字段

**规则**：使用 `status`，配合枚举类型

- ✅ `status` - 状态字段（VARCHAR 或 ENUM）
- ❌ `state` - 错误：应统一使用 `status`
- ❌ `orderStatus` - 错误：应使用 `status`

```sql
CREATE TABLE seed_mes_orders (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    -- ...
    status VARCHAR(20) DEFAULT 'pending',  -- ✅ 状态字段
    -- 状态值：pending, processing, completed, cancelled
    CHECK (status IN ('pending', 'processing', 'completed', 'cancelled'))
);
```

### 数量字段

**规则**：使用明确的单位后缀

- ✅ `quantity` - 数量
- ✅ `total_amount` - 总金额
- ✅ `unit_price` - 单价
- ✅ `discount_rate` - 折扣率
- ❌ `amount` - 不推荐：应明确是总金额还是单价
- ❌ `price` - 不推荐：应明确是单价还是总价

```sql
CREATE TABLE seed_mes_order_items (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    order_id INTEGER NOT NULL,
    product_id INTEGER NOT NULL,
    quantity INTEGER NOT NULL,  -- ✅ 数量
    unit_price DECIMAL(10, 2) NOT NULL,  -- ✅ 单价
    total_amount DECIMAL(10, 2) NOT NULL,  -- ✅ 总金额
    -- ...
);
```

### 文本字段

**规则**：使用描述性名称

- ✅ `username` - 用户名
- ✅ `email` - 邮箱
- ✅ `full_name` - 全名
- ✅ `description` - 描述
- ✅ `remarks` - 备注
- ❌ `name` - 不推荐：应明确是用户名、产品名还是其他
- ❌ `desc` - 错误：应使用完整单词 `description`

```sql
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    username VARCHAR(50) NOT NULL,  -- ✅ 用户名
    email VARCHAR(100) NOT NULL,  -- ✅ 邮箱
    full_name VARCHAR(100),  -- ✅ 全名
    description TEXT,  -- ✅ 描述
    -- ...
);
```

### JSON 字段

**规则**：使用 `_config`、`_data`、`_settings` 等后缀

- ✅ `config` - 配置（JSONB）
- ✅ `settings` - 设置（JSONB）
- ✅ `metadata` - 元数据（JSONB）
- ✅ `extra_data` - 额外数据（JSONB）
- ❌ `json` - 错误：应使用描述性名称

```sql
CREATE TABLE sys_tenants (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    -- ...
    settings JSONB DEFAULT '{}',  -- ✅ 组织设置（JSONB）
    config JSONB DEFAULT '{}'  -- ✅ 组织配置（JSONB）
);
```

## 📐 索引命名规范

### 基本规则

**规则**：使用 `idx_表名_字段名` 或 `uk_表名_字段名`（唯一索引）

### 普通索引

**规则**：索引命名格式为 `idx_表名_字段名`（表名包含模块前缀）

- ✅ `idx_core_users_tenant_id` - 用户表组织 ID 索引（根系统）
- ✅ `idx_core_users_email` - 用户表邮箱索引（根系统）
- ✅ `idx_seed_mes_orders_status` - MES 插件订单表状态索引
- ✅ `idx_seed_mes_orders_created_at` - MES 插件订单表创建时间索引
- ❌ `idx_users_tenant_id` - 错误：表名应包含模块前缀
- ❌ `idx_mes_orders_status` - 错误：缺少 `seed_` 前缀
- ✅ `idx_sys_users_tenant_id` - 兼容别名（推荐使用 `idx_core_users_tenant_id`）
- ❌ `users_tenant_id_idx` - 错误：应使用 `idx_` 前缀
- ❌ `index_users_tenant_id` - 错误：应使用 `idx_` 前缀

```sql
-- 核心系统表索引（core_ 前缀，符合框架哲学）
CREATE INDEX idx_core_users_tenant_id ON core_users(tenant_id);
CREATE INDEX idx_core_users_email ON core_users(email);
CREATE INDEX idx_root_roles_tenant_id ON root_roles(tenant_id);

-- 应用插件表索引
CREATE INDEX idx_seed_mes_orders_status ON seed_mes_orders(status);
CREATE INDEX idx_seed_mes_orders_created_at ON seed_mes_orders(created_at);
CREATE INDEX idx_seed_mes_order_items_order_id ON seed_mes_order_items(order_id);

-- 复合索引
CREATE INDEX idx_seed_mes_orders_tenant_status ON seed_mes_orders(tenant_id, status);
CREATE INDEX idx_seed_mes_order_items_order_product ON seed_mes_order_items(order_id, product_id);
```

### 唯一索引

**规则**：使用 `uk_` 前缀（unique key），格式为 `uk_表名_字段名`（表名包含模块前缀）

- ✅ `uk_core_users_email` - 用户表邮箱唯一索引（根系统）
- ✅ `uk_core_users_tenant_username` - 用户表组织+用户名唯一索引（根系统）
- ✅ `uk_sys_users_email` - 兼容别名（推荐使用 `uk_core_users_email`）
- ✅ `uk_seed_mes_orders_order_no` - MES 插件订单表订单号唯一索引
- ❌ `uk_users_email` - 错误：表名应包含模块前缀
- ❌ `uk_mes_orders_order_no` - 错误：缺少 `seed_` 前缀
- ❌ `unique_users_email` - 错误：应使用 `uk_` 前缀

```sql
-- 核心系统表唯一索引（core_ 前缀，符合框架哲学）
CREATE UNIQUE INDEX uk_core_users_email ON core_users(email);

-- 复合唯一索引（组织内唯一）
CREATE UNIQUE INDEX uk_core_users_tenant_username ON core_users(tenant_id, username);
CREATE UNIQUE INDEX uk_seed_mes_orders_tenant_order_no ON seed_mes_orders(tenant_id, order_no);
```

### 主键索引

**规则**：主键索引自动创建，无需手动命名

```sql
-- 主键索引自动创建，无需手动创建
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,  -- ✅ 主键索引自动创建
    -- ...
);
```

### 外键索引

**规则**：外键索引自动创建，但建议显式创建以提高性能

```sql
-- 外键索引（建议显式创建）
CREATE INDEX idx_seed_mes_order_items_order_id ON seed_mes_order_items(order_id);
CREATE INDEX idx_seed_mes_order_items_product_id ON seed_mes_order_items(product_id);
```

## 🔗 约束命名规范

### 基本规则

**规则**：使用 `ck_表名_字段名`（检查约束）、`fk_表名_字段名`（外键约束）

### 检查约束

**规则**：约束命名格式为 `ck_表名_字段名`（表名包含模块前缀）

- ✅ `ck_core_users_email_format` - 用户表邮箱格式检查（根系统）
- ✅ `ck_seed_mes_orders_status_valid` - MES 插件订单表状态有效性检查
- ✅ `ck_seed_mes_order_items_quantity_positive` - MES 插件订单明细表数量正数检查
- ❌ `ck_users_email_format` - 错误：表名应包含模块前缀
- ❌ `ck_mes_orders_status_valid` - 错误：缺少 `seed_` 前缀
- ✅ `ck_sys_users_email_format` - 兼容别名（推荐使用 `ck_core_users_email_format`）

```sql
-- 核心系统表检查约束（core_ 前缀，符合框架哲学）
ALTER TABLE core_users ADD CONSTRAINT ck_core_users_email_format
    CHECK (email ~* '^[\w\.-]+@[\w\.-]+\.\w+$');

-- 应用插件表检查约束
ALTER TABLE seed_mes_orders ADD CONSTRAINT ck_seed_mes_orders_status_valid
    CHECK (status IN ('pending', 'processing', 'completed', 'cancelled'));

ALTER TABLE seed_mes_order_items ADD CONSTRAINT ck_seed_mes_order_items_quantity_positive
    CHECK (quantity > 0);
```

### 外键约束

**规则**：约束命名格式为 `fk_表名_字段名`（表名包含模块前缀）

- ✅ `fk_seed_mes_order_items_order_id` - MES 插件订单明细表订单 ID 外键
- ✅ `fk_seed_mes_order_items_product_id` - MES 插件订单明细表产品 ID 外键
- ❌ `fk_order_items_order_id` - 错误：表名应包含模块前缀
- ❌ `fk_mes_order_items_order_id` - 错误：缺少 `seed_` 前缀

```sql
-- 应用插件表外键约束
ALTER TABLE seed_mes_order_items ADD CONSTRAINT fk_seed_mes_order_items_order_id
    FOREIGN KEY (order_id) REFERENCES seed_mes_orders(id) ON DELETE CASCADE;

ALTER TABLE seed_mes_order_items ADD CONSTRAINT fk_seed_mes_order_items_product_id
    FOREIGN KEY (product_id) REFERENCES seed_mes_products(id) ON DELETE RESTRICT;

-- 核心系统表外键约束（core_ 前缀，符合框架哲学）
ALTER TABLE core_users ADD CONSTRAINT fk_core_users_tenant_id
    FOREIGN KEY (tenant_id) REFERENCES tree_tenants(id) ON DELETE RESTRICT;
```

## 📋 字段类型规范

### 字符串类型

**规则**：根据长度选择 VARCHAR 或 TEXT

- ✅ `VARCHAR(50)` - 短字符串（用户名、状态等，≤ 100 字符）
- ✅ `VARCHAR(100)` - 中等字符串（邮箱、名称等，≤ 255 字符）
- ✅ `VARCHAR(255)` - 长字符串（标题、描述等，≤ 500 字符）
- ✅ `TEXT` - 超长文本（内容、备注等，> 500 字符）
- ❌ `CHAR(n)` - 不推荐：除非长度固定，否则使用 VARCHAR

```sql
CREATE TABLE root_users (
    username VARCHAR(50) NOT NULL,   -- ✅ 用户名（短字符串）
    email VARCHAR(100) NOT NULL,    -- ✅ 邮箱（中等字符串）
    full_name VARCHAR(100),          -- ✅ 全名（中等字符串）
    description TEXT                 -- ✅ 描述（超长文本）
);
```

### 数值类型

**规则**：根据范围选择 INTEGER 或 BIGINT

- ✅ `INTEGER` - 整数（-2,147,483,648 到 2,147,483,647）
- ✅ `BIGINT` - 大整数（-9,223,372,036,854,775,808 到 9,223,372,036,854,775,807）
- ✅ `DECIMAL(10, 2)` - 金额（10 位数字，2 位小数）
- ✅ `DECIMAL(18, 4)` - 高精度金额（18 位数字，4 位小数）
- ❌ `FLOAT` - 不推荐：金额应使用 DECIMAL
- ❌ `DOUBLE` - 不推荐：金额应使用 DECIMAL

```sql
CREATE TABLE seed_mes_orders (
    id SERIAL PRIMARY KEY,                    -- ✅ 主键（自动递增）
    tenant_id INTEGER NOT NULL,               -- ✅ 组织 ID（整数）
    quantity INTEGER NOT NULL,                -- ✅ 数量（整数）
    unit_price DECIMAL(10, 2) NOT NULL,       -- ✅ 单价（金额）
    total_amount DECIMAL(10, 2) NOT NULL,     -- ✅ 总金额（金额）
    discount_rate DECIMAL(5, 2) DEFAULT 0     -- ✅ 折扣率（百分比）
);
```

### 日期时间类型

**规则**：统一使用 TIMESTAMP

- ✅ `TIMESTAMP` - 日期时间（带时区）
- ✅ `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` - 默认当前时间
- ❌ `DATETIME` - 错误：PostgreSQL 使用 TIMESTAMP
- ❌ `DATE` - 不推荐：除非只需要日期不需要时间

```sql
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,
    -- ...
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- ✅ 创建时间
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- ✅ 更新时间
    deleted_at TIMESTAMP NULL                        -- ✅ 删除时间（软删除）
);
```

### 布尔类型

**规则**：使用 BOOLEAN

- ✅ `BOOLEAN` - 布尔值
- ✅ `BOOLEAN DEFAULT TRUE` - 默认值
- ✅ `BOOLEAN DEFAULT FALSE` - 默认值
- ❌ `TINYINT(1)` - 错误：PostgreSQL 使用 BOOLEAN
- ❌ `INTEGER` - 错误：应使用 BOOLEAN

```sql
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,
    -- ...
    is_active BOOLEAN DEFAULT TRUE,      -- ✅ 是否激活
    is_deleted BOOLEAN DEFAULT FALSE,    -- ✅ 是否删除
    is_superuser BOOLEAN DEFAULT FALSE   -- ✅ 是否超级管理员
);
```

### JSON 类型

**规则**：使用 JSONB（二进制 JSON，性能更好）

- ✅ `JSONB` - JSON 数据（推荐，支持索引和查询）
- ✅ `JSONB DEFAULT '{}'` - 默认空对象
- ✅ `JSONB DEFAULT '[]'` - 默认空数组
- ❌ `JSON` - 不推荐：JSONB 性能更好

```sql
CREATE TABLE tree_tenants (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    settings JSONB DEFAULT '{}',  -- ✅ 组织设置（JSONB）
    config JSONB DEFAULT '{}'     -- ✅ 组织配置（JSONB）
);

-- JSONB 支持索引
CREATE INDEX idx_core_tenants_settings_plan ON core_tenants USING GIN (settings);
```

### 枚举类型

**规则**：使用 VARCHAR + CHECK 约束或 PostgreSQL ENUM

```sql
-- 方式一：使用 VARCHAR + CHECK（推荐，灵活性高）
CREATE TABLE seed_mes_orders (
    id SERIAL PRIMARY KEY,
    status VARCHAR(20) DEFAULT 'pending',
    CHECK (status IN ('pending', 'processing', 'completed', 'cancelled'))
);

-- 方式二：使用 PostgreSQL ENUM（类型安全）
CREATE TYPE seed_mes_order_status AS ENUM ('pending', 'processing', 'completed', 'cancelled');
CREATE TABLE seed_mes_orders (
    id SERIAL PRIMARY KEY,
    status seed_mes_order_status DEFAULT 'pending'
);
```

## 🏗️ 表结构规范

### 标准表结构

**规则**：所有表应包含以下标准字段

```sql
CREATE TABLE 模块前缀_表名 (
    -- 主键
    id SERIAL PRIMARY KEY,

    -- 组织隔离（所有表必须包含）
    tenant_id INTEGER NOT NULL,

    -- 业务字段
    -- ...

    -- 标准时间字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,  -- 软删除（可选）

    -- 标准索引
    INDEX idx_模块前缀_表名_tenant_id (tenant_id),
    INDEX idx_模块前缀_表名_created_at (created_at)
);

-- 示例：核心系统表（core_ 前缀，符合框架哲学：core = 根）
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    -- ...
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_root_users_tenant_id (tenant_id),
    INDEX idx_core_users_created_at (created_at)
);

-- 示例：应用插件表
CREATE TABLE seed_mes_orders (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    -- ...
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_seed_mes_orders_tenant_id (tenant_id),
    INDEX idx_seed_mes_orders_created_at (created_at)
);
```

### 完整示例

```sql
-- 核心系统表：用户表（core_ 前缀，符合框架哲学：core = 根）
CREATE TABLE root_users (
    -- 主键
    id SERIAL PRIMARY KEY,

    -- 组织隔离（必须）
    tenant_id INTEGER NOT NULL,

    -- 业务字段
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100) NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(100),
    phone VARCHAR(20),
    avatar_url VARCHAR(255),

    -- 状态字段
    is_active BOOLEAN DEFAULT TRUE,
    is_superuser BOOLEAN DEFAULT FALSE,
    status VARCHAR(20) DEFAULT 'active',

    -- 时间字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,

    -- 索引（core_ 前缀，符合框架哲学）
    INDEX idx_root_users_tenant_id (tenant_id),
    INDEX idx_core_users_email (email),
    INDEX idx_core_users_username (username),
    INDEX idx_core_users_status (status),
    INDEX idx_core_users_created_at (created_at),

    -- 唯一约束（组织内用户名唯一）
    UNIQUE KEY uk_core_users_tenant_username (tenant_id, username),

    -- 唯一约束（邮箱全局唯一）
    UNIQUE KEY uk_core_users_email (email),

    -- 外键约束
    FOREIGN KEY (tenant_id) REFERENCES tree_tenants(id) ON DELETE RESTRICT
);

-- MES 应用插件表：订单表（主表）
CREATE TABLE seed_mes_orders (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    order_no VARCHAR(50) NOT NULL,
    customer_name VARCHAR(100) NOT NULL,
    total_amount DECIMAL(10, 2) NOT NULL,
    status VARCHAR(20) DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    INDEX idx_seed_mes_orders_tenant_id (tenant_id),
    INDEX idx_seed_mes_orders_order_no (order_no),
    INDEX idx_seed_mes_orders_status (status),
    INDEX idx_seed_mes_orders_created_at (created_at),

    UNIQUE KEY uk_seed_mes_orders_tenant_order_no (tenant_id, order_no),

    FOREIGN KEY (tenant_id) REFERENCES tree_tenants(id) ON DELETE RESTRICT
);

-- MES 应用插件表：订单明细表（子表）
CREATE TABLE seed_mes_order_items (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    order_id INTEGER NOT NULL,
    product_id INTEGER NOT NULL,
    product_name VARCHAR(100) NOT NULL,
    quantity INTEGER NOT NULL,
    unit_price DECIMAL(10, 2) NOT NULL,
    total_amount DECIMAL(10, 2) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    INDEX idx_seed_mes_order_items_tenant_id (tenant_id),
    INDEX idx_seed_mes_order_items_order_id (order_id),
    INDEX idx_seed_mes_order_items_product_id (product_id),

    FOREIGN KEY (tenant_id) REFERENCES tree_tenants(id) ON DELETE RESTRICT,
    FOREIGN KEY (order_id) REFERENCES seed_mes_orders(id) ON DELETE CASCADE,
    FOREIGN KEY (product_id) REFERENCES seed_mes_products(id) ON DELETE RESTRICT,

    -- 检查约束
    CONSTRAINT ck_seed_mes_order_items_quantity_positive CHECK (quantity > 0),
    CONSTRAINT ck_seed_mes_order_items_price_positive CHECK (unit_price > 0)
);
```

## 🔍 查询优化规范

### 索引使用规范

**规则**：为常用查询字段创建索引

#### 必须创建索引的字段

1. **组织字段**：所有表的 `tenant_id`（多组织查询必需）
2. **外键字段**：所有外键字段（关联查询必需）
3. **状态字段**：`status`（状态筛选常用）
4. **时间字段**：`created_at`、`updated_at`（时间范围查询常用）
5. **唯一字段**：邮箱、用户名等（唯一性检查必需）

#### 复合索引

**规则**：为常用组合查询创建复合索引

```sql
-- 核心系统表复合索引（core_ 前缀，符合框架哲学）
CREATE INDEX idx_core_users_tenant_status ON core_users(tenant_id, status);

-- 应用插件表复合索引
-- 组织 + 状态查询（常用）
CREATE INDEX idx_seed_mes_orders_tenant_status ON seed_mes_orders(tenant_id, status);

-- 组织 + 创建时间查询（常用）
CREATE INDEX idx_seed_mes_orders_tenant_created ON seed_mes_orders(tenant_id, created_at);

-- 订单 + 产品查询（主子表关联）
CREATE INDEX idx_seed_mes_order_items_order_product ON seed_mes_order_items(order_id, product_id);
```

### 查询性能规范

**规则**：避免全表扫描，使用索引字段查询

```sql
-- ✅ 推荐：使用索引字段查询（核心系统表，core_ 前缀）
SELECT * FROM core_users WHERE tenant_id = 1 AND status = 'active';

-- ✅ 推荐：使用索引字段查询（应用插件表）
SELECT * FROM seed_mes_orders WHERE tenant_id = 1 AND status = 'pending';

-- ❌ 不推荐：全表扫描
SELECT * FROM core_users WHERE full_name LIKE '%张%';

-- ✅ 推荐：使用索引字段 + 限制条件
SELECT * FROM core_users
WHERE tenant_id = 1
  AND created_at >= '2024-01-01'
LIMIT 100;
```

## 📝 命名示例总结

### 完整表结构示例

```sql
-- 核心系统表：用户表（core_ 前缀，符合框架哲学：core = 根）
CREATE TABLE root_users (
    id SERIAL PRIMARY KEY,
    tenant_id INTEGER NOT NULL,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100) NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(100),
    phone VARCHAR(20),
    is_active BOOLEAN DEFAULT TRUE,
    is_superuser BOOLEAN DEFAULT FALSE,
    status VARCHAR(20) DEFAULT 'active',
    settings JSONB DEFAULT '{}',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,

    INDEX idx_root_users_tenant_id (tenant_id),
    INDEX idx_core_users_email (email),
    INDEX idx_core_users_username (username),
    INDEX idx_core_users_status (status),
    INDEX idx_core_users_created_at (created_at),

    UNIQUE KEY uk_core_users_tenant_username (tenant_id, username),
    UNIQUE KEY uk_core_users_email (email),

    FOREIGN KEY (tenant_id) REFERENCES tree_tenants(id) ON DELETE RESTRICT,

    CONSTRAINT ck_core_users_email_format CHECK (email ~* '^[\w\.-]+@[\w\.-]+\.\w+$'),
    CONSTRAINT ck_core_users_status_valid CHECK (status IN ('active', 'inactive', 'suspended', 'deleted'))
);
```

## ✅ 检查清单

### 表设计检查

- [ ] **表名是否包含模块前缀？**（核心系统：`core_`，应用插件：`seed_插件名_`）
- [ ] 表名是否使用复数形式？
- [ ] 表名是否使用小写字母和下划线？
- [ ] 是否包含 `tenant_id` 字段？
- [ ] 是否包含 `id` 主键？
- [ ] 是否包含 `created_at` 和 `updated_at`？
- [ ] 是否创建了 `tenant_id` 索引？（索引名应包含模块前缀）
- [ ] 是否创建了必要的唯一索引？（索引名应包含模块前缀）

### 字段设计检查

- [ ] 字段名是否使用小写字母和下划线？
- [ ] 主键是否使用 `id`？
- [ ] 外键是否使用 `表名_id` 格式？
- [ ] 布尔字段是否使用 `is_` 前缀？
- [ ] 时间字段是否使用 `_at` 后缀？
- [ ] 金额字段是否使用 `DECIMAL` 类型？
- [ ] JSON 字段是否使用 `JSONB` 类型？

### 索引设计检查

- [ ] 是否为 `tenant_id` 创建了索引？（索引名应包含模块前缀）
- [ ] 是否为外键创建了索引？（索引名应包含模块前缀）
- [ ] 是否为常用查询字段创建了索引？（索引名应包含模块前缀）
- [ ] 是否为常用组合查询创建了复合索引？（索引名应包含模块前缀）
- [ ] 唯一索引是否使用 `uk_` 前缀？
- [ ] 普通索引是否使用 `idx_` 前缀？
- [ ] 索引名中的表名是否包含模块前缀？

## 📚 相关文档

- [2.字段命名规范.md](./2.字段命名规范.md) - 代码命名规范
- [4.注释规范.md](./4.注释规范.md) - 代码注释规范
