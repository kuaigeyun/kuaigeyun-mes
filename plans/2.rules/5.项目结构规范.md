# RiverEdge SaaS 多组织框架 - 项目结构规范

## 📋 概述

本文档定义 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的**统一项目结构规范**，确保项目组织清晰、模块划分明确，提升代码可维护性和可扩展性。

**创建日期**：2025-11-17

## 🎯 设计原则

1. **模块化**：按功能模块组织代码，职责清晰
2. **分层架构**：API、Service、Model、Schema 分层清晰
3. **约定优于配置**：统一的目录结构，减少配置
4. **可扩展性**：支持插件化扩展，结构灵活
5. **多组织优先**：所有模块从设计之初就考虑多组织隔离
6. **绝对禁止 PowerShell**：全程严禁使用 PowerShell 进行任何操作 ⭐ **重要**

## 📁 根目录结构

```
riveredge/
├── riveredge-backend/       # 后端核心系统
├── riveredge-frontend/      # 前端应用
├── riveredge-seed/          # 应用插件集合（种子，单数形式）
├── riveredge-land/          # 着陆页/官网（可选）
├── riveredge-leaf/          # 移动端应用（可选）
└── plans/                   # 开发计划和设计规范
```

## 🔧 riveredge-backend（后端核心系统）

### 标准目录结构

```
riveredge-backend/
├── src/                      # 源代码目录
│   ├── app/                  # 应用入口层
│   │   ├── __init__.py
│   │   └── main.py           # FastAPI 应用入口，整合所有路由
│   │
│   ├── platform/             # 平台级功能模块
│   │   ├── api/              # 平台级 API 路由
│   │   │   ├── tenants/      # 租户管理 API
│   │   │   ├── packages/     # 套餐管理 API
│   │   │   ├── platform_superadmin/  # 平台超管 API
│   │   │   ├── monitoring/   # 监控统计 API
│   │   │   ├── saved_searches/ # 保存的搜索 API
│   │   │   └── auth/         # 平台认证 API
│   │   ├── models/           # 平台级数据模型
│   │   ├── schemas/          # 平台级数据验证
│   │   ├── services/        # 平台级业务服务
│   │   ├── domain/          # 平台级领域逻辑
│   │   │   ├── security/    # 安全相关
│   │   │   ├── tenant_context.py  # 租户上下文
│   │   │   └── query_filter.py    # 查询过滤
│   │   ├── config/          # 平台级配置
│   │   ├── infrastructure/  # 基础设施
│   │   │   ├── database/    # 数据库连接
│   │   │   ├── cache/       # 缓存管理
│   │   │   └── logging/     # 日志配置
│   │   └── exceptions/      # 异常定义
│   │
│   ├── core/                # 系统级功能模块
│   │   ├── api/             # 系统级 API 路由
│   │   │   ├── users/       # 用户管理 API
│   │   │   ├── roles/       # 角色管理 API
│   │   │   ├── permissions/ # 权限管理 API
│   │   │   ├── departments/ # 部门管理 API
│   │   │   └── ...          # 其他系统级 API
│   │   ├── models/          # 系统级数据模型
│   │   ├── schemas/         # 系统级数据验证
│   │   ├── services/        # 系统级业务服务
│   │   ├── middleware/      # 系统级中间件
│   │   ├── inngest/         # Inngest 任务调度 ⭐ FIR 核心
│   │   │   ├── client.py    # Inngest 客户端
│   │   │   └── functions/   # Inngest 函数定义
│   │   ├── context/         # 上下文管理
│   │   └── utils/           # 工具函数
│   │
│   └── static/              # 静态资源
│
├── migrations/              # Aerich 数据库迁移
│   ├── models/             # 模型定义（Aerich 自动生成）
│   └── versions/           # 迁移版本文件
│
├── tests/                  # 测试代码
│   ├── __init__.py
│   ├── conftest.py         # pytest 配置和 fixtures
│   ├── test_api/           # API 测试
│   ├── test_services/      # Service 测试
│   └── test_models/         # Model 测试
│
├── .env.example            # 环境变量示例
├── .gitignore              # Git 忽略文件
├── requirements.txt        # Python 依赖
├── requirements-dev.txt    # 开发依赖
├── pyproject.toml          # 项目配置（Aerich 配置）
├── README.md               # 项目说明
└── aerich_config.py        # Aerich 配置（可选）
```

### 目录说明

#### `src/server/` - 服务器入口层

**职责**：FastAPI 应用初始化、路由注册、中间件配置

**文件说明**：
- `main.py`: FastAPI 应用入口，整合所有路由（platform、core、插件路由）

**架构设计**：应用入口层，负责整合所有模块

#### `src/platform/` - 平台级功能模块

**职责**：平台级后端基础设施和服务

**架构设计**：传统分层架构（Controller → Service → Repository）

**目录结构**：
- `api/`: 平台级 API 路由（tenants、packages、monitoring 等）
- `models/`: 平台级数据模型
- `schemas/`: 平台级数据验证
- `services/`: 平台级业务服务
- `domain/`: 平台级领域逻辑（security、tenant_context、query_filter）
- `config/`: 平台级配置
- `infrastructure/`: 基础设施（database、cache、logging）
- `exceptions/`: 异常定义

**命名规范**：
- API 文件使用复数形式（`tenants.py`、`packages.py`）
- 路由函数使用 `snake_case`（`get_tenants`、`create_tenant`）
- 模型文件使用单数形式（`tenant.py`、`package.py`）
- 服务文件使用 `snake_case` + `_service` 后缀（`tenant_service.py`）

#### `src/core/` - 系统级功能模块

**职责**：系统级后端 API 和核心功能

**架构设计**：传统分层架构（Controller → Service → Repository）

**目录结构**：
- `api/`: 系统级 API 路由（users、roles、permissions、departments 等）
- `models/`: 系统级数据模型
- `schemas/`: 系统级数据验证
- `services/`: 系统级业务服务
- `middleware/`: 系统级中间件
- `inngest/`: Inngest 任务调度 ⭐ **FIR 核心**
- `context/`: 上下文管理
- `utils/`: 工具函数

**命名规范**：
- API 文件使用复数形式（`users.py`、`roles.py`）
- 路由函数使用 `snake_case`（`get_users`、`create_user`）
- 模型文件使用单数形式（`user.py`、`role.py`）
- 服务文件使用 `snake_case` + `_service` 后缀（`user_service.py`）

## 🎨 riveredge-frontend（前端应用）

### 标准目录结构（扁平化结构）

```
riveredge-frontend/
├── src/                      # 源代码目录（扁平化结构，已移除 app 目录层级）
│   ├── components/           # 公共组件
│   │   ├── error-boundary/   # 错误边界
│   │   ├── uni_table/        # 统一表格组件
│   │   ├── tenant_selector/  # 租户选择器
│   │   ├── page_tabs/        # 页面标签
│   │   └── ...               # 其他公共组件
│   │
│   ├── pages/                # 页面组件
│   │   ├── platform/         # 平台级页面
│   │   │   ├── tenants/     # 租户管理
│   │   │   ├── packages/    # 套餐管理
│   │   │   └── monitoring/  # 监控统计
│   │   ├── system/          # 系统级页面
│   │   │   ├── users/       # 用户管理
│   │   │   ├── roles/       # 角色管理
│   │   │   ├── departments/ # 部门管理
│   │   │   └── ...          # 其他系统级页面
│   │   ├── personal/        # 个人功能页面
│   │   └── login/           # 登录页面
│   │
│   ├── routes/               # 路由配置
│   │   └── index.tsx         # 路由定义
│   │
│   ├── services/             # API 服务层
│   │   ├── api.ts           # API 基础配置（TanStack Query）
│   │   ├── auth.ts          # 认证服务
│   │   ├── user.ts          # 用户服务
│   │   ├── tenant.ts        # 租户服务
│   │   └── ...              # 其他 API 服务
│   │
│   ├── stores/               # Zustand状态存储
│   │   └── globalStore.ts   # 全局状态
│   │
│   ├── hooks/               # 自定义Hooks
│   │   └── useProTableSearch.ts
│   │
│   ├── layouts/             # 布局组件
│   │   └── BasicLayout.tsx  # 基础布局
│   │
│   ├── config/              # 配置文件
│   │   ├── i18n.ts         # 国际化配置
│   │   └── mode.ts         # 应用模式配置
│   │
│   ├── utils/               # 工具函数
│   ├── types/               # TypeScript类型定义
│   ├── locales/             # 国际化文件
│   ├── contexts/            # React Context
│   ├── guards/              # 路由守卫
│   ├── app.tsx              # 应用主组件
│   ├── main.tsx             # React应用启动文件
│   ├── access.ts            # 权限控制
│   ├── global.less          # 全局样式
│   └── vite.config.ts       # Vite配置
│
├── public/                  # 静态资源
├── index.html               # HTML模板
├── tsconfig.json            # TypeScript配置
└── package.json
```

### 目录说明（扁平化结构）

**重要说明**：前端采用扁平化结构，所有代码直接在 `src/` 下组织，已移除 `app/` 目录层级。

#### `src/components/` - 公共组件

**职责**：可复用的 UI 组件

**文件说明**：
- `uni_table/`: 统一表格组件（基于 ProTable）
- `tenant_selector/`: 租户选择器组件
- `page_tabs/`: 页面标签组件
- `riveredge_query/`: 查询条件保存组件
- `error-boundary/`: 错误边界组件

**命名规范**：
- **组件文件夹统一使用 `snake_case`**（`uni_table/`、`tenant_selector/`、`page_tabs/`）⭐ **重要**
- 组件文件使用 `PascalCase`（`UniTable.tsx`）
- ❌ **禁止使用 `PascalCase` 命名文件夹**：`UniTable/`、`TenantSelector/`
- ❌ **禁止使用 `camelCase` 命名文件夹**：`uniTable/`、`tenantSelector/`
- ❌ **禁止使用 `kebab-case` 命名文件夹**：`uni-table/`、`tenant-selector/`（除非是框架特殊要求）

#### `src/pages/` - 页面目录

**职责**：定义页面组件和路由

**文件说明**：
- `platform/`: 平台级页面（tenants、packages、monitoring 等）
- `system/`: 系统级页面（users、roles、departments 等）
- `personal/`: 个人功能页面
- `login/`: 登录页面

**命名规范**：
- **目录统一使用 `snake_case`**（`user/`、`role/`、`uni_table/`、`tenant_selector/`）⭐ **重要**
- 组件文件使用 `PascalCase`（`UserList.tsx`）
- 功能模块文件夹使用单数：`user/`、`role/`、`dashboard/`、`login/`
- 功能集合文件夹使用复数：`components/`、`services/`、`stores/`

#### `src/routes/` - 路由配置

**职责**：定义应用路由

**文件说明**：
- `index.tsx`: 路由定义（路径、组件、权限等）

#### `src/services/` - API 服务层

**职责**：封装 API 调用

**文件说明**：
- `api.ts`: TanStack Query 基础配置（拦截器、错误处理）
- 每个资源一个服务文件（如 `user.ts`、`role.ts`、`tenant.ts`）

**命名规范**：
- 服务文件使用 `camelCase`（`user.ts`、`role.ts`）
- 服务函数使用 `camelCase`（`getUserList`、`createUser`）

#### `src/stores/` - Zustand状态存储

**职责**：使用 Zustand 管理全局状态

**文件说明**：
- `globalStore.ts`: 全局状态管理

#### `src/hooks/` - 自定义Hooks

**职责**：封装可复用的逻辑

**文件说明**：
- `useProTableSearch.ts`: ProTable 搜索 Hook
- 使用 `use` 前缀命名（`useAuth`、`useUser`）

#### `src/layouts/` - 布局组件

**职责**：页面布局组件

**文件说明**：
- `BasicLayout.tsx`: 基础布局组件

#### `src/config/` - 配置文件

**职责**：应用配置文件

**文件说明**：
- `i18n.ts`: 国际化配置
- `mode.ts`: 应用模式配置

#### `src/utils/` - 工具函数

**职责**：提供通用工具函数

#### `src/types/` - 类型定义

**职责**：定义 TypeScript 类型

#### `src/locales/` - 国际化文件

**职责**：国际化翻译文件

#### `src/contexts/` - React Context

**职责**：提供全局上下文

#### `src/guards/` - 路由守卫

**职责**：实现路由级权限控制

## 🌱 riveredge-seed（应用插件集合，单数形式）

### 标准目录结构

```
riveredge-seed/
├── seed-example/             # 示例插件
│   ├── frontend/             # 前端代码
│   │   ├── src/
│   │   │   ├── index.ts      # 前端插件入口
│   │   │   ├── routes.ts     # 前端路由
│   │   │   └── components/  # 前端组件
│   │   ├── package.json
│   │   └── vite.config.ts
│   │
│   ├── backend/              # 后端代码
│   │   ├── src/
│   │   │   ├── __init__.py
│   │   │   ├── plugin.py     # 后端插件入口
│   │   │   ├── api/
│   │   │   │   └── routes.py # API 路由
│   │   │   ├── models/
│   │   │   │   └── example.py # 数据模型
│   │   │   ├── schemas/
│   │   │   │   └── example.py # Pydantic Schema
│   │   │   └── services/
│   │   │       └── example.py # 业务逻辑
│   │   └── requirements.txt
│   │
│   ├── manifest.json         # 插件清单（必需）
│   └── README.md
│
└── README.md                 # 插件集合说明
```

详细说明请参考：[插件目录结构说明.md](../4.branch/插件目录结构说明.md)

## 📋 文件命名规范

### 后端文件命名

- **Python 文件**: `snake_case`（`user_service.py`、`auth_api.py`）
- **模型文件**: 单数形式（`user.py`、`role.py`）
- **API 文件**: 复数形式（`users.py`、`roles.py`）
- **服务文件**: `snake_case` + `_service` 后缀（`user_service.py`）

### 前端文件命名

- **TypeScript 文件**: `camelCase` 或 `PascalCase`
  - 组件文件: `PascalCase`（`UserList.tsx`）
  - 工具文件: `camelCase`（`userService.ts`）
- **目录**: **统一使用 `snake_case`**（`user/`、`role/`、`uni_table/`、`tenant_selector/`）⭐ **重要**
  - ✅ **功能模块文件夹使用单数**：`user/`、`role/`、`dashboard/`、`login/`
  - ✅ **功能集合文件夹使用复数**：`components/`、`services/`、`stores/`、`hooks/`、`utils/`
  - ✅ **组件文件夹使用 `snake_case`**：`uni_table/`、`tenant_selector/`、`page_tabs/`、`lucky_import/`
  - ✅ **页面文件夹使用 `snake_case`**：`user/`、`role/`、`dashboard/`、`operations/`、`system/`
  - ❌ **禁止使用 `PascalCase`**：`UniTable/`、`TenantSelector/`、`PageTabs/`
  - ❌ **禁止使用 `camelCase`**：`uniTable/`、`tenantSelector/`、`pageTabs/`
  - ❌ **禁止使用 `kebab-case`**：`uni-table/`、`tenant-selector/`、`page-tabs/`（除非是框架特殊要求）

## ✅ 检查清单

创建新模块时，请确保：

- [ ] 目录结构符合规范
- [ ] 文件命名符合规范
- [ ] 模块职责清晰，单一职责原则
- [ ] 多组织隔离已考虑（后端模型包含 `tenant_id`）
- [ ] 类型定义完整（TypeScript 类型、Pydantic Schema）
- [ ] 注释完整（参考 [4.注释规范.md](./4.注释规范.md)）

## 🎯 技术栈（FIR）

**FIR 技术栈组合**：
- **F** - **FastAPI**: 高性能异步 Web 框架
- **I** - **Inngest**: 现代化任务调度系统
- **R** - **React**: 现代化前端框架

**架构设计**：
- **平台级、系统级**：传统分层架构（Controller → Service → Repository）
- **应用级**：DDD（领域驱动设计）架构

## 📚 相关文档

- [1.框架命名哲学.md](./1.框架命名哲学.md) - 命名哲学
- [2.字段命名规范.md](./2.字段命名规范.md) - 命名规范
- [4.注释规范.md](./4.注释规范.md) - 注释规范
- [6.API设计规范.md](./6.API设计规范.md) - API 设计规范
- [2.架构设计.md](../1.stack/2.架构设计.md) - 架构设计

---

**最后更新**：2025-01-04
**状态**：根据最新架构设计更新为主流 B 端命名和扁平化前端结构

