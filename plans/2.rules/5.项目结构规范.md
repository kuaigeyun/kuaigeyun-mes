# RiverEdge SaaS 多组织框架 - 项目结构规范

## 📋 概述

本文档定义 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的**统一项目结构规范**，确保项目组织清晰、模块划分明确，提升代码可维护性和可扩展性。

**创建日期**：2025-11-17

## 🎯 设计原则

1. **模块化**：按功能模块组织代码，职责清晰
2. **分层架构**：API、Service、Model、Schema 分层清晰
3. **约定优于配置**：统一的目录结构，减少配置
4. **可扩展性**：支持插件化扩展，结构灵活
5. **多组织优先**：所有模块从设计之初就考虑多组织隔离
6. **绝对禁止 PowerShell**：全程严禁使用 PowerShell 进行任何操作 ⭐ **重要**

## 📁 根目录结构

```
riveredge/
├── riveredge-core/          # 后端核心系统（根）
├── riveredge-shell/         # 前端基础框架（壳）
├── riveredge-seed/          # 应用插件集合（种子，单数形式）
├── riveredge-land/          # 着陆页/官网（土地）
├── riveredge-leaf/          # 移动端应用（叶子）
└── Farming Plan/            # 开发计划和设计规范
```

## 🔧 riveredge-core（后端核心系统）

### 标准目录结构

```
riveredge-core/
├── src/                      # 源代码目录
│   ├── app/                  # FastAPI 应用配置
│   │   ├── __init__.py
│   │   ├── main.py           # FastAPI 应用入口
│   │   ├── config.py         # 应用配置（Pydantic Settings）
│   │   ├── dependencies.py   # 依赖注入（获取当前用户、组织等）
│   │   └── middleware.py     # 中间件配置（CORS、异常处理等）
│   │
│   ├── api/                  # API 路由层
│   │   ├── __init__.py
│   │   ├── deps.py           # API 依赖（认证、权限检查等）
│   │   └── v1/               # API 版本 1
│   │       ├── __init__.py
│   │       ├── auth.py       # 认证授权 API
│   │       ├── users.py      # 用户管理 API
│   │       ├── roles.py      # 角色权限 API
│   │       ├── tenants.py    # 组织管理 API
│   │       ├── plugins.py     # 插件管理 API
│   │       └── system.py     # 系统设置 API
│   │
│   ├── core/                 # 核心功能模块
│   │   ├── __init__.py
│   │   ├── base.py           # 基础类（BaseModel、BaseService 等）
│   │   ├── security.py       # 安全相关（JWT、密码加密）
│   │   ├── config.py         # 核心配置
│   │   ├── database.py       # 数据库连接和配置
│   │   ├── cache.py          # Redis 缓存工具
│   │   ├── response.py       # 统一响应封装
│   │   ├── exceptions.py     # 自定义异常类
│   │   ├── query.py          # 查询增强组件
│   │   └── plugin_loader.py  # 插件加载器
│   │
│   ├── models/               # 数据模型（Tortoise ORM）
│   │   ├── __init__.py
│   │   ├── base.py           # 基础模型（包含 tenant_id）
│   │   ├── user.py           # 用户模型
│   │   ├── role.py           # 角色模型
│   │   ├── tenant.py         # 组织模型
│   │   ├── plugin.py         # 插件模型
│   │   └── system.py         # 系统配置模型
│   │
│   ├── schemas/              # Pydantic Schema（数据验证）
│   │   ├── __init__.py
│   │   ├── user.py           # 用户 Schema（Create、Update、Response）
│   │   ├── role.py           # 角色 Schema
│   │   ├── tenant.py         # 组织 Schema
│   │   ├── plugin.py         # 插件 Schema
│   │   └── common.py         # 通用 Schema（分页、响应等）
│   │
│   ├── services/             # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── base.py           # 基础服务类（包含组织过滤）
│   │   ├── auth_service.py   # 认证服务
│   │   ├── user_service.py   # 用户服务
│   │   ├── role_service.py   # 角色服务
│   │   ├── tenant_service.py # 组织服务
│   │   └── plugin_service.py # 插件服务
│   │
│   └── utils/                # 工具函数
│       ├── __init__.py
│       ├── logger.py         # 日志工具（loguru）
│       ├── password.py        # 密码加密工具
│       └── helpers.py        # 辅助函数
│
├── migrations/               # Aerich 数据库迁移
│   ├── models.py            # 模型定义（Aerich 自动生成）
│   └── versions/            # 迁移版本文件
│
├── tests/                   # 测试代码
│   ├── __init__.py
│   ├── conftest.py          # pytest 配置和 fixtures
│   ├── test_api/            # API 测试
│   ├── test_services/       # Service 测试
│   └── test_models/         # Model 测试
│
├── .env.example             # 环境变量示例
├── .gitignore               # Git 忽略文件
├── requirements.txt         # Python 依赖
├── requirements-dev.txt     # 开发依赖
├── pyproject.toml           # 项目配置（可选）
├── README.md                # 项目说明
└── alembic.ini              # Aerich 配置（可选）
```

### 目录说明

#### `src/app/` - 应用配置

**职责**：FastAPI 应用初始化、配置、中间件

**文件说明**：
- `main.py`: FastAPI 应用入口，注册路由、中间件
- `config.py`: 使用 Pydantic Settings 管理配置
- `dependencies.py`: FastAPI 依赖注入（获取当前用户、组织等）
- `middleware.py`: 中间件配置（CORS、异常处理、日志等）

#### `src/api/` - API 路由层

**职责**：定义 API 路由，处理 HTTP 请求

**文件说明**：
- `deps.py`: API 依赖（认证、权限检查、获取当前用户等）
- `v1/`: API 版本 1 的路由文件
  - 每个资源一个文件（如 `users.py`、`roles.py`）
  - 使用 FastAPI Router 组织路由

**命名规范**：
- API 文件使用复数形式（`users.py`、`roles.py`）
- 路由函数使用 `snake_case`（`get_users`、`create_user`）

#### `src/core/` - 核心功能模块

**职责**：提供核心功能，供其他模块使用

**文件说明**：
- `base.py`: 基础类（BaseModel、BaseService 等）
- `security.py`: 安全相关（JWT Token 生成/验证、密码加密）
- `database.py`: 数据库连接和配置（Tortoise ORM）
- `cache.py`: Redis 缓存工具类
- `response.py`: 统一响应封装（ResponseModel、PageResponse 等）
- `exceptions.py`: 自定义异常类
- `query.py`: 查询增强组件（分页、排序、筛选）
- `plugin_loader.py`: 插件加载器

#### `src/models/` - 数据模型

**职责**：定义数据库模型（Tortoise ORM）

**文件说明**：
- `base.py`: 基础模型（包含 `id`、`created_at`、`updated_at`、`tenant_id`）
- 每个业务实体一个文件（如 `user.py`、`role.py`）

**命名规范**：
- 模型文件使用单数形式（`user.py`、`role.py`）
- 模型类使用 `PascalCase`（`User`、`Role`）

#### `src/schemas/` - 数据验证

**职责**：定义 Pydantic Schema，用于请求/响应数据验证

**文件说明**：
- 每个业务实体一个文件（如 `user.py`、`role.py`）
- 每个文件包含多个 Schema 类：
  - `XxxCreate`: 创建请求 Schema
  - `XxxUpdate`: 更新请求 Schema
  - `XxxResponse`: 响应 Schema
  - `XxxListResponse`: 列表响应 Schema

**命名规范**：
- Schema 文件使用单数形式（`user.py`、`role.py`）
- Schema 类使用 `PascalCase`（`UserCreate`、`UserUpdate`）

#### `src/services/` - 业务逻辑层

**职责**：实现业务逻辑，处理数据操作

**文件说明**：
- `base.py`: 基础服务类（包含组织过滤、CRUD 方法）
- 每个业务实体一个服务文件（如 `user_service.py`、`role_service.py`）

**命名规范**：
- 服务文件使用 `snake_case` + `_service` 后缀（`user_service.py`）
- 服务类使用 `PascalCase` + `Service` 后缀（`UserService`）

#### `src/utils/` - 工具函数

**职责**：提供通用工具函数

**文件说明**：
- `logger.py`: 日志工具（loguru 配置）
- `password.py`: 密码加密工具
- `helpers.py`: 辅助函数

## 🎨 riveredge-shell（前端基础框架）

### 标准目录结构

```
riveredge-shell/
├── src/                      # 源代码目录
│   ├── pages/                # 页面目录
│   │   ├── user/             # 用户管理页面
│   │   │   ├── index.tsx     # 用户列表页
│   │   │   └── components/   # 页面组件
│   │   ├── role/             # 角色管理页面
│   │   ├── tenant/            # 组织管理页面
│   │   ├── system/            # 系统设置页面
│   │   └── login/             # 登录页面
│   │
│   ├── components/           # 公共组件
│   │   ├── Layout/            # 布局组件
│   │   ├── Table/             # 表格组件
│   │   ├── Form/              # 表单组件
│   │   └── common/           # 通用组件
│   │
│   ├── hooks/                # 自定义Hooks
│   │   ├── useAuth.ts         # 认证相关Hooks
│   │   ├── useUser.ts         # 用户相关Hooks
│   │   └── useTable.ts        # 表格相关Hooks
│   │
│   ├── stores/               # Zustand状态存储
│   │   ├── userStore.ts       # 用户状态管理
│   │   ├── globalStore.ts     # 全局状态管理
│   │   └── pluginStore.ts     # 插件状态管理
│   │
│   ├── services/             # API 服务
│   │   ├── api.ts             # API 基础配置（TanStack Query）
│   │   ├── userService.ts     # 用户 API 服务
│   │   ├── roleService.ts     # 角色 API 服务
│   │   └── tenantService.ts   # 组织 API 服务
│   │
│   ├── utils/                # 工具函数
│   │   ├── request.ts         # 请求工具（Fetch API 配置）
│   │   ├── auth.ts            # 认证工具（Token 管理）
│   │   └── helpers.ts         # 辅助函数
│   │
│   ├── types/                # TypeScript 类型定义
│   │   ├── api.ts             # API 类型
│   │   ├── user.ts             # 用户类型
│   │   └── common.ts          # 通用类型
│   │
│   ├── locales/              # 国际化文件
│   │   ├── zh-CN/             # 中文
│   │   └── en-US/             # 英文
│   │
│   ├── guards/               # 路由守卫
│   ├── contexts/             # React Context
│   │   ├── AuthContext.tsx    # 权限上下文
│   │   └── ThemeContext.tsx   # 主题上下文
│   ├── router/               # 路由配置
│   │   ├── routes.tsx         # 路由定义
│   │   └── index.tsx          # 路由入口
│   ├── App.tsx                # 应用入口组件
│   ├── main.tsx               # React应用启动文件
│   └── index.css              # 全局样式
│
├── public/                   # 静态资源
│   ├── favicon.ico
│   └── logo.png
│
├── vite.config.ts             # Vite 配置文件
├── .eslintrc.js               # ESLint 配置
├── .prettierrc                # Prettier 配置
├── package.json
├── tsconfig.json              # TypeScript 配置
└── README.md
```

### 目录说明

#### `src/pages/` - 页面目录

**职责**：定义页面组件和路由

**文件说明**：
- 每个功能模块一个目录（如 `user/`、`role/`）
- 每个目录包含：
  - `index.tsx`: 主页面组件
  - `components/`: 页面专用组件

**命名规范**：
- **目录统一使用 `snake_case`**（`user/`、`role/`、`uni_table/`、`tenant_selector/`）⭐ **重要**
- 组件文件使用 `PascalCase`（`UserList.tsx`）
- 功能模块文件夹使用单数：`user/`、`role/`、`dashboard/`
- 功能集合文件夹使用复数：`components/`、`services/`、`stores/`

#### `src/components/` - 公共组件

**职责**：可复用的 UI 组件

**文件说明**：
- `uni_table/`: 统一表格组件（基于 ProTable）
- `tenant_selector/`: 租户选择器组件
- `page_tabs/`: 页面标签组件
- `riveredge_query/`: 查询条件保存组件

**命名规范**：
- **组件文件夹统一使用 `snake_case`**（`uni_table/`、`tenant_selector/`、`page_tabs/`）⭐ **重要**
- 组件文件使用 `PascalCase`（`UniTable.tsx`）
- ❌ **禁止使用 `PascalCase` 命名文件夹**：`UniTable/`、`TenantSelector/`
- ❌ **禁止使用 `camelCase` 命名文件夹**：`uniTable/`、`tenantSelector/`
- ❌ **禁止使用 `kebab-case` 命名文件夹**：`uni-table/`、`tenant-selector/`

#### `src/hooks/` - 自定义Hooks

**职责**：封装可复用的逻辑

**文件说明**：
- 每个功能模块一个Hook文件（如 `useAuth.ts`、`useUser.ts`）
- 使用 `use` 前缀命名（`useAuth`、`useUser`）

#### `src/stores/` - Zustand状态存储

**职责**：使用 Zustand 管理全局状态

**文件说明**：
- 每个功能模块一个Store文件（如 `userStore.ts`、`globalStore.ts`）
- 使用 `Store` 后缀命名（`userStore.ts`）

#### `src/services/` - API 服务

**职责**：封装 API 调用

**文件说明**：
- `api.ts`: TanStack Query 基础配置（拦截器、错误处理）
- 每个资源一个服务文件（如 `userService.ts`、`roleService.ts`）

**命名规范**：
- 服务文件使用 `camelCase` + `Service` 后缀（`userService.ts`）
- 服务函数使用 `camelCase`（`getUserList`、`createUser`）

#### `src/types/` - 类型定义

**职责**：定义 TypeScript 类型

#### `src/guards/` - 路由守卫

**职责**：实现路由级权限控制

**文件说明**：
- `AuthGuard.tsx`: 认证守卫（检查用户登录状态）
- `RoleGuard.tsx`: 角色守卫（检查用户权限）

#### `src/contexts/` - React Context

**职责**：提供全局上下文

**文件说明**：
- `AuthContext.tsx`: 权限上下文（用户信息、权限状态）
- `ThemeContext.tsx`: 主题上下文（主题配置、切换逻辑）

#### `src/router/` - 路由配置

**职责**：定义应用路由

**文件说明**：
- `routes.tsx`: 路由定义（路径、组件、权限等）
- `index.tsx`: 路由入口（Router 配置、守卫应用）

## 🌱 riveredge-seed（应用插件集合，单数形式）

### 标准目录结构

```
riveredge-seed/
├── seed-example/             # 示例插件
│   ├── frontend/             # 前端代码
│   │   ├── src/
│   │   │   ├── index.ts      # 前端插件入口
│   │   │   ├── routes.ts     # 前端路由
│   │   │   └── components/  # 前端组件
│   │   ├── package.json
│   │   └── vite.config.ts
│   │
│   ├── backend/              # 后端代码
│   │   ├── src/
│   │   │   ├── __init__.py
│   │   │   ├── plugin.py     # 后端插件入口
│   │   │   ├── api/
│   │   │   │   └── routes.py # API 路由
│   │   │   ├── models/
│   │   │   │   └── example.py # 数据模型
│   │   │   ├── schemas/
│   │   │   │   └── example.py # Pydantic Schema
│   │   │   └── services/
│   │   │       └── example.py # 业务逻辑
│   │   └── requirements.txt
│   │
│   ├── manifest.json         # 插件清单（必需）
│   └── README.md
│
└── README.md                 # 插件集合说明
```

详细说明请参考：[插件目录结构说明.md](../4.branch/插件目录结构说明.md)

## 📋 文件命名规范

### 后端文件命名

- **Python 文件**: `snake_case`（`user_service.py`、`auth_api.py`）
- **模型文件**: 单数形式（`user.py`、`role.py`）
- **API 文件**: 复数形式（`users.py`、`roles.py`）
- **服务文件**: `snake_case` + `_service` 后缀（`user_service.py`）

### 前端文件命名

- **TypeScript 文件**: `camelCase` 或 `PascalCase`
  - 组件文件: `PascalCase`（`UserList.tsx`）
  - 工具文件: `camelCase`（`userService.ts`）
- **目录**: **统一使用 `snake_case`**（`user/`、`role/`、`uni_table/`、`tenant_selector/`）⭐ **重要**
  - ✅ **功能模块文件夹使用单数**：`user/`、`role/`、`dashboard/`、`login/`
  - ✅ **功能集合文件夹使用复数**：`components/`、`services/`、`stores/`、`hooks/`、`utils/`
  - ✅ **组件文件夹使用 `snake_case`**：`uni_table/`、`tenant_selector/`、`page_tabs/`、`lucky_import/`
  - ✅ **页面文件夹使用 `snake_case`**：`user/`、`role/`、`dashboard/`、`operations/`、`system/`
  - ❌ **禁止使用 `PascalCase`**：`UniTable/`、`TenantSelector/`、`PageTabs/`
  - ❌ **禁止使用 `camelCase`**：`uniTable/`、`tenantSelector/`、`pageTabs/`
  - ❌ **禁止使用 `kebab-case`**：`uni-table/`、`tenant-selector/`、`page-tabs/`（除非是框架特殊要求）

## ✅ 检查清单

创建新模块时，请确保：

- [ ] 目录结构符合规范
- [ ] 文件命名符合规范
- [ ] 模块职责清晰，单一职责原则
- [ ] 多组织隔离已考虑（后端模型包含 `tenant_id`）
- [ ] 类型定义完整（TypeScript 类型、Pydantic Schema）
- [ ] 注释完整（参考 [4.注释规范.md](./4.注释规范.md)）

## 📚 相关文档

- [1.框架命名哲学.md](./1.框架命名哲学.md) - 命名哲学
- [2.字段命名规范.md](./2.字段命名规范.md) - 命名规范
- [4.注释规范.md](./4.注释规范.md) - 注释规范
- [6.API设计规范.md](./6.API设计规范.md) - API 设计规范
- [2.架构设计文档.md](../1.plan/2.架构设计文档.md) - 架构设计
- [插件目录结构说明.md](../4.branch/插件目录结构说明.md) - 插件目录结构

---

**最后更新**：2025-11-18

