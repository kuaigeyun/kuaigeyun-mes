# RiverEdge SaaS 多组织框架 - 命名规范

## 📋 概述

本文档定义 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的**统一命名规范**，确保代码风格一致，提升代码可读性和可维护性。

## 🎯 命名原则

1. **一致性**：同类事物使用相同的命名风格
2. **可读性**：命名清晰表达意图，见名知意
3. **简洁性**：避免过长的命名，但不过度缩写
4. **规范性**：遵循语言和框架的命名约定
5. **避免系统关键字**：禁止使用编程语言、数据库、框架的系统关键字和保留字 ⭐ **重要**
6. **绝对禁止 PowerShell**：全程严禁使用 PowerShell 进行任何操作 ⭐ **重要**

## 🔧 后端命名规范（Python）

### 文件命名

**规则**：使用 `snake_case`（小写字母 + 下划线）

#### 模型文件
- ✅ `user.py` - 用户模型
- ✅ `order.py` - 订单模型
- ✅ `order_item.py` - 订单明细模型
- ❌ `User.py` - 错误：不应使用大写
- ❌ `userModel.py` - 错误：不应使用驼峰

#### API 文件
- ✅ `users.py` - 用户管理 API（**复数形式**）
- ✅ `orders.py` - 订单管理 API（**复数形式**）
- ✅ `auth.py` - 认证 API（功能名称）
- ❌ `user.py` - 错误：API 文件应使用复数

#### Service 文件
- ✅ `user_service.py` - 用户服务
- ✅ `order_service.py` - 订单服务
- ✅ `auth_service.py` - 认证服务
- ❌ `userService.py` - 错误：不应使用驼峰
- ❌ `userservice.py` - 错误：应使用下划线分隔

#### Schema 文件
- ✅ `user_schema.py` - 用户 Schema
- ✅ `order_schema.py` - 订单 Schema
- ✅ `auth_schema.py` - 认证 Schema
- ❌ `userSchema.py` - 错误：不应使用驼峰

#### 工具文件
- ✅ `utils.py` - 工具函数
- ✅ `helpers.py` - 辅助函数
- ✅ `constants.py` - 常量定义
- ✅ `exceptions.py` - 异常定义

#### 配置文件
- ✅ `config.py` - 配置文件
- ✅ `settings.py` - 设置文件
- ✅ `database.py` - 数据库配置

### 目录命名

**规则**：使用 `snake_case`（小写字母 + 下划线）

```
riveredge-backend/
├── src/
│   ├── app/              # 应用入口层
│   ├── platform/         # 平台级功能模块
│   ├── core/             # 系统级功能模块
│   ├── api/              # API 路由（已废弃，使用 platform/api 或 core/api）
│   ├── models/           # 数据模型（已废弃，使用 platform/models 或 core/models）
│   ├── schemas/          # Pydantic Schema（已废弃，使用 platform/schemas 或 core/schemas）
│   ├── services/         # 业务逻辑（已废弃，使用 platform/services 或 core/services）
│   ├── core/             # 核心功能
│   └── utils/            # 工具函数
```

### 类命名

**规则**：使用 `PascalCase`（首字母大写的驼峰命名）

#### 模型类
- ✅ `User` - 用户模型
- ✅ `Order` - 订单模型
- ✅ `OrderItem` - 订单明细模型
- ✅ `BaseModel` - 基础模型（抽象基类）
- ❌ `user` - 错误：类名应使用 PascalCase
- ❌ `user_model` - 错误：类名不应使用下划线

#### Schema 类
- ✅ `UserCreate` - 用户创建 Schema
- ✅ `UserUpdate` - 用户更新 Schema
- ✅ `UserResponse` - 用户响应 Schema
- ✅ `UserListResponse` - 用户列表响应 Schema
- ❌ `user_create` - 错误：类名应使用 PascalCase
- ❌ `CreateUser` - 错误：应使用名词+动作格式

#### Service 类
- ✅ `UserService` - 用户服务
- ✅ `OrderService` - 订单服务
- ✅ `AuthService` - 认证服务
- ✅ `BaseService` - 基础服务（抽象基类）
- ❌ `user_service` - 错误：类名应使用 PascalCase

#### API 类（可选，通常使用函数）
- ✅ `UserAPI` - 用户 API（如果使用类）
- ✅ `OrderAPI` - 订单 API（如果使用类）

#### 异常类
- ✅ `UserNotFoundError` - 用户不存在异常
- ✅ `PermissionDeniedError` - 权限拒绝异常
- ✅ `ValidationError` - 验证异常
- ❌ `user_not_found_error` - 错误：类名应使用 PascalCase

### 函数命名

**规则**：使用 `snake_case`（小写字母 + 下划线），动词开头

#### CRUD 操作
- ✅ `create_user` - 创建用户
- ✅ `get_user` - 获取用户
- ✅ `get_user_by_id` - 根据 ID 获取用户
- ✅ `update_user` - 更新用户
- ✅ `delete_user` - 删除用户
- ❌ `createUser` - 错误：函数名应使用 snake_case
- ❌ `user_create` - 错误：应使用动词开头

#### 查询操作
- ✅ `list_users` - 用户列表
- ✅ `search_users` - 搜索用户
- ✅ `filter_users` - 筛选用户
- ✅ `count_users` - 统计用户数量
- ❌ `get_users` - 不推荐：列表查询应使用 `list_` 前缀

#### 业务操作
- ✅ `activate_user` - 激活用户
- ✅ `deactivate_user` - 停用用户
- ✅ `reset_password` - 重置密码
- ✅ `send_email` - 发送邮件
- ❌ `user_activate` - 错误：应使用动词开头

#### 工具函数
- ✅ `format_date` - 格式化日期
- ✅ `parse_amount` - 解析金额
- ✅ `validate_email` - 验证邮箱
- ✅ `generate_token` - 生成 Token
- ❌ `date_format` - 错误：应使用动词开头

#### 异步函数
- ✅ `async def create_user(...)` - 异步函数
- ✅ `async def get_user(...)` - 异步函数
- ✅ `async def list_users(...)` - 异步函数

### 变量命名

**规则**：使用 `snake_case`（小写字母 + 下划线）

#### 普通变量
- ✅ `user_id` - 用户 ID
- ✅ `order_no` - 订单号
- ✅ `total_amount` - 总金额
- ✅ `is_active` - 是否激活（布尔值）
- ❌ `userId` - 错误：变量名应使用 snake_case
- ❌ `orderNo` - 错误：变量名应使用 snake_case

#### 常量
- ✅ `MAX_RETRY_COUNT` - 最大重试次数（全大写 + 下划线）
- ✅ `DEFAULT_PAGE_SIZE` - 默认分页大小
- ✅ `API_BASE_URL` - API 基础 URL
- ❌ `maxRetryCount` - 错误：常量应使用全大写

#### 私有变量/方法
- ✅ `_private_var` - 私有变量（单下划线前缀）
- ✅ `_internal_method` - 私有方法（单下划线前缀）
- ✅ `__magic_method` - 魔法方法（双下划线前缀）

### 参数命名

**规则**：使用 `snake_case`，与变量命名一致，**禁止使用 Python 关键字**

```python
async def create_user(
    name: str,           # ✅ 使用 snake_case
    email: str,         # ✅ 使用 snake_case
    tenant_id: int,     # ✅ 使用 snake_case
    is_active: bool = True  # ✅ 使用 snake_case
) -> User:
    pass

# ❌ 错误示例：使用 Python 关键字
async def create_user(
    class: str,          # ❌ 错误：class 是 Python 关键字
    def: str,            # ❌ 错误：def 是 Python 关键字
    import: str,         # ❌ 错误：import 是 Python 关键字
    from: str,           # ❌ 错误：from 是 Python 关键字
    if: str,             # ❌ 错误：if 是 Python 关键字
    else: str,           # ❌ 错误：else 是 Python 关键字
    for: str,            # ❌ 错误：for 是 Python 关键字
    while: str,          # ❌ 错误：while 是 Python 关键字
    try: str,            # ❌ 错误：try 是 Python 关键字
    except: str,         # ❌ 错误：except 是 Python 关键字
    finally: str,        # ❌ 错误：finally 是 Python 关键字
    with: str,           # ❌ 错误：with 是 Python 关键字
    as: str,              # ❌ 错误：as 是 Python 关键字
    pass: str,           # ❌ 错误：pass 是 Python 关键字
    return: str,         # ❌ 错误：return 是 Python 关键字
    yield: str,          # ❌ 错误：yield 是 Python 关键字
    break: str,          # ❌ 错误：break 是 Python 关键字
    continue: str,       # ❌ 错误：continue 是 Python 关键字
    lambda: str,         # ❌ 错误：lambda 是 Python 关键字
    None: str,           # ❌ 错误：None 是 Python 关键字
    True: str,           # ❌ 错误：True 是 Python 关键字
    False: str,          # ❌ 错误：False 是 Python 关键字
    and: str,            # ❌ 错误：and 是 Python 关键字
    or: str,              # ❌ 错误：or 是 Python 关键字
    not: str,             # ❌ 错误：not 是 Python 关键字
    in: str,              # ❌ 错误：in 是 Python 关键字
    is: str,              # ❌ 错误：is 是 Python 关键字
    del: str,             # ❌ 错误：del 是 Python 关键字
    global: str,          # ❌ 错误：global 是 Python 关键字
    nonlocal: str,        # ❌ 错误：nonlocal 是 Python 关键字
    assert: str,          # ❌ 错误：assert 是 Python 关键字
    async: str,           # ❌ 错误：async 是 Python 关键字
    await: str,           # ❌ 错误：await 是 Python 关键字
):
    pass

# ✅ 正确示例：使用替代命名
async def create_user(
    class_name: str,     # ✅ 正确：使用 class_name 替代 class
    definition: str,     # ✅ 正确：使用 definition 替代 def
    import_path: str,    # ✅ 正确：使用 import_path 替代 import
    from_location: str,  # ✅ 正确：使用 from_location 替代 from
    condition: str,     # ✅ 正确：使用 condition 替代 if
    alternative: str,   # ✅ 正确：使用 alternative 替代 else
    loop_item: str,      # ✅ 正确：使用 loop_item 替代 for
    condition_check: str, # ✅ 正确：使用 condition_check 替代 while
    attempt: str,        # ✅ 正确：使用 attempt 替代 try
    exception_type: str, # ✅ 正确：使用 exception_type 替代 except
    cleanup: str,       # ✅ 正确：使用 cleanup 替代 finally
    context_manager: str, # ✅ 正确：使用 context_manager 替代 with
    alias: str,          # ✅ 正确：使用 alias 替代 as
    placeholder: str,   # ✅ 正确：使用 placeholder 替代 pass
    return_value: str,   # ✅ 正确：使用 return_value 替代 return
    generator_value: str, # ✅ 正确：使用 generator_value 替代 yield
    break_point: str,   # ✅ 正确：使用 break_point 替代 break
    continue_flag: str,  # ✅ 正确：使用 continue_flag 替代 continue
    lambda_func: str,   # ✅ 正确：使用 lambda_func 替代 lambda
    none_value: str,     # ✅ 正确：使用 none_value 替代 None
    true_value: str,     # ✅ 正确：使用 true_value 替代 True
    false_value: str,    # ✅ 正确：使用 false_value 替代 False
    and_condition: str,  # ✅ 正确：使用 and_condition 替代 and
    or_condition: str,   # ✅ 正确：使用 or_condition 替代 or
    not_condition: str, # ✅ 正确：使用 not_condition 替代 not
    in_list: str,        # ✅ 正确：使用 in_list 替代 in
    is_check: str,       # ✅ 正确：使用 is_check 替代 is
    delete_flag: str,    # ✅ 正确：使用 delete_flag 替代 del
    global_var: str,     # ✅ 正确：使用 global_var 替代 global
    nonlocal_var: str,   # ✅ 正确：使用 nonlocal_var 替代 nonlocal
    assertion: str,     # ✅ 正确：使用 assertion 替代 assert
    async_flag: str,     # ✅ 正确：使用 async_flag 替代 async
    await_result: str,  # ✅ 正确：使用 await_result 替代 await
):
    pass
```

### 避免 Python 关键字

**规则**：禁止使用 Python 关键字和保留字作为变量名、函数名、参数名、类名等

**Python 关键字列表**（Python 3.13+）：
- `False`, `None`, `True`, `and`, `as`, `assert`, `async`, `await`, `break`, `class`, `continue`, `def`, `del`, `elif`, `else`, `except`, `finally`, `for`, `from`, `global`, `if`, `import`, `in`, `is`, `lambda`, `nonlocal`, `not`, `or`, `pass`, `raise`, `return`, `try`, `while`, `with`, `yield`

**检查方法**：
```python
import keyword

# 检查是否为关键字
print(keyword.iskeyword('class'))  # True
print(keyword.iskeyword('user'))   # False

# 获取所有关键字
print(keyword.kwlist)
```

**常见冲突和替代方案**：
- ❌ `class` → ✅ `class_name`, `class_type`, `category`
- ❌ `def` → ✅ `definition`, `def_name`
- ❌ `import` → ✅ `import_path`, `import_name`
- ❌ `from` → ✅ `from_location`, `from_source`
- ❌ `if` → ✅ `condition`, `if_condition`
- ❌ `else` → ✅ `alternative`, `else_branch`
- ❌ `for` → ✅ `loop_item`, `for_item`
- ❌ `while` → ✅ `condition_check`, `while_condition`
- ❌ `try` → ✅ `attempt`, `try_block`
- ❌ `except` → ✅ `exception_type`, `except_type`
- ❌ `finally` → ✅ `cleanup`, `finally_block`
- ❌ `with` → ✅ `context_manager`, `with_context`
- ❌ `as` → ✅ `alias`, `as_alias`
- ❌ `pass` → ✅ `placeholder`, `pass_through`
- ❌ `return` → ✅ `return_value`, `return_data`
- ❌ `yield` → ✅ `generator_value`, `yield_value`
- ❌ `break` → ✅ `break_point`, `break_flag`
- ❌ `continue` → ✅ `continue_flag`, `continue_next`
- ❌ `lambda` → ✅ `lambda_func`, `lambda_expression`
- ❌ `None` → ✅ `none_value`, `null_value`
- ❌ `True` → ✅ `true_value`, `is_true`
- ❌ `False` → ✅ `false_value`, `is_false`
- ❌ `and` → ✅ `and_condition`, `and_logic`
- ❌ `or` → ✅ `or_condition`, `or_logic`
- ❌ `not` → ✅ `not_condition`, `not_logic`
- ❌ `in` → ✅ `in_list`, `in_collection`
- ❌ `is` → ✅ `is_check`, `is_condition`
- ❌ `del` → ✅ `delete_flag`, `del_flag`
- ❌ `global` → ✅ `global_var`, `global_variable`
- ❌ `nonlocal` → ✅ `nonlocal_var`, `nonlocal_variable`
- ❌ `assert` → ✅ `assertion`, `assert_check`
- ❌ `async` → ✅ `async_flag`, `is_async`
- ❌ `await` → ✅ `await_result`, `await_value`

### 枚举命名

**规则**：枚举类使用 `PascalCase`，枚举值使用 `UPPER_SNAKE_CASE`

```python
from enum import Enum

class UserStatus(str, Enum):
    """用户状态枚举"""
    ACTIVE = "active"        # ✅ 枚举值使用全大写
    INACTIVE = "inactive"
    SUSPENDED = "suspended"
    DELETED = "deleted"
```

### 类型别名

**规则**：使用 `PascalCase`

```python
from typing import List, Dict

# ✅ 类型别名使用 PascalCase
UserList = List[User]
UserDict = Dict[str, User]
```

## 🎨 前端命名规范（TypeScript/React）

### 文件命名

**规则**：组件文件使用 `PascalCase`，其他文件使用 `camelCase` 或 `kebab-case`

#### 页面文件
- ✅ `UserList.tsx` - 用户列表页面
- ✅ `UserForm.tsx` - 用户表单页面
- ✅ `OrderDetail.tsx` - 订单详情页面
- ❌ `userList.tsx` - 错误：组件文件应使用 PascalCase
- ❌ `user-list.tsx` - 错误：组件文件应使用 PascalCase

#### 组件文件
- ✅ `UserCard.tsx` - 用户卡片组件
- ✅ `UserTable.tsx` - 用户表格组件
- ✅ `BaseButton.tsx` - 基础按钮组件（Base 前缀）
- ✅ `BaseInput.tsx` - 基础输入组件（Base 前缀）
- ❌ `userCard.tsx` - 错误：组件文件应使用 PascalCase

#### 工具文件
- ✅ `userUtils.ts` - 用户工具函数
- ✅ `orderUtils.ts` - 订单工具函数
- ✅ `formatUtils.ts` - 格式化工具
- ✅ `dateUtils.ts` - 日期工具
- ❌ `UserUtils.ts` - 错误：工具文件应使用 camelCase

#### API 文件
- ✅ `userApi.ts` - 用户 API
- ✅ `orderApi.ts` - 订单 API
- ✅ `authApi.ts` - 认证 API
- ❌ `user_api.ts` - 错误：API 文件应使用 camelCase
- ❌ `UserApi.ts` - 错误：API 文件应使用 camelCase

#### 类型文件
- ✅ `userTypes.ts` - 用户类型定义
- ✅ `orderTypes.ts` - 订单类型定义
- ✅ `types.ts` - 通用类型定义
- ✅ `index.ts` - 导出文件

#### 配置文件
- ✅ `vite.config.ts` - Vite 配置文件
- ✅ `tsconfig.json` - TypeScript 配置
- ✅ `tailwind.config.js` - Tailwind CSS 配置（可选）

### 目录命名

**规则**：**统一使用 `snake_case`**（小写字母 + 下划线）⭐ **重要**

```
riveredge-frontend/
├── src/
│   ├── pages/              # 页面目录
│   │   ├── user/           # 用户模块（snake_case，单数）
│   │   │   ├── list/       # 列表页（snake_case，单数）
│   │   │   └── form/       # 表单页（snake_case，单数）
│   │   └── order/          # 订单模块（snake_case，单数）
│   ├── components/          # 组件目录（snake_case，复数）
│   │   ├── uni_table/      # 组件目录（snake_case）
│   │   ├── tenant_selector/ # 组件目录（snake_case）
│   │   └── page_tabs/      # 组件目录（snake_case）
│   ├── services/           # API 服务（snake_case，复数）
│   ├── stores/             # Zustand状态存储（snake_case，复数）
│   ├── utils/              # 工具函数（snake_case，复数）
│   └── types/              # 类型定义（snake_case，复数）
```

**命名规范说明**：
- ✅ **所有文件夹统一使用 `snake_case`**（小写字母 + 下划线）
- ✅ **功能模块文件夹使用单数**：`user/`、`role/`、`dashboard/`
- ✅ **功能集合文件夹使用复数**：`components/`、`services/`、`stores/`
- ✅ **组件文件夹使用 `snake_case`**：`uni_table/`、`tenant_selector/`、`page_tabs/`
- ❌ **禁止使用 `PascalCase`**：`UniTable/`、`TenantSelector/`
- ❌ **禁止使用 `camelCase`**：`uniTable/`、`tenantSelector/`
- ❌ **禁止使用 `kebab-case`**：`uni-table/`、`tenant-selector/`（除非是框架特殊要求）

### 组件命名

**规则**：使用 `PascalCase`（首字母大写的驼峰命名）

#### 页面组件
- ✅ `UserList` - 用户列表页面
- ✅ `UserForm` - 用户表单页面
- ✅ `OrderDetail` - 订单详情页面
- ❌ `userList` - 错误：组件名应使用 PascalCase
- ❌ `user-list` - 错误：组件名应使用 PascalCase

#### 业务组件
- ✅ `UserCard` - 用户卡片组件
- ✅ `UserTable` - 用户表格组件
- ✅ `OrderItem` - 订单项组件
- ❌ `userCard` - 错误：组件名应使用 PascalCase

#### 基础组件
- ✅ `BaseButton` - 基础按钮组件（Base 前缀）
- ✅ `BaseInput` - 基础输入组件（Base 前缀）
- ✅ `BaseTable` - 基础表格组件（Base 前缀）
- ❌ `Button` - 不推荐：可能与 Ant Design 组件冲突

#### 高阶组件（HOC）
- ✅ `withAuth` - 认证高阶组件（with 前缀）
- ✅ `withPermission` - 权限高阶组件（with 前缀）
- ✅ `withLoading` - 加载高阶组件（with 前缀）

### 函数命名

**规则**：使用 `camelCase`（小写字母开头的驼峰命名），动词开头

#### API 函数
- ✅ `getUserList` - 获取用户列表
- ✅ `getUserById` - 根据 ID 获取用户
- ✅ `createUser` - 创建用户
- ✅ `updateUser` - 更新用户
- ✅ `deleteUser` - 删除用户
- ❌ `get_user_list` - 错误：函数名应使用 camelCase
- ❌ `getUsers` - 不推荐：列表查询应使用 `getXxxList`

#### 工具函数
- ✅ `formatDate` - 格式化日期
- ✅ `parseAmount` - 解析金额
- ✅ `validateEmail` - 验证邮箱
- ✅ `debounce` - 防抖函数
- ✅ `throttle` - 节流函数
- ❌ `date_format` - 错误：函数名应使用 camelCase

#### 事件处理函数
- ✅ `handleSubmit` - 处理提交（handle 前缀）
- ✅ `handleChange` - 处理变更（handle 前缀）
- ✅ `handleClick` - 处理点击（handle 前缀）
- ✅ `onSubmit` - 提交事件（on 前缀，用于 props）
- ✅ `onChange` - 变更事件（on 前缀，用于 props）
- ❌ `submit` - 不推荐：事件处理函数应使用 handle/on 前缀

#### Hook 函数
- ✅ `useUser` - 用户 Hook（use 前缀）
- ✅ `useOrder` - 订单 Hook（use 前缀）
- ✅ `useAuth` - 认证 Hook（use 前缀）
- ✅ `usePermission` - 权限 Hook（use 前缀）
- ❌ `getUser` - 错误：Hook 应使用 use 前缀

### 变量命名

**规则**：使用 `camelCase`（小写字母开头的驼峰命名）

#### 普通变量
- ✅ `userId` - 用户 ID
- ✅ `orderNo` - 订单号
- ✅ `totalAmount` - 总金额
- ✅ `isActive` - 是否激活（布尔值，is 前缀）
- ✅ `hasPermission` - 是否有权限（has 前缀）
- ❌ `user_id` - 错误：变量名应使用 camelCase
- ❌ `order_no` - 错误：变量名应使用 camelCase

#### 常量
- ✅ `MAX_RETRY_COUNT` - 最大重试次数（全大写 + 下划线）
- ✅ `DEFAULT_PAGE_SIZE` - 默认分页大小
- ✅ `API_BASE_URL` - API 基础 URL
- ❌ `maxRetryCount` - 错误：常量应使用全大写

#### 私有变量/方法
- ✅ `_privateVar` - 私有变量（单下划线前缀，TypeScript 中较少使用）
- ✅ `#privateField` - 私有字段（# 前缀，ES2022 私有字段）

### Props 命名

**规则**：使用 `camelCase`，事件 props 使用 `on` 前缀，**禁止使用 TypeScript/JavaScript 关键字**

```tsx
interface UserCardProps {
  userId: number;           // ✅ 使用 camelCase
  userName: string;         // ✅ 使用 camelCase
  isActive: boolean;        // ✅ 布尔值使用 is 前缀
  onSubmit: () => void;    // ✅ 事件使用 on 前缀
  onChange: (value: string) => void;  // ✅ 事件使用 on 前缀
}

// ❌ 错误示例：使用 TypeScript/JavaScript 关键字
interface BadProps {
  class: string;           // ❌ 错误：class 是关键字
  function: string;        // ❌ 错误：function 是关键字
  const: string;           // ❌ 错误：const 是关键字
  let: string;            // ❌ 错误：let 是关键字
  var: string;            // ❌ 错误：var 是关键字
  if: string;             // ❌ 错误：if 是关键字
  else: string;           // ❌ 错误：else 是关键字
  for: string;            // ❌ 错误：for 是关键字
  while: string;          // ❌ 错误：while 是关键字
  try: string;            // ❌ 错误：try 是关键字
  catch: string;          // ❌ 错误：catch 是关键字
  finally: string;        // ❌ 错误：finally 是关键字
  switch: string;         // ❌ 错误：switch 是关键字
  case: string;           // ❌ 错误：case 是关键字
  default: string;        // ❌ 错误：default 是关键字
  break: string;          // ❌ 错误：break 是关键字
  continue: string;       // ❌ 错误：continue 是关键字
  return: string;         // ❌ 错误：return 是关键字
  yield: string;          // ❌ 错误：yield 是关键字
  async: string;         // ❌ 错误：async 是关键字
  await: string;         // ❌ 错误：await 是关键字
  import: string;         // ❌ 错误：import 是关键字
  export: string;         // ❌ 错误：export 是关键字
  from: string;           // ❌ 错误：from 是关键字
  as: string;             // ❌ 错误：as 是关键字
  new: string;            // ❌ 错误：new 是关键字
  this: string;           // ❌ 错误：this 是关键字
  super: string;          // ❌ 错误：super 是关键字
  extends: string;        // ❌ 错误：extends 是关键字
  implements: string;     // ❌ 错误：implements 是关键字
  interface: string;      // ❌ 错误：interface 是关键字
  type: string;           // ❌ 错误：type 是关键字
  enum: string;           // ❌ 错误：enum 是关键字
  namespace: string;      // ❌ 错误：namespace 是关键字
  module: string;         // ❌ 错误：module 是关键字
  declare: string;        // ❌ 错误：declare 是关键字
  abstract: string;       // ❌ 错误：abstract 是关键字
  static: string;         // ❌ 错误：static 是关键字
  readonly: string;       // ❌ 错误：readonly 是关键字
  public: string;         // ❌ 错误：public 是关键字
  private: string;        // ❌ 错误：private 是关键字
  protected: string;      // ❌ 错误：protected 是关键字
  get: string;            // ❌ 错误：get 是关键字
  set: string;            // ❌ 错误：set 是关键字
  constructor: string;    // ❌ 错误：constructor 是关键字
  null: string;           // ❌ 错误：null 是关键字
  undefined: string;      // ❌ 错误：undefined 是关键字
  true: string;           // ❌ 错误：true 是关键字
  false: string;          // ❌ 错误：false 是关键字
  NaN: string;           // ❌ 错误：NaN 是关键字
  Infinity: string;      // ❌ 错误：Infinity 是关键字
}

// ✅ 正确示例：使用替代命名
interface GoodProps {
  className: string;      // ✅ 正确：使用 className 替代 class
  functionName: string;   // ✅ 正确：使用 functionName 替代 function
  constantValue: string; // ✅ 正确：使用 constantValue 替代 const
  letValue: string;       // ✅ 正确：使用 letValue 替代 let
  varValue: string;      // ✅ 正确：使用 varValue 替代 var
  condition: string;      // ✅ 正确：使用 condition 替代 if
  alternative: string;    // ✅ 正确：使用 alternative 替代 else
  loopItem: string;       // ✅ 正确：使用 loopItem 替代 for
  whileCondition: string; // ✅ 正确：使用 whileCondition 替代 while
  attempt: string;        // ✅ 正确：使用 attempt 替代 try
  catchBlock: string;     // ✅ 正确：使用 catchBlock 替代 catch
  finallyBlock: string;   // ✅ 正确：使用 finallyBlock 替代 finally
  switchValue: string;    // ✅ 正确：使用 switchValue 替代 switch
  caseValue: string;      // ✅ 正确：使用 caseValue 替代 case
  defaultValue: string;  // ✅ 正确：使用 defaultValue 替代 default
  breakPoint: string;     // ✅ 正确：使用 breakPoint 替代 break
  continueFlag: string;   // ✅ 正确：使用 continueFlag 替代 continue
  returnValue: string;    // ✅ 正确：使用 returnValue 替代 return
  yieldValue: string;     // ✅ 正确：使用 yieldValue 替代 yield
  asyncFlag: string;      // ✅ 正确：使用 asyncFlag 替代 async
  awaitResult: string;    // ✅ 正确：使用 awaitResult 替代 await
  importPath: string;    // ✅ 正确：使用 importPath 替代 import
  exportName: string;     // ✅ 正确：使用 exportName 替代 export
  fromLocation: string;   // ✅ 正确：使用 fromLocation 替代 from
  alias: string;          // ✅ 正确：使用 alias 替代 as
  newInstance: string;    // ✅ 正确：使用 newInstance 替代 new
  thisContext: string;    // ✅ 正确：使用 thisContext 替代 this
  superClass: string;     // ✅ 正确：使用 superClass 替代 super
  extendsClass: string;   // ✅ 正确：使用 extendsClass 替代 extends
  implementsInterface: string; // ✅ 正确：使用 implementsInterface 替代 implements
  interfaceName: string;  // ✅ 正确：使用 interfaceName 替代 interface
  typeName: string;       // ✅ 正确：使用 typeName 替代 type
  enumName: string;       // ✅ 正确：使用 enumName 替代 enum
  namespaceName: string;  // ✅ 正确：使用 namespaceName 替代 namespace
  moduleName: string;     // ✅ 正确：使用 moduleName 替代 module
  declaration: string;    // ✅ 正确：使用 declaration 替代 declare
  abstractClass: string;  // ✅ 正确：使用 abstractClass 替代 abstract
  staticValue: string;    // ✅ 正确：使用 staticValue 替代 static
  readonlyValue: string;  // ✅ 正确：使用 readonlyValue 替代 readonly
  publicAccess: string;   // ✅ 正确：使用 publicAccess 替代 public
  privateAccess: string;  // ✅ 正确：使用 privateAccess 替代 private
  protectedAccess: string; // ✅ 正确：使用 protectedAccess 替代 protected
  getValue: string;       // ✅ 正确：使用 getValue 替代 get
  setValue: string;       // ✅ 正确：使用 setValue 替代 set
  constructorName: string; // ✅ 正确：使用 constructorName 替代 constructor
  nullValue: string;      // ✅ 正确：使用 nullValue 替代 null
  undefinedValue: string; // ✅ 正确：使用 undefinedValue 替代 undefined
  trueValue: string;      // ✅ 正确：使用 trueValue 替代 true
  falseValue: string;     // ✅ 正确：使用 falseValue 替代 false
  nanValue: string;       // ✅ 正确：使用 nanValue 替代 NaN
  infinityValue: string;  // ✅ 正确：使用 infinityValue 替代 Infinity
}
```

### 避免 TypeScript/JavaScript 关键字

**规则**：禁止使用 TypeScript/JavaScript 关键字和保留字作为变量名、函数名、参数名、类型名等

**TypeScript/JavaScript 关键字列表**：
- **控制流**：`if`, `else`, `for`, `while`, `do`, `switch`, `case`, `default`, `break`, `continue`, `return`, `yield`, `try`, `catch`, `finally`, `throw`
- **声明**：`const`, `let`, `var`, `function`, `class`, `interface`, `type`, `enum`, `namespace`, `module`, `declare`, `abstract`, `static`, `readonly`, `public`, `private`, `protected`, `get`, `set`, `constructor`
- **导入导出**：`import`, `export`, `from`, `as`
- **其他**：`new`, `this`, `super`, `extends`, `implements`, `async`, `await`, `in`, `of`, `instanceof`, `typeof`, `void`, `null`, `undefined`, `true`, `false`, `NaN`, `Infinity`

**常见冲突和替代方案**：
- ❌ `class` → ✅ `className`, `classType`, `category`
- ❌ `function` → ✅ `functionName`, `func`, `method`
- ❌ `const` → ✅ `constantValue`, `constValue`
- ❌ `let` → ✅ `letValue`, `variable`
- ❌ `var` → ✅ `varValue`, `variable`
- ❌ `if` → ✅ `condition`, `ifCondition`
- ❌ `else` → ✅ `alternative`, `elseBranch`
- ❌ `for` → ✅ `loopItem`, `forItem`
- ❌ `while` → ✅ `whileCondition`, `conditionCheck`
- ❌ `try` → ✅ `attempt`, `tryBlock`
- ❌ `catch` → ✅ `catchBlock`, `exceptionHandler`
- ❌ `finally` → ✅ `finallyBlock`, `cleanup`
- ❌ `switch` → ✅ `switchValue`, `switchCase`
- ❌ `case` → ✅ `caseValue`, `caseItem`
- ❌ `default` → ✅ `defaultValue`, `defaultOption`
- ❌ `break` → ✅ `breakPoint`, `breakFlag`
- ❌ `continue` → ✅ `continueFlag`, `continueNext`
- ❌ `return` → ✅ `returnValue`, `returnData`
- ❌ `yield` → ✅ `yieldValue`, `generatorValue`
- ❌ `async` → ✅ `asyncFlag`, `isAsync`
- ❌ `await` → ✅ `awaitResult`, `awaitValue`
- ❌ `import` → ✅ `importPath`, `importName`
- ❌ `export` → ✅ `exportName`, `exportValue`
- ❌ `from` → ✅ `fromLocation`, `fromSource`
- ❌ `as` → ✅ `alias`, `asAlias`
- ❌ `new` → ✅ `newInstance`, `newObject`
- ❌ `this` → ✅ `thisContext`, `thisInstance`
- ❌ `super` → ✅ `superClass`, `superInstance`
- ❌ `extends` → ✅ `extendsClass`, `extendsType`
- ❌ `implements` → ✅ `implementsInterface`, `implementsType`
- ❌ `interface` → ✅ `interfaceName`, `interfaceType`
- ❌ `type` → ✅ `typeName`, `typeDefinition`
- ❌ `enum` → ✅ `enumName`, `enumType`
- ❌ `namespace` → ✅ `namespaceName`, `namespaceValue`
- ❌ `module` → ✅ `moduleName`, `moduleValue`
- ❌ `declare` → ✅ `declaration`, `declareStatement`
- ❌ `abstract` → ✅ `abstractClass`, `abstractType`
- ❌ `static` → ✅ `staticValue`, `staticMember`
- ❌ `readonly` → ✅ `readonlyValue`, `readonlyProperty`
- ❌ `public` → ✅ `publicAccess`, `publicMember`
- ❌ `private` → ✅ `privateAccess`, `privateMember`
- ❌ `protected` → ✅ `protectedAccess`, `protectedMember`
- ❌ `get` → ✅ `getValue`, `getter`
- ❌ `set` → ✅ `setValue`, `setter`
- ❌ `constructor` → ✅ `constructorName`, `constructorMethod`
- ❌ `null` → ✅ `nullValue`, `nullData`
- ❌ `undefined` → ✅ `undefinedValue`, `undefinedData`
- ❌ `true` → ✅ `trueValue`, `isTrue`
- ❌ `false` → ✅ `falseValue`, `isFalse`
- ❌ `NaN` → ✅ `nanValue`, `notANumber`
- ❌ `Infinity` → ✅ `infinityValue`, `infiniteValue`

### 类型命名

**规则**：使用 `PascalCase`，接口使用 `I` 前缀（可选）

#### 接口定义
- ✅ `User` - 用户接口
- ✅ `Order` - 订单接口
- ✅ `UserListResponse` - 用户列表响应接口
- ✅ `IUser` - 用户接口（I 前缀，可选）
- ❌ `user` - 错误：类型名应使用 PascalCase

#### 类型别名
- ✅ `UserList = User[]` - 用户列表类型
- ✅ `UserId = number` - 用户 ID 类型
- ✅ `OrderStatus = 'pending' | 'processing' | 'completed'` - 订单状态类型

#### 泛型类型
- ✅ `Response<T>` - 响应类型
- ✅ `ListResponse<T>` - 列表响应类型
- ✅ `PageResponse<T>` - 分页响应类型

### 枚举命名

**规则**：枚举使用 `PascalCase`，枚举值使用 `PascalCase` 或 `UPPER_SNAKE_CASE`

```typescript
// 方式一：使用字符串枚举（推荐）
enum UserStatus {
  Active = 'active',        // ✅ 枚举值使用 PascalCase
  Inactive = 'inactive',
  Suspended = 'suspended',
  Deleted = 'deleted',
}

// 方式二：使用常量枚举
const enum UserStatus {
  ACTIVE = 'active',         // ✅ 枚举值使用全大写
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
  DELETED = 'deleted',
}
```

## 📦 插件命名规范

### 插件目录命名

**规则**：使用 `seed-` 前缀 + `kebab-case`

- ✅ `seed-example` - 示例插件
- ✅ `seed-payment-alipay` - 支付宝支付插件
- ✅ `seed-sms-aliyun` - 阿里云短信插件
- ❌ `example` - 错误：插件目录应使用 `seed-` 前缀
- ❌ `seed_example` - 错误：应使用连字符而非下划线

### 插件 ID 命名

**规则**：与目录名一致，使用 `kebab-case`

```json
{
  "id": "seed-example",           // ✅ 与目录名一致
  "id": "seed-payment-alipay",    // ✅ 与目录名一致
  "id": "seed-sms-aliyun"         // ✅ 与目录名一致
}
```

## 🗄️ 数据库命名规范

详细规范请参考：[3.数据库命名规范.md](./3.数据库命名规范.md)

### 表命名（简要）

- ✅ `users` - 用户表（复数形式）
- ✅ `orders` - 订单表（复数形式）
- ✅ `order_items` - 订单明细表（复数形式）
- ✅ `user_roles` - 用户角色关联表

### 字段命名（简要）

- ✅ `id` - 主键
- ✅ `tenant_id` - 组织 ID（所有表必须包含）
- ✅ `user_id` - 外键（表名_id）
- ✅ `created_at` - 创建时间
- ✅ `updated_at` - 更新时间
- ✅ `is_active` - 是否激活（布尔值，is 前缀）

### 避免数据库关键字

**规则**：禁止使用 PostgreSQL 关键字和保留字作为表名、字段名、索引名等

**PostgreSQL 关键字列表**（部分常见）：
- **DDL**：`CREATE`, `DROP`, `ALTER`, `TABLE`, `INDEX`, `VIEW`, `SEQUENCE`, `DATABASE`, `SCHEMA`, `TRIGGER`, `FUNCTION`, `PROCEDURE`
- **DML**：`SELECT`, `INSERT`, `UPDATE`, `DELETE`, `FROM`, `WHERE`, `JOIN`, `INNER`, `LEFT`, `RIGHT`, `FULL`, `OUTER`, `ON`, `AS`, `UNION`, `ALL`, `DISTINCT`
- **约束**：`PRIMARY`, `KEY`, `FOREIGN`, `REFERENCES`, `UNIQUE`, `NOT`, `NULL`, `DEFAULT`, `CHECK`, `CONSTRAINT`
- **查询**：`ORDER`, `BY`, `GROUP`, `HAVING`, `LIMIT`, `OFFSET`, `ASC`, `DESC`
- **聚合**：`COUNT`, `SUM`, `AVG`, `MAX`, `MIN`
- **条件**：`AND`, `OR`, `NOT`, `IN`, `EXISTS`, `BETWEEN`, `LIKE`, `ILIKE`, `IS`, `CASE`, `WHEN`, `THEN`, `ELSE`, `END`
- **其他**：`TRUE`, `FALSE`, `NULL`, `IF`, `ELSE`, `FOR`, `WHILE`, `LOOP`, `RETURN`, `RAISE`, `EXCEPTION`, `BEGIN`, `END`, `COMMIT`, `ROLLBACK`, `TRANSACTION`

**常见冲突和替代方案**：
- ❌ `order` → ✅ `order_no`, `order_number`, `order_id`（注意：`ORDER BY` 是关键字）
- ❌ `user` → ✅ `user_name`, `user_id`, `username`（注意：`USER` 在某些数据库中可能是关键字）
- ❌ `group` → ✅ `group_name`, `group_id`, `grouping`（注意：`GROUP BY` 是关键字）
- ❌ `select` → ✅ `select_value`, `select_option`, `selection`
- ❌ `from` → ✅ `from_location`, `from_source`, `from_address`
- ❌ `where` → ✅ `where_clause`, `where_condition`
- ❌ `join` → ✅ `join_type`, `join_table`
- ❌ `key` → ✅ `key_name`, `key_value`, `key_id`
- ❌ `index` → ✅ `index_name`, `index_value`, `index_id`
- ❌ `table` → ✅ `table_name`, `table_id`
- ❌ `view` → ✅ `view_name`, `view_id`
- ❌ `function` → ✅ `function_name`, `function_id`
- ❌ `procedure` → ✅ `procedure_name`, `procedure_id`
- ❌ `trigger` → ✅ `trigger_name`, `trigger_id`
- ❌ `sequence` → ✅ `sequence_name`, `sequence_id`
- ❌ `database` → ✅ `database_name`, `database_id`
- ❌ `schema` → ✅ `schema_name`, `schema_id`
- ❌ `constraint` → ✅ `constraint_name`, `constraint_id`
- ❌ `default` → ✅ `default_value`, `default_option`
- ❌ `null` → ✅ `null_value`, `null_flag`
- ❌ `true` → ✅ `true_value`, `is_true`
- ❌ `false` → ✅ `false_value`, `is_false`
- ❌ `case` → ✅ `case_value`, `case_type`
- ❌ `when` → ✅ `when_time`, `when_date`
- ❌ `then` → ✅ `then_value`, `then_result`
- ❌ `else` → ✅ `else_value`, `else_result`
- ❌ `end` → ✅ `end_time`, `end_date`
- ❌ `begin` → ✅ `begin_time`, `begin_date`
- ❌ `if` → ✅ `if_condition`, `if_flag`
- ❌ `for` → ✅ `for_item`, `for_loop`
- ❌ `while` → ✅ `while_condition`, `while_loop`
- ❌ `return` → ✅ `return_value`, `return_data`
- ❌ `raise` → ✅ `raise_flag`, `raise_error`
- ❌ `exception` → ✅ `exception_type`, `exception_message`
- ❌ `transaction` → ✅ `transaction_id`, `transaction_no`
- ❌ `commit` → ✅ `commit_time`, `commit_date`
- ❌ `rollback` → ✅ `rollback_flag`, `rollback_reason`

**检查方法**：
```sql
-- PostgreSQL 中检查关键字
SELECT word FROM pg_get_keywords() WHERE catcode = 'R';  -- 保留字
SELECT word FROM pg_get_keywords() WHERE catcode = 'C';  -- 非保留关键字（可用作列名，但不推荐）

-- 如果必须使用关键字，使用双引号转义（不推荐）
CREATE TABLE "order" (
    "user" VARCHAR(50),  -- ❌ 不推荐：使用关键字
    "group" VARCHAR(50)  -- ❌ 不推荐：使用关键字
);

-- ✅ 推荐：使用替代命名
CREATE TABLE orders (
    user_name VARCHAR(50),  -- ✅ 推荐：使用 user_name
    group_name VARCHAR(50)  -- ✅ 推荐：使用 group_name
);
```

## 📝 命名示例

### 后端完整示例

```python
# models/user.py
class User(BaseModel):
    """用户模型"""
    id = fields.IntField(pk=True)
    tenant_id = fields.IntField()
    username = fields.CharField(max_length=50)
    email = fields.CharField(max_length=100)
    is_active = fields.BooleanField(default=True)

# schemas/user_schema.py
class UserCreate(BaseModel):
    """用户创建 Schema"""
    username: str
    email: str
    tenant_id: int

class UserResponse(BaseModel):
    """用户响应 Schema"""
    id: int
    username: str
    email: str
    is_active: bool

# services/user_service.py
class UserService:
    """用户服务"""
    
    async def create_user(self, data: UserCreate) -> User:
        """创建用户"""
        pass
    
    async def get_user_by_id(self, user_id: int) -> User:
        """根据 ID 获取用户"""
        pass
    
    async def list_users(self, page: int, page_size: int) -> List[User]:
        """获取用户列表"""
        pass

# api/v1/users.py
@router.post("", response_model=UserResponse)
async def create_user(
    user: UserCreate,
    current_user = Depends(get_current_user)
):
    """创建用户"""
    return await UserService().create_user(user)
```

### 前端完整示例

```tsx
// types/userTypes.ts
export interface User {
  id: number;
  username: string;
  email: string;
  isActive: boolean;
}

export interface UserListResponse {
  items: User[];
  total: number;
  page: number;
  pageSize: number;
}

// services/userApi.ts
export async function getUserList(params: {
  page: number;
  pageSize: number;
  keyword?: string;
}): Promise<UserListResponse> {
  return request('/api/v1/users', { params });
}

export async function createUser(data: UserCreate): Promise<User> {
  return request('/api/v1/users', {
    method: 'POST',
    data,
  });
}

// pages/user/UserList.tsx
export default function UserList() {
  const handleSubmit = () => {
    // 处理提交
  };
  
  return (
    <ProTable
      request={getUserList}
      // ...
    />
  );
}
```

## ✅ 检查清单

### 代码审查时检查

- [ ] 文件命名是否符合规范？
- [ ] 类命名是否符合规范？
- [ ] 函数命名是否符合规范？
- [ ] 变量命名是否符合规范？
- [ ] 常量是否使用全大写？
- [ ] 组件命名是否符合规范？
- [ ] 类型命名是否符合规范？
- [ ] 命名是否清晰表达意图？
- [ ] **是否避免了 Python 关键字？** ⭐ **重要**
- [ ] **是否避免了 TypeScript/JavaScript 关键字？** ⭐ **重要**
- [ ] **是否避免了数据库关键字？** ⭐ **重要**
- [ ] **是否避免了框架内置关键字？**（如 React、Ant Design 等）

### 关键字检查工具

**Python 关键字检查**：
```python
import keyword

# 检查变量名是否为关键字
def is_keyword(name: str) -> bool:
    return keyword.iskeyword(name)

# 使用示例
print(is_keyword('class'))  # True
print(is_keyword('user'))   # False
```

**TypeScript/JavaScript 关键字检查**：
```typescript
// 常见关键字列表（部分）
const KEYWORDS = [
  'class', 'function', 'const', 'let', 'var',
  'if', 'else', 'for', 'while', 'try', 'catch',
  'import', 'export', 'from', 'as', 'new', 'this',
  'interface', 'type', 'enum', 'namespace', 'module',
  'async', 'await', 'return', 'yield', 'break', 'continue',
  'null', 'undefined', 'true', 'false', 'NaN', 'Infinity'
];

function isKeyword(name: string): boolean {
  return KEYWORDS.includes(name);
}
```

**PostgreSQL 关键字检查**：
```sql
-- 查询所有保留字
SELECT word FROM pg_get_keywords() WHERE catcode = 'R';

-- 查询非保留关键字（可用作列名，但不推荐）
SELECT word FROM pg_get_keywords() WHERE catcode = 'C';
```

## 📚 相关文档

- [4.注释规范.md](./4.注释规范.md) - 注释规范
- [3.数据库命名规范.md](./3.数据库命名规范.md) - 数据库命名规范
- [1.框架命名哲学.md](./1.框架命名哲学.md) - 框架命名哲学

