# 页面开发标准模板规范

## 📋 概述

本文档定义了 RiverEdge SaaS 多组织框架的**页面开发标准模板**，以**组织管理**（`superadmin/tenants`）为参考标准，确保所有页面的开发风格和交互方式保持一致。

**⚠️ 重要原则**：
- ✅ **默认遵循标准模板**：所有新开发的页面必须遵循组织管理的标准模式
- ✅ **特殊情况单独处理**：如有特殊需求，在标准模板基础上进行扩展，并在代码中明确标注
- ✅ **保持一致性**：相同功能的页面应保持相同的实现方式

---

## 🎯 标准模板结构

### 标准页面目录结构

```
pages/
  {module}/
    list/
      index.tsx      # 列表页面（标准模板）
    detail/
      index.tsx     # 详情页面（标准模板）
    form/
      index.tsx     # 表单页面（可选，如果使用 Modal 则不需要）
```

**参考标准**：`pages/operations/tenants/`

**⚠️ 重要说明**：
- ✅ 所有组织管理统一在平台级（`operations/tenants`）进行
- ❌ 不创建普通租户的组织管理页面（`pages/tenant/` 下的组织管理页面已删除）
- ✅ 只有超级管理员可以管理组织
- ✅ **统一层级的功能放在同一个文件夹下**：
  - `operations/` - 包含所有运营管理功能（组织管理、监控、套餐等）
  - `system/` - 包含所有系统级功能（用户管理、角色管理等）

---

## 📄 1. 列表页面（List）标准模板

### 1.1 标准功能

**必须包含的功能**：
- ✅ 数据列表展示（使用 `UniTable`）
- ✅ 新建功能（使用 **Modal**，不跳转新页面）
- ✅ 编辑功能（使用 **Modal**，不跳转新页面）
- ✅ 详情查看（使用 **Drawer**，侧边抽屉）
- ✅ 删除功能（批量删除）
- ✅ 导入/导出功能（可选）
- ✅ 搜索和筛选
- ✅ 分页和排序

### 1.2 标准实现方式

#### 1.2.1 新建和编辑 - 使用 Modal

**标准模式**：使用 Modal 弹窗，不跳转新页面

```typescript
// ✅ 标准实现
const [modalVisible, setModalVisible] = useState(false);
const [isEdit, setIsEdit] = useState(false);
const [currentId, setCurrentId] = useState<number | null>(null);

// 新建
const handleCreate = () => {
  setIsEdit(false);
  setCurrentId(null);
  setModalVisible(true);
  formRef.current?.resetFields();
};

// 编辑
const handleEdit = async (keys: React.Key[]) => {
  if (keys.length === 1) {
    const id = Number(keys[0]);
    setIsEdit(true);
    setCurrentId(id);
    setModalVisible(true);
    // 加载数据...
  }
};

// Modal 组件
<Modal
  title={isEdit ? '编辑XXX' : '新建XXX'}
  open={modalVisible}
  onCancel={handleCloseModal}
  footer={null}
  width={800}
  destroyOnClose
>
  <ProForm
    formRef={formRef}
    layout="vertical"
    grid={true}
    rowProps={{ gutter: 16 }}
    onFinish={handleSubmit}
  >
    {/* 表单项，使用 colProps={{ span: 12 }} 实现两栏布局 */}
  </ProForm>
</Modal>
```

**参考实现**：`pages/superadmin/tenants/list/index.tsx`

#### 1.2.2 详情查看 - 使用 Drawer

**标准模式**：使用 Drawer 侧边抽屉

```typescript
// ✅ 标准实现
const [drawerVisible, setDrawerVisible] = useState(false);
const [currentId, setCurrentId] = useState<number | null>(null);
const [detailData, setDetailData] = useState<DataType | null>(null);

const handleOpenDetail = async (id: number) => {
  setCurrentId(id);
  setDrawerVisible(true);
  // 加载详情数据...
};

<Drawer
  title="XXX详情"
  width={720}
  open={drawerVisible}
  onClose={handleCloseDetail}
>
  <ProDescriptions
    dataSource={detailData}
    column={2}
    columns={[...]}
  />
</Drawer>
```

**参考实现**：`pages/superadmin/tenants/list/index.tsx`

#### 1.2.3 表单布局 - 两栏布局

**标准模式**：Modal 中的表单使用两栏布局，单个字段 label 和 input 垂直排列

```typescript
<ProForm
  layout="vertical"        // label 在上，input 在下
  grid={true}              // 启用网格布局
  rowProps={{ gutter: 16 }} // 列间距
>
  <ProFormText
    name="field1"
    label="字段1"
    colProps={{ span: 12 }}  // 两栏布局，每栏占 12 列
  />
  <ProFormText
    name="field2"
    label="字段2"
    colProps={{ span: 12 }}
  />
</ProForm>
```

**参考实现**：`pages/superadmin/tenants/list/index.tsx`

### 1.3 标准代码结构

```typescript
/**
 * XXX列表页面
 * 
 * 用于XXX的查看和管理。
 */

import React, { useRef, useState, useEffect } from 'react';
import { ActionType, ProColumns, ProDescriptions, ProForm, ... } from '@ant-design/pro-components';
import { App, Popconfirm, Button, Tag, Space, Drawer, Modal } from 'antd';
import { UniTable } from '@/components/UniTable';
import { ... } from '@/services/xxx';

/**
 * XXX列表页面组件
 */
const XxxList: React.FC = () => {
  const { message } = App.useApp();
  const actionRef = useRef<ActionType>(null);
  const formRef = useRef<ProFormInstance>();
  
  // 状态管理
  const [selectedRowKeys, setSelectedRowKeys] = useState<React.Key[]>([]);
  const [drawerVisible, setDrawerVisible] = useState(false);
  const [modalVisible, setModalVisible] = useState(false);
  const [isEdit, setIsEdit] = useState(false);
  const [currentId, setCurrentId] = useState<number | null>(null);
  
  // 新建
  const handleCreate = () => { ... };
  
  // 编辑
  const handleEdit = async (keys: React.Key[]) => { ... };
  
  // 删除
  const handleDelete = async (keys: React.Key[]) => { ... };
  
  // 详情
  const handleOpenDetail = (id: number) => { ... };
  
  // 表格列定义
  const columns: ProColumns<DataType>[] = [ ... ];
  
  return (
    <>
      <UniTable
        actionRef={actionRef}
        columns={columns}
        request={...}
        showCreateButton={true}
        onCreate={handleCreate}
        showEditButton={true}
        onEdit={handleEdit}
        showDeleteButton={true}
        onDelete={handleDelete}
      />
      
      {/* 详情 Drawer */}
      <Drawer ... />
      
      {/* 新建/编辑 Modal */}
      <Modal ... />
    </>
  );
};

export default XxxList;
```

---

## 📄 2. 详情页面（Detail）标准模板

### 2.1 标准功能

**必须包含的功能**：
- ✅ 数据详情展示（使用 `ProDescriptions`）
- ✅ 返回按钮
- ✅ 操作按钮（编辑、删除等，根据业务需求）
- ✅ 加载状态

### 2.2 标准实现方式

```typescript
/**
 * XXX详情页面
 * 
 * 用于查看XXX详细信息。
 */

import React, { useEffect, useState } from 'react';
import { ProDescriptions } from '@ant-design/pro-components';
import { PageContainer } from '@ant-design/pro-components';
import { Button, Tag, message, Space } from 'antd';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { getXxxById, Xxx } from '@/services/xxx';

const XxxDetail: React.FC = () => {
  const navigate = useNavigate();
  const [searchParams] = useSearchParams();
  const id = searchParams.get('id');
  const [loading, setLoading] = useState(false);
  const [data, setData] = useState<Xxx | null>(null);

  useEffect(() => {
    if (id) {
      loadData();
    }
  }, [id]);

  const loadData = async () => {
    if (!id) return;
    setLoading(true);
    try {
      const result = await getXxxById(Number(id));
      setData(result);
    } catch (error: any) {
      message.error(error.message || '加载数据失败');
    } finally {
      setLoading(false);
    }
  };

  return (
    <PageContainer
      title="XXX详情"
      breadcrumb={false}
      extra={
        <Space>
          <Button onClick={() => navigate(-1)}>返回</Button>
          {/* 其他操作按钮 */}
        </Space>
      }
    >
      <ProDescriptions<Xxx>
        dataSource={data}
        loading={loading}
        column={2}
        columns={[
          // 字段定义
        ]}
      />
    </PageContainer>
  );
};

export default XxxDetail;
```

**参考实现**：`pages/superadmin/tenants/detail/index.tsx`

---

## 📄 3. 表单页面（Form）标准模板

### 3.1 使用场景

**⚠️ 注意**：
- ✅ **默认使用 Modal**：新建和编辑功能应使用 Modal，不创建独立的表单页面
- ⚠️ **特殊情况使用独立页面**：只有在以下情况下才创建独立的表单页面：
  - 表单非常复杂（字段 > 15 个）
  - 需要多步骤向导流程
  - 需要大量辅助信息展示

### 3.2 标准实现方式

如果确实需要独立的表单页面，参考以下结构：

```typescript
/**
 * XXX表单页面
 * 
 * 用于新增和编辑XXX信息。
 */

import React, { useEffect, useState, useRef } from 'react';
import { ProForm, ProFormText, ... } from '@ant-design/pro-components';
import { message } from 'antd';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { getXxxById, createXxx, updateXxx, Xxx } from '@/services/xxx';

const XxxForm: React.FC = () => {
  const navigate = useNavigate();
  const [searchParams] = useSearchParams();
  const id = searchParams.get('id');
  const [loading, setLoading] = useState(false);
  const formRef = useRef<ProFormInstance>();

  useEffect(() => {
    if (id) {
      loadData();
    }
  }, [id]);

  const handleSubmit = async (values: any) => {
    try {
      if (id) {
        await updateXxx(Number(id), values);
        message.success('更新成功');
      } else {
        await createXxx(values);
        message.success('创建成功');
      }
      navigate('/xxx/list');
    } catch (error: any) {
      message.error(error.message || '操作失败');
    }
  };

  return (
    <ProForm
      formRef={formRef}
      loading={loading}
      onFinish={handleSubmit}
    >
      {/* 表单项 */}
    </ProForm>
  );
};

export default XxxForm;
```

---

## 🎨 4. UI/UX 标准规范

### 4.1 Modal 标准配置

```typescript
<Modal
  title={isEdit ? '编辑XXX' : '新建XXX'}
  open={modalVisible}
  onCancel={handleCloseModal}
  footer={null}              // 使用 ProForm 的 submitter
  width={800}                // 标准宽度
  destroyOnClose             // 关闭时销毁内容
>
```

### 4.2 Drawer 标准配置

```typescript
<Drawer
  title="XXX详情"
  width={720}                // 标准宽度
  open={drawerVisible}
  onClose={handleCloseDetail}
>
```

### 4.3 表单布局标准

```typescript
<ProForm
  layout="vertical"         // label 在上，input 在下
  grid={true}               // 启用网格布局
  rowProps={{ gutter: 16 }} // 列间距 16px
>
  {/* 表单项使用 colProps={{ span: 12 }} 实现两栏布局 */}
</ProForm>
```

### 4.4 按钮标准

- **新建按钮**：`type="primary"`，图标 `PlusOutlined`
- **编辑按钮**：`type="link"`，图标 `EditOutlined`
- **删除按钮**：`danger`，图标 `DeleteOutlined`
- **详情按钮**：`type="link"`，文本 "详情"

---

## 📝 5. 特殊情况处理

### 5.1 何时需要特殊处理

以下情况可以在标准模板基础上进行扩展：

1. **复杂表单**：字段 > 15 个，使用独立表单页面
2. **多步骤流程**：使用 `Steps` 组件
3. **特殊交互**：如拖拽排序、树形选择等
4. **业务特定功能**：如审核流程、审批流程等

### 5.2 特殊处理标注

在代码中明确标注特殊情况：

```typescript
/**
 * XXX列表页面
 * 
 * ⚠️ 特殊情况：
 * - 使用独立表单页面（字段过多，> 15 个）
 * - 包含多步骤向导流程
 */

// 或者使用注释
// ⚠️ 特殊情况：此页面使用独立表单页面，因为表单字段过多
```

---

## ✅ 6. 检查清单

开发新页面时，请检查以下项目：

### 列表页面检查清单

- [ ] 使用 `UniTable` 组件
- [ ] 新建功能使用 Modal（不跳转新页面）
- [ ] 编辑功能使用 Modal（不跳转新页面）
- [ ] 详情查看使用 Drawer
- [ ] Modal 表单使用两栏布局（`colProps={{ span: 12 }}`）
- [ ] Modal 表单使用垂直布局（`layout="vertical"`）
- [ ] 包含删除功能（批量删除）
- [ ] 包含搜索和筛选功能
- [ ] 包含分页和排序功能

### 详情页面检查清单

- [ ] 使用 `ProDescriptions` 组件
- [ ] 使用 `PageContainer` 包裹
- [ ] 包含返回按钮
- [ ] 包含加载状态
- [ ] 详情字段使用两栏布局（`column={2}`）

### 表单页面检查清单

- [ ] 默认使用 Modal（不创建独立页面）
- [ ] 如使用独立页面，需明确标注原因
- [ ] 表单验证规则完整
- [ ] 错误提示清晰

---

## 📚 7. 参考实现

### 标准参考实现

**组织管理（标准模板）**：
- 列表页面：`pages/operations/tenants/list/index.tsx`
- 详情页面：`pages/operations/tenants/detail/index.tsx`

**⚠️ 重要说明**：
- ✅ **统一平台级管理**：所有组织管理统一在平台级（`operations/tenants`）进行
- ❌ **不创建普通租户组织管理页面**：普通租户不需要组织管理功能，所有组织由平台管理统一管理
- ✅ **权限分离**：只有超级管理员可以管理组织，普通用户只能使用组织功能
- ✅ **目录结构规范**：
  - `operations/` - 运营管理功能（组织管理、监控、套餐等）
  - `system/` - 系统级功能（用户管理、角色管理等）

### 特殊情况参考

如有特殊情况，请在代码中明确标注，并说明原因。

---

## 🔄 8. 更新记录

- **2025-01-XX**：创建标准模板规范，以组织管理为参考标准
- **2025-01-XX**：统一页面目录结构：
  - 组织管理移动到 `operations/tenants/`
  - 用户和角色管理移动到 `system/user/` 和 `system/role/`
  - 明确统一层级的功能放在同一个文件夹下

---

## 📖 相关文档

- [项目结构规范](./5.项目结构规范.md)
- [API设计规范](./6.API设计规范.md)
- [错误处理规范](./7.错误处理规范.md)

