# RiverEdge SaaS 多组织框架 - 注释规范

## 📋 概述

本文档定义 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的**注释规范**，确保代码注释清晰、完整，提升代码可读性和可维护性。

## 🎯 注释原则

1. **必要性**：注释应该解释"为什么"而不是"是什么"
2. **清晰性**：注释应该清晰、简洁、准确
3. **完整性**：关键代码应该有完整的注释
4. **及时性**：代码变更时同步更新注释
5. **中文优先**：使用中文注释，便于团队理解
6. **日期规范**：**文件头注释中的日期必须使用当前系统时间**，不要硬编码日期

### 📅 日期获取方法

**重要**：所有文件头注释中的日期字段应使用当前系统日期，格式为 `YYYY-MM-DD`。

**使用流程**：
1. **先获取系统时间**：使用以下命令获取当前系统日期
2. **硬编码到注释**：将获取到的准确日期写入文件头注释中

**获取系统日期的方法**：

**⚠️ 重要**：**绝对禁止使用 PowerShell**，必须使用以下替代方案：

```bash
# Git Bash（推荐）⭐
date +%Y-%m-%d

# Windows CMD
date /t

# Linux/macOS
date +%Y-%m-%d

# Python
python -c "from datetime import date; print(date.today().strftime('%Y-%m-%d'))"
```

```python
# Python 代码中
from datetime import date
date.today().strftime('%Y-%m-%d')
```

```typescript
// TypeScript/JavaScript
new Date().toISOString().split('T')[0]
```

**注意**：
- 创建或修改文件时，**先运行命令获取系统时间**，然后将获取到的准确日期**硬编码**写入注释
- 不要使用占位符或变量，应直接写入准确的日期字符串
- 确保日期信息的准确性和时效性

## 🔧 后端注释规范（Python）

### 文件头注释

**规则**：每个文件开头应包含文件说明

**获取当前系统日期**：
**⚠️ 重要**：**绝对禁止使用 PowerShell**，必须使用以下替代方案：

```bash
# Git Bash（推荐）⭐
date +%Y-%m-%d

# Python
from datetime import date
date.today().strftime('%Y-%m-%d')
```

**注释模板**：
```python
"""
用户模型模块

提供用户数据模型定义，包括用户基本信息、状态管理等。

Author: Luigi Lu
Date: 2025-11-17  # 使用系统时间，格式：YYYY-MM-DD（创建文件时获取当前系统日期）
"""

from tortoise.models import Model
from tortoise import fields
```

### 模块注释

**规则**：模块级注释说明模块用途

```python
"""
用户管理模块

包含用户相关的 API、Service、Schema 等。
"""

from fastapi import APIRouter
from services.user_service import UserService
```

### 类注释

**规则**：使用三引号文档字符串，包含类说明、属性说明、使用示例

#### 基础类注释

```python
class UserService:
    """
    用户服务类
    
    提供用户的 CRUD 操作和业务逻辑处理。
    """
```

#### 完整类注释（推荐）

```python
class UserService:
    """
    用户服务类
    
    提供用户的 CRUD 操作和业务逻辑处理，包括：
    - 用户创建、查询、更新、删除
    - 用户状态管理（激活、停用）
    - 用户权限验证
    
    Attributes:
        db: 数据库连接对象
        
    Example:
        >>> service = UserService()
        >>> user = await service.create_user(
        ...     name="张三",
        ...     email="zhangsan@example.com",
        ...     tenant_id=1
        ... )
    """
```

#### 模型类注释

```python
class User(BaseModel):
    """
    用户模型
    
    存储用户基本信息，包括用户名、邮箱、状态等。
    所有用户数据都包含组织隔离字段（tenant_id）。
    
    Attributes:
        id: 用户 ID（主键）
        tenant_id: 组织 ID（用于多组织隔离）
        username: 用户名（组织内唯一）
        email: 用户邮箱（全局唯一）
        is_active: 是否激活
        created_at: 创建时间
        updated_at: 更新时间
    """
    id = fields.IntField(pk=True)
    tenant_id = fields.IntField(index=True)
    username = fields.CharField(max_length=50)
    email = fields.CharField(max_length=100, unique=True)
    is_active = fields.BooleanField(default=True)
    created_at = fields.DatetimeField(auto_now_add=True)
    updated_at = fields.DatetimeField(auto_now=True)
```

#### Schema 类注释

```python
class UserCreate(BaseModel):
    """
    用户创建 Schema
    
    用于接收用户创建请求的数据验证。
    
    Attributes:
        username: 用户名（必填，3-50 字符）
        email: 用户邮箱（必填，邮箱格式验证）
        password: 用户密码（必填，至少 8 位）
        tenant_id: 组织 ID（必填）
    """
    username: str = Field(..., min_length=3, max_length=50)
    email: str = Field(..., regex=r'^[\w\.-]+@[\w\.-]+\.\w+$')
    password: str = Field(..., min_length=8)
    tenant_id: int
```

### 函数注释

**规则**：使用三引号文档字符串，包含函数说明、参数说明、返回值说明、异常说明、使用示例

#### 基础函数注释

```python
async def create_user(self, name: str, email: str) -> User:
    """创建用户"""
    pass
```

#### 完整函数注释（推荐）

```python
async def create_user(
    self,
    name: str,
    email: str,
    tenant_id: int
) -> User:
    """
    创建用户
    
    创建新用户并保存到数据库。如果邮箱已存在，则抛出异常。
    
    Args:
        name: 用户名称（必填，3-50 字符）
        email: 用户邮箱（必填，邮箱格式，全局唯一）
        tenant_id: 组织 ID（必填，用于多组织隔离）
        
    Returns:
        User: 创建的用户对象
        
    Raises:
        ValueError: 当邮箱已存在时抛出
        ValidationError: 当数据验证失败时抛出
        
    Example:
        >>> service = UserService()
        >>> user = await service.create_user(
        ...     name="张三",
        ...     email="zhangsan@example.com",
        ...     tenant_id=1
        ... )
        >>> print(user.id)
        1
    """
```

#### 查询函数注释

```python
async def list_users(
    self,
    page: int = 1,
    page_size: int = 10,
    keyword: str = None,
    tenant_id: int = None
) -> dict:
    """
    获取用户列表
    
    支持分页、关键词搜索和组织过滤。
    
    Args:
        page: 页码（默认 1，从 1 开始）
        page_size: 每页数量（默认 10，最大 100）
        keyword: 关键词搜索（可选，搜索用户名和邮箱）
        tenant_id: 组织 ID（可选，用于组织过滤）
        
    Returns:
        dict: 包含以下字段的字典
            - items: 用户列表
            - total: 总数量
            - page: 当前页码
            - page_size: 每页数量
            
    Example:
        >>> result = await service.list_users(
        ...     page=1,
        ...     page_size=20,
        ...     keyword="张三",
        ...     tenant_id=1
        ... )
        >>> print(result['total'])
        10
    """
```

#### 业务函数注释

```python
async def activate_user(self, user_id: int) -> User:
    """
    激活用户
    
    将用户状态设置为激活，并记录操作日志。
    
    Args:
        user_id: 用户 ID
        
    Returns:
        User: 更新后的用户对象
        
    Raises:
        UserNotFoundError: 当用户不存在时抛出
        
    Example:
        >>> user = await service.activate_user(1)
        >>> print(user.is_active)
        True
    """
```

### 方法注释

**规则**：与函数注释相同

```python
class UserService:
    async def get_user_by_id(self, user_id: int) -> Optional[User]:
        """
        根据 ID 获取用户
        
        Args:
            user_id: 用户 ID
            
        Returns:
            Optional[User]: 用户对象，如果不存在则返回 None
        """
```

### 属性注释

**规则**：使用行内注释或属性文档字符串

```python
class User(BaseModel):
    """用户模型"""
    
    id = fields.IntField(pk=True)  # 用户 ID（主键）
    tenant_id = fields.IntField(index=True)  # 组织 ID（用于多组织隔离）
    username = fields.CharField(max_length=50)  # 用户名（组织内唯一）
    email = fields.CharField(max_length=100, unique=True)  # 用户邮箱（全局唯一）
    is_active = fields.BooleanField(default=True)  # 是否激活
```

### 行内注释

**规则**：解释复杂逻辑，使用 `#` 注释

```python
# 获取当前组织 ID（从 JWT Token 或请求头）
tenant_id = get_current_tenant_id()

# 如果组织 ID 为空，抛出异常
if not tenant_id:
    raise ValueError("组织 ID 不能为空")

# 查询用户列表（自动过滤组织）
users = await User.filter(tenant_id=tenant_id).all()

# 计算总金额（避免浮点数精度问题）
total_amount = sum(item.amount for item in order.items)
```

### 复杂逻辑注释

**规则**：对复杂算法或业务逻辑添加详细注释

```python
async def calculate_order_total(self, order_id: int) -> Decimal:
    """
    计算订单总金额
    
    计算逻辑：
    1. 获取订单所有明细项
    2. 计算每项金额（数量 × 单价）
    3. 应用折扣（如果有）
    4. 累加所有明细项金额
    5. 返回总金额
    
    Args:
        order_id: 订单 ID
        
    Returns:
        Decimal: 订单总金额
    """
    # 1. 获取订单及其明细
    order = await Order.get(id=order_id).prefetch_related('items')
    
    # 2. 计算每项金额并累加
    total = Decimal('0')
    for item in order.items:
        item_amount = item.quantity * item.price
        total += item_amount
    
    # 3. 应用折扣（如果有）
    if order.discount_rate:
        discount = total * (order.discount_rate / 100)
        total -= discount
    
    return total
```

### TODO 注释

**规则**：使用 `TODO`、`FIXME`、`NOTE`、`WARNING` 等标签

```python
# TODO: 添加用户头像上传功能
# TODO: 实现用户批量导入功能
# FIXME: 修复时区问题，当前使用 UTC 时间
# NOTE: 此方法需要优化性能，当用户数量超过 10000 时可能较慢
# WARNING: 此方法会删除所有关联数据，请谨慎使用
```

### 类型提示注释

**规则**：使用类型提示，减少注释负担

```python
# ✅ 推荐：使用类型提示
async def create_user(
    name: str,
    email: str,
    tenant_id: int
) -> User:
    """创建用户"""
    pass

# ❌ 不推荐：在注释中说明类型
async def create_user(name, email, tenant_id):
    """
    创建用户
    
    Args:
        name: str - 用户名称
        email: str - 用户邮箱
        tenant_id: int - 组织 ID
    """
    pass
```

## 🎨 前端注释规范（TypeScript/React）

### 文件头注释

**规则**：每个文件开头应包含文件说明

**获取当前系统日期**：
**⚠️ 重要**：**绝对禁止使用 PowerShell**，必须使用以下替代方案：

```bash
# Git Bash（推荐）⭐
date +%Y-%m-%d

# Node.js/TypeScript
new Date().toISOString().split('T')[0]
```

**注释模板**：
```typescript
/**
 * 用户列表页面
 * 
 * 提供用户列表展示、搜索、筛选、分页等功能。
 * 
 * @author Luigi Lu
 * @date 2025-11-17  // 使用系统时间，格式：YYYY-MM-DD（创建文件时获取当前系统日期）
 */

import React from 'react';
import { ProTable } from '@ant-design/pro-components';
```

### 组件注释

**规则**：使用 JSDoc 格式，包含组件说明、Props 说明、使用示例

#### 基础组件注释

```tsx
interface UserCardProps {
  userId: number;
  userName: string;
}

export default function UserCard({ userId, userName }: UserCardProps) {
  return <div>{userName}</div>;
}
```

#### 完整组件注释（推荐）

```tsx
/**
 * 用户卡片组件
 * 
 * 用于展示用户基本信息，包括头像、姓名、邮箱等。
 * 
 * @param props - 组件属性
 * @param props.userId - 用户 ID
 * @param props.userName - 用户名称
 * @param props.email - 用户邮箱（可选）
 * @param props.onClick - 点击事件回调（可选）
 * 
 * @example
 * ```tsx
 * <UserCard
 *   userId={1}
 *   userName="张三"
 *   email="zhangsan@example.com"
 *   onClick={() => console.log('点击了用户卡片')}
 * />
 * ```
 */
interface UserCardProps {
  userId: number;
  userName: string;
  email?: string;
  onClick?: () => void;
}

export default function UserCard({
  userId,
  userName,
  email,
  onClick,
}: UserCardProps) {
  return (
    <div onClick={onClick}>
      <h3>{userName}</h3>
      {email && <p>{email}</p>}
    </div>
  );
}
```

### 函数注释

**规则**：使用 JSDoc 格式，包含函数说明、参数说明、返回值说明、使用示例

#### API 函数注释

```typescript
/**
 * 获取用户列表
 * 
 * 支持分页、关键词搜索和组织过滤。
 * 
 * @param params - 查询参数
 * @param params.page - 页码（默认 1，从 1 开始）
 * @param params.pageSize - 每页数量（默认 10，最大 100）
 * @param params.keyword - 关键词搜索（可选，搜索用户名和邮箱）
 * @param params.tenantId - 组织 ID（可选，用于组织过滤）
 * @returns 用户列表响应数据
 * 
 * @example
 * ```typescript
 * const result = await getUserList({
 *   page: 1,
 *   pageSize: 20,
 *   keyword: "张三",
 *   tenantId: 1
 * });
 * console.log(result.items);
 * ```
 * 
 * @throws {Error} 当请求失败时抛出错误
 */
export async function getUserList(params: {
  page: number;
  pageSize: number;
  keyword?: string;
  tenantId?: number;
}): Promise<UserListResponse> {
  return request('/api/v1/users', { params });
}
```

#### 工具函数注释

```typescript
/**
 * 格式化日期
 * 
 * 将日期对象格式化为指定格式的字符串。
 * 
 * @param date - 日期对象或时间戳
 * @param format - 格式化模板（默认 'YYYY-MM-DD HH:mm:ss'）
 * @returns 格式化后的日期字符串
 * 
 * @example
 * ```typescript
 * formatDate(new Date(), 'YYYY-MM-DD');
 * // => '2024-01-01'
 * 
 * formatDate(1704067200000, 'YYYY年MM月DD日');
 * // => '2024年01月01日'
 * ```
 */
export function formatDate(
  date: Date | number,
  format: string = 'YYYY-MM-DD HH:mm:ss'
): string {
  return dayjs(date).format(format);
}
```

#### 事件处理函数注释

```tsx
/**
 * 处理表单提交
 * 
 * 验证表单数据并提交到服务器。
 * 
 * @param values - 表单数据
 * @returns Promise<void>
 * 
 * @example
 * ```tsx
 * const handleSubmit = async (values: UserFormData) => {
 *   await createUser(values);
 *   message.success('创建成功');
 * };
 * ```
 */
const handleSubmit = async (values: UserFormData) => {
  try {
    await createUser(values);
    message.success('创建成功');
  } catch (error) {
    message.error('创建失败');
  }
};
```

### Hook 注释

**规则**：使用 JSDoc 格式，说明 Hook 用途和返回值

```typescript
/**
 * 用户数据 Hook
 * 
 * 获取用户列表数据，支持自动刷新和缓存。
 * 
 * @param params - 查询参数
 * @param params.page - 页码
 * @param params.pageSize - 每页数量
 * @param params.keyword - 关键词搜索
 * @returns Hook 返回值
 *   - data: 用户列表数据
 *   - loading: 加载状态
 *   - error: 错误信息
 *   - refresh: 刷新函数
 * 
 * @example
 * ```tsx
 * const { data, loading, refresh } = useUserList({
 *   page: 1,
 *   pageSize: 10,
 *   keyword: "张三"
 * });
 * ```
 */
export function useUserList(params: UserListParams) {
  // ...
}
```

### 类型注释

**规则**：使用 JSDoc 格式，说明类型用途和字段含义

```typescript
/**
 * 用户接口
 * 
 * 定义用户数据结构，包括基本信息、状态等。
 */
export interface User {
  /** 用户 ID（主键） */
  id: number;
  
  /** 组织 ID（用于多组织隔离） */
  tenantId: number;
  
  /** 用户名（组织内唯一） */
  username: string;
  
  /** 用户邮箱（全局唯一） */
  email: string;
  
  /** 是否激活 */
  isActive: boolean;
  
  /** 创建时间 */
  createdAt: string;
  
  /** 更新时间 */
  updatedAt: string;
}

/**
 * 用户列表响应接口
 * 
 * 定义用户列表 API 的响应数据结构。
 */
export interface UserListResponse {
  /** 用户列表 */
  items: User[];
  
  /** 总数量 */
  total: number;
  
  /** 当前页码 */
  page: number;
  
  /** 每页数量 */
  pageSize: number;
}
```

### 行内注释

**规则**：解释复杂逻辑，使用 `//` 注释

```tsx
// 获取当前组织 ID（从 JWT Token 或状态管理）
const tenantId = useModel('user')?.tenantId;

// 如果组织 ID 为空，显示错误提示
if (!tenantId) {
  message.error('请先选择组织');
  return;
}

// 查询用户列表（自动过滤组织）
const { data } = useUserList({ tenantId });

// 计算总金额（避免浮点数精度问题）
const totalAmount = order.items.reduce((sum, item) => {
  return sum + item.quantity * item.price;
}, 0);
```

### 复杂逻辑注释

**规则**：对复杂算法或业务逻辑添加详细注释

```tsx
/**
 * 计算订单总金额
 * 
 * 计算逻辑：
 * 1. 遍历订单所有明细项
 * 2. 计算每项金额（数量 × 单价）
 * 3. 应用折扣（如果有）
 * 4. 累加所有明细项金额
 * 5. 返回总金额
 */
const calculateOrderTotal = (order: Order): number => {
  // 1. 计算每项金额并累加
  let total = 0;
  order.items.forEach((item) => {
    const itemAmount = item.quantity * item.price;
    total += itemAmount;
  });
  
  // 2. 应用折扣（如果有）
  if (order.discountRate) {
    const discount = total * (order.discountRate / 100);
    total -= discount;
  }
  
  return total;
};
```

### TODO 注释

**规则**：使用 `TODO`、`FIXME`、`NOTE`、`WARNING` 等标签

```tsx
// TODO: 添加用户头像上传功能
// TODO: 实现用户批量导入功能
// FIXME: 修复时区问题，当前使用 UTC 时间
// NOTE: 此组件需要优化性能，当数据量超过 1000 时可能较慢
// WARNING: 此操作会删除所有关联数据，请谨慎使用
```

### 条件注释

**规则**：对复杂条件添加注释说明

```tsx
// 判断用户是否有权限（超级管理员或组织管理员）
if (user.isSuperuser || (user.tenantId === currentTenantId && user.isAdmin)) {
  // 显示管理功能
}

// 判断订单状态是否允许编辑（待审核或待生产状态）
const canEdit = order.status === 'pending' || order.status === 'reviewing';
```

## 📝 注释模板

### Python 类模板

```python
class ClassName:
    """
    类说明
    
    详细描述类的用途和功能。
    
    Attributes:
        attr1: 属性1说明
        attr2: 属性2说明
        
    Example:
        >>> obj = ClassName()
        >>> obj.method()
    """
```

### Python 函数模板

```python
async def function_name(
    param1: type1,
    param2: type2 = default_value
) -> ReturnType:
    """
    函数说明
    
    详细描述函数的用途和功能。
    
    Args:
        param1: 参数1说明
        param2: 参数2说明（可选）
        
    Returns:
        ReturnType: 返回值说明
        
    Raises:
        ExceptionType: 异常说明
        
    Example:
        >>> result = await function_name(value1, value2)
        >>> print(result)
    """
```

### TypeScript 函数模板

```typescript
/**
 * 函数说明
 * 
 * 详细描述函数的用途和功能。
 * 
 * @param param1 - 参数1说明
 * @param param2 - 参数2说明（可选）
 * @returns 返回值说明
 * 
 * @example
 * ```typescript
 * const result = functionName(value1, value2);
 * console.log(result);
 * ```
 * 
 * @throws {Error} 异常说明
 */
export function functionName(
  param1: Type1,
  param2?: Type2
): ReturnType {
  // ...
}
```

### React 组件模板

```tsx
/**
 * 组件说明
 * 
 * 详细描述组件的用途和功能。
 * 
 * @param props - 组件属性
 * @param props.prop1 - 属性1说明
 * @param props.prop2 - 属性2说明（可选）
 * 
 * @example
 * ```tsx
 * <ComponentName
 *   prop1={value1}
 *   prop2={value2}
 * />
 * ```
 */
interface ComponentNameProps {
  prop1: Type1;
  prop2?: Type2;
}

export default function ComponentName({
  prop1,
  prop2,
}: ComponentNameProps) {
  // ...
}
```

## ✅ 注释检查清单

### 代码审查时检查

- [ ] 所有公共类是否有注释？
- [ ] 所有公共函数是否有注释？
- [ ] 复杂逻辑是否有注释？
- [ ] 参数和返回值是否有说明？
- [ ] 异常情况是否有说明？
- [ ] 是否有使用示例？
- [ ] 注释是否与代码同步更新？
- [ ] 注释是否清晰、准确？

## 📚 相关文档

- [2.字段命名规范.md](./2.字段命名规范.md) - 命名规范
- [3.数据库命名规范.md](./3.数据库命名规范.md) - 数据库命名规范

