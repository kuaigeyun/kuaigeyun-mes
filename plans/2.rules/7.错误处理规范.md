# RiverEdge SaaS 多组织框架 - 错误处理规范

## 📋 概述

本文档定义 **RiverEdge SaaS 多组织框架 (RiverEdge SaaS Multi-tenant Framework)** 的**统一错误处理规范**，确保错误信息一致、错误处理统一，提升开发效率和用户体验。

**创建日期**：2025-11-17

## 🎯 设计原则

1. **统一格式**：所有错误使用统一的响应格式
2. **清晰明确**：错误信息清晰，便于定位问题
3. **分层处理**：后端异常处理、前端错误拦截
4. **用户友好**：前端错误提示用户友好
5. **日志记录**：所有错误记录日志，便于排查
6. **绝对禁止 PowerShell**：全程严禁使用 PowerShell 进行任何操作 ⭐ **重要**

## 🔧 后端错误处理

### 自定义异常类

**位置**：`src/core/exceptions.py`

**异常类定义**：
```python
"""
自定义异常类
"""
from fastapi import HTTPException, status


class BaseAPIException(HTTPException):
    """基础 API 异常类"""
    
    def __init__(
        self,
        status_code: int,
        detail: str,
        code: int = None,
        headers: dict = None
    ):
        super().__init__(status_code=status_code, detail=detail, headers=headers)
        self.code = code or status_code


class ValidationError(BaseAPIException):
    """数据验证错误（400）"""
    
    def __init__(self, detail: str = "数据验证失败"):
        super().__init__(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=detail,
            code=400
        )


class UnauthorizedError(BaseAPIException):
    """未授权错误（401）"""
    
    def __init__(self, detail: str = "未授权，请先登录"):
        super().__init__(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail=detail,
            code=401
        )


class PermissionDeniedError(BaseAPIException):
    """权限不足错误（403）"""
    
    def __init__(self, detail: str = "权限不足"):
        super().__init__(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=detail,
            code=403
        )


class NotFoundError(BaseAPIException):
    """资源不存在错误（404）"""
    
    def __init__(self, detail: str = "资源不存在"):
        super().__init__(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=detail,
            code=404
        )


class ConflictError(BaseAPIException):
    """资源冲突错误（409）"""
    
    def __init__(self, detail: str = "资源冲突"):
        super().__init__(
            status_code=status.HTTP_409_CONFLICT,
            detail=detail,
            code=409
        )


class InternalServerError(BaseAPIException):
    """服务器内部错误（500）"""
    
    def __init__(self, detail: str = "服务器内部错误"):
        super().__init__(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            detail=detail,
            code=500
        )
```

### 异常处理中间件

**位置**：`src/app/middleware.py`

**统一异常处理**：
```python
"""
异常处理中间件
"""
from fastapi import Request, status
from fastapi.responses import JSONResponse
from fastapi.exceptions import RequestValidationError
from starlette.exceptions import HTTPException as StarletteHTTPException
from core.exceptions import BaseAPIException
from utils.logger import logger
from datetime import datetime


async def exception_handler(request: Request, exc: Exception):
    """统一异常处理"""
    
    # 自定义异常
    if isinstance(exc, BaseAPIException):
        logger.error(f"API Error: {exc.detail}", exc_info=exc)
        return JSONResponse(
            status_code=exc.status_code,
            content={
                "code": exc.code,
                "message": exc.detail,
                "timestamp": datetime.now().isoformat()
            }
        )
    
    # FastAPI 验证错误
    if isinstance(exc, RequestValidationError):
        errors = []
        for error in exc.errors():
            field = ".".join(str(loc) for loc in error["loc"])
            errors.append({
                "field": field,
                "message": error["msg"]
            })
        logger.warning(f"Validation Error: {errors}")
        return JSONResponse(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            content={
                "code": 422,
                "message": "数据验证失败",
                "detail": errors,
                "timestamp": datetime.now().isoformat()
            }
        )
    
    # HTTP 异常
    if isinstance(exc, StarletteHTTPException):
        logger.error(f"HTTP Error: {exc.status_code} - {exc.detail}")
        return JSONResponse(
            status_code=exc.status_code,
            content={
                "code": exc.status_code,
                "message": exc.detail,
                "timestamp": datetime.now().isoformat()
            }
        )
    
    # 未知异常
    logger.exception(f"Unhandled Exception: {str(exc)}")
    return JSONResponse(
        status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
        content={
            "code": 500,
            "message": "服务器内部错误",
            "detail": "An unexpected error occurred" if request.app.debug else None,
            "timestamp": datetime.now().isoformat()
        }
    )
```

**注册中间件**：
```python
# src/server/main.py
from fastapi import FastAPI
from app.middleware import exception_handler

app = FastAPI()

# 注册异常处理器
app.add_exception_handler(Exception, exception_handler)
```

### 使用示例

**在 API 中使用**：
```python
from fastapi import APIRouter, Depends
from core.exceptions import NotFoundError, ValidationError, PermissionDeniedError
from core.dependencies import get_current_user

router = APIRouter()

@router.get("/users/{user_id}")
async def get_user(
    user_id: int,
    current_user = Depends(get_current_user)
):
    """获取用户详情"""
    user = await UserService.get_by_id(user_id)
    
    # ✅ 正确：使用自定义异常
    if not user:
        raise NotFoundError("用户不存在")
    
    # ✅ 正确：权限检查
    if user.tenant_id != current_user.tenant_id:
        raise PermissionDeniedError("无权访问该用户")
    
    return ResponseModel(data=user)
```

**在 Service 中使用**：
```python
from core.exceptions import NotFoundError, ConflictError

class UserService:
    @staticmethod
    async def get_by_id(user_id: int, tenant_id: int):
        """获取用户"""
        user = await User.filter(id=user_id, tenant_id=tenant_id).first()
        if not user:
            raise NotFoundError("用户不存在")
        return user
    
    @staticmethod
    async def create(data: UserCreate, tenant_id: int):
        """创建用户"""
        # 检查用户名是否已存在
        existing = await User.filter(
            username=data.username,
            tenant_id=tenant_id
        ).first()
        if existing:
            raise ConflictError("用户名已存在")
        
        user = await User.create(**data.dict(), tenant_id=tenant_id)
        return user
```

## 🎨 前端错误处理

### TanStack Query 错误处理

**位置**：`src/services/api.ts`

**错误处理配置**：
```typescript
/**
 * TanStack Query 错误处理
 */
import { message, Modal } from 'antd';
import { useNavigate } from 'react-router-dom';

/**
 * 错误处理
 */
const errorHandler = (error: any) => {
  const { response } = error;

  if (response && response.status) {
    const { status, data } = response;
    
    switch (status) {
      case 400:
        // 请求参数错误
        message.error(data?.message || '请求参数错误');
        break;
      
      case 401:
        // 未授权，跳转到登录页
        message.error('未授权，请先登录');
        // 清除 Token
        localStorage.removeItem('token');
        history.push('/login');
        break;
      
      case 403:
        // 权限不足
        message.error(data?.message || '权限不足');
        break;
      
      case 404:
        // 资源不存在
        message.error(data?.message || '资源不存在');
        break;
      
      case 409:
        // 资源冲突
        message.error(data?.message || '资源冲突');
        break;
      
      case 422:
        // 数据验证失败
        const errors = data?.detail || [];
        if (Array.isArray(errors) && errors.length > 0) {
          const errorMsg = errors.map((e: any) => e.message).join(', ');
          message.error(`数据验证失败: ${errorMsg}`);
        } else {
          message.error(data?.message || '数据验证失败');
        }
        break;
      
      case 500:
        // 服务器内部错误
        message.error('服务器内部错误，请稍后重试');
        break;
      
      default:
        message.error(data?.message || '请求失败，请稍后重试');
    }
  } else {
    // 网络错误
    message.error('网络错误，请检查网络连接');
  }
  
  throw error;
};

/**
 * Request 配置
 */
export const requestConfig: RequestConfig = {
  errorHandler,
  // 其他配置...
};
```

### 组件中使用

**使用 try-catch**：
```typescript
import { message } from 'antd';
import { createUser } from '@/services/userService';

const handleCreate = async (data: UserCreate) => {
  try {
    const response = await createUser(data);
    if (response.code === 200) {
      message.success('创建成功');
      // 刷新列表
      fetchUsers();
    }
  } catch (error: any) {
    // 错误已在拦截器中处理，这里可以添加额外处理
    console.error('创建用户失败:', error);
  }
};
```

**使用 ProTable 自动处理**：
```typescript
import { ProTable } from '@ant-design/pro-components';
import { getUserList } from '@/services/userService';

export default function UserList() {
  return (
    <ProTable
      request={async (params) => {
        // ProTable 会自动处理错误
        const response = await getUserList(params);
        return {
          data: response.data?.items || [],
          total: response.data?.total || 0,
          success: response.code === 200,
        };
      }}
      columns={[...]}
    />
  );
}
```

## 📊 错误码定义

### HTTP 状态码映射

| HTTP 状态码 | 业务错误码 | 说明 | 使用场景 |
|------------|-----------|------|----------|
| `200` | `200` | 成功 | 请求成功 |
| `400` | `400` | 请求参数错误 | 参数验证失败 |
| `401` | `401` | 未授权 | Token 无效或过期 |
| `403` | `403` | 权限不足 | 无权限访问资源 |
| `404` | `404` | 资源不存在 | 资源不存在 |
| `409` | `409` | 资源冲突 | 资源已存在（如用户名重复） |
| `422` | `422` | 数据验证失败 | Pydantic 验证失败 |
| `500` | `500` | 服务器内部错误 | 服务器异常 |

### 业务错误码（可选）

**说明**：可以在 `code` 字段中使用业务错误码，但必须配合 HTTP 状态码使用

**示例**：
```json
{
  "code": 10001,  // 业务错误码
  "message": "用户不存在",
  "detail": "用户 ID 123 不存在"
}
```

**错误码范围**：
- `10001-10099`: 通用业务错误（HTTP 400）
- `10101-10199`: 认证相关错误（HTTP 401）
- `10201-10299`: 权限相关错误（HTTP 403）
- `10301-10399`: 资源不存在错误（HTTP 404）
- `10401-10499`: 资源冲突错误（HTTP 409）

## 📝 错误响应格式

### 标准错误响应

```json
{
  "code": 404,
  "message": "用户不存在",
  "timestamp": "2025-11-17T10:00:00Z"
}
```

### 验证错误响应

```json
{
  "code": 422,
  "message": "数据验证失败",
  "detail": [
    {
      "field": "email",
      "message": "邮箱格式不正确"
    },
    {
      "field": "password",
      "message": "密码长度至少 8 位"
    }
  ],
  "timestamp": "2025-11-17T10:00:00Z"
}
```

## 🔍 日志记录

### 后端日志

**使用 loguru**：
```python
from utils.logger import logger

try:
    user = await UserService.get_by_id(user_id)
except NotFoundError as e:
    logger.error(f"User not found: user_id={user_id}, error={e.detail}")
    raise
except Exception as e:
    logger.exception(f"Unexpected error: {str(e)}")
    raise InternalServerError("服务器内部错误")
```

### 前端日志

**使用 console.error**：
```typescript
try {
  await createUser(data);
} catch (error) {
  console.error('创建用户失败:', error);
  // 错误已在拦截器中处理
}
```

## ✅ 检查清单

实现错误处理时，请确保：

- [ ] 使用自定义异常类（后端）
- [ ] 注册异常处理中间件（后端）
- [ ] 配置错误拦截器（前端）
- [ ] 错误信息清晰明确
- [ ] 错误日志已记录
- [ ] 用户友好的错误提示（前端）

## 📚 相关文档

- [6.API设计规范.md](./6.API设计规范.md) - API 设计规范（包含错误响应格式）
- [2.字段命名规范.md](./2.字段命名规范.md) - 命名规范
- [4.注释规范.md](./4.注释规范.md) - 注释规范
- [AGENTS.md](./AGENTS.md) - AI 助手开发规范

---

**最后更新**：2025-11-18

